class UserExperienceValidator { constructor() { this.testResults = []; this.userFlows = []; this.currentStep = 0; this.isRunning = false; } log(message, type = 'info') { const timestamp = new Date().toLocaleTimeString(); const colors = { 'info': 'color: blue', 'success': 'color: green; font-weight: bold', 'error': 'color: red; font-weight: bold', 'warn': 'color: orange; font-weight: bold', 'user': 'color: purple; font-weight: bold' };  this.testResults.push({ timestamp, message, type, step: this.currentStep }); } async sleep(ms) { return new Promise(resolve => setTimeout(resolve, ms)); } async testFirstTimeVisit() { this.log('🚀 开始测试：首次访问用户流程', 'user'); this.currentStep = 1; try { this.log('步骤1：检查页面加载状态', 'info'); await this.sleep(1000); const chatContainer = document.getElementById('chatContainer'); const messageInput = document.getElementById('messageInput'); const sendButton = document.getElementById('sendButton'); if (!chatContainer || !messageInput || !sendButton) { throw new Error('关键UI元素缺失'); } this.log('✅ 页面基本元素加载正常', 'success'); this.log('步骤2：检查登录流程', 'info'); await this.sleep(500); const loginModal = document.getElementById('loginModal'); const guestModeBtn = document.getElementById('guestModeBtn'); if (loginModal && guestModeBtn) { this.log('✅ 登录模态框存在', 'success'); this.log('🖱️ 模拟点击访客模式按钮', 'user'); guestModeBtn.click(); await this.sleep(1000); const guestMode = localStorage.getItem('guestMode'); if (guestMode === 'true') { this.log('✅ 访客模式激活成功', 'success'); } else { this.log('⚠️ 访客模式可能未正确设置', 'warn'); } } else { this.log('⚠️ 登录模态框或访客按钮不存在', 'warn'); } await this.testBasicChatFlow(); return true; } catch (error) { this.log(`❌ 首次访问流程失败: ${error.message}`, 'error'); return false; } } async testBasicChatFlow() { this.log('步骤3：测试基本聊天功能', 'info'); this.currentStep = 3; const messageInput = document.getElementById('messageInput'); const sendButton = document.getElementById('sendButton'); const chatContainer = document.getElementById('chatContainer'); if (!messageInput || !sendButton || !chatContainer) { throw new Error('聊天组件不完整'); } const initialMessageCount = chatContainer.querySelectorAll('.message').length; this.log(`初始消息数量: ${initialMessageCount}`, 'info'); this.log('🖱️ 发送测试消息1: "你好"', 'user'); messageInput.value = '你好'; await this.sleep(500); sendButton.click(); await this.sleep(2000); let currentMessageCount = chatContainer.querySelectorAll('.message').length; if (currentMessageCount > initialMessageCount) { this.log('✅ 消息1发送成功，界面有更新', 'success'); } else { this.log('⚠️ 消息1可能没有正确显示', 'warn'); } this.log('🖱️ 发送测试消息2: 包含**粗体**的消息', 'user'); messageInput.value = '这是一条包含**粗体**和`代码`的测试消息'; await this.sleep(500); sendButton.click(); await this.sleep(2000); const finalMessageCount = chatContainer.querySelectorAll('.message').length; if (finalMessageCount > currentMessageCount) { this.log('✅ 消息2发送成功，Markdown格式测试完成', 'success'); } else { this.log('⚠️ 消息2可能没有正确显示', 'warn'); } const messages = chatContainer.querySelectorAll('.message'); let formattedMessagesFound = 0; messages.forEach(msg => { if (msg.innerHTML.includes('<strong>') || msg.innerHTML.includes('<code>')) { formattedMessagesFound++; } }); if (formattedMessagesFound > 0) { this.log('✅ 发现格式化消息，Markdown处理正常', 'success'); } else { this.log('⚠️ 未发现格式化消息，Markdown处理可能有问题', 'warn'); } this.log('🖱️ 测试回车键发送功能', 'user'); messageInput.value = '回车键测试消息'; await this.sleep(500); const enterEvent = new KeyboardEvent('keydown', { key: 'Enter', code: 'Enter', which: 13, keyCode: 13 }); messageInput.dispatchEvent(enterEvent); await this.sleep(2000); const afterEnterCount = chatContainer.querySelectorAll('.message').length; if (afterEnterCount > finalMessageCount) { this.log('✅ 回车键发送功能正常', 'success'); } else { this.log('⚠️ 回车键发送功能可能有问题', 'warn'); } } async testUIInteractions() { this.log('🧪 测试UI交互功能', 'user'); this.currentStep = 4; try { const settingsBtn = document.getElementById('settingsBtn'); if (settingsBtn) { this.log('🖱️ 测试设置按钮', 'user'); settingsBtn.click(); await this.sleep(1000); this.log('✅ 设置按钮可点击', 'success'); } else { this.log('⚠️ 设置按钮不存在', 'warn'); } const historyBtn = document.getElementById('historyBtn'); if (historyBtn) { this.log('🖱️ 测试历史按钮', 'user'); historyBtn.click(); await this.sleep(1000); this.log('✅ 历史按钮可点击', 'success'); } else { this.log('⚠️ 历史按钮不存在', 'warn'); } const languageBtn = document.getElementById('languageBtn'); if (languageBtn) { this.log('🖱️ 测试语言切换按钮', 'user'); languageBtn.click(); await this.sleep(1000); this.log('✅ 语言切换按钮可点击', 'success'); } else { this.log('⚠️ 语言切换按钮不存在', 'warn'); } return true; } catch (error) { this.log(`❌ UI交互测试失败: ${error.message}`, 'error'); return false; } } async testErrorHandling() { this.log('🧪 测试错误处理机制', 'user'); this.currentStep = 5; try { this.log('🖱️ 测试发送空消息', 'user'); const messageInput = document.getElementById('messageInput'); const sendButton = document.getElementById('sendButton'); messageInput.value = ''; sendButton.click(); await this.sleep(1000); this.log('✅ 空消息处理测试完成', 'success'); this.log('🖱️ 测试发送极长消息', 'user'); const longMessage = 'A'.repeat(10000); messageInput.value = longMessage; sendButton.click(); await this.sleep(2000); this.log('✅ 长消息处理测试完成', 'success'); return true; } catch (error) { this.log(`❌ 错误处理测试失败: ${error.message}`, 'error'); return false; } } async testResponsiveDesign() { this.log('🧪 测试响应式设计', 'user'); this.currentStep = 6; try { const originalWidth = window.innerWidth; this.log('📱 模拟移动设备视口', 'user'); window.resizeTo(375, 667); await this.sleep(1000); const chatContainer = document.getElementById('chatContainer'); const messageInput = document.getElementById('messageInput'); if (chatContainer && messageInput) { const containerRect = chatContainer.getBoundingClientRect(); const inputRect = messageInput.getBoundingClientRect(); if (containerRect.width > 0 && inputRect.width > 0) { this.log('✅ 移动设备布局正常', 'success'); } else { this.log('⚠️ 移动设备布局可能有问题', 'warn'); } } window.resizeTo(originalWidth, window.innerHeight); await this.sleep(500); return true; } catch (error) { this.log(`❌ 响应式设计测试失败: ${error.message}`, 'error'); return false; } } generateUXReport() { const report = { timestamp: new Date().toISOString(), totalTests: this.testResults.length, successCount: this.testResults.filter(r => r.type === 'success').length, errorCount: this.testResults.filter(r => r.type === 'error').length, warnCount: this.testResults.filter(r => r.type === 'warn').length, userScore: 0, results: this.testResults }; const totalNonInfoMessages = report.successCount + report.errorCount + report.warnCount; if (totalNonInfoMessages > 0) { report.userScore = Math.round((report.successCount / totalNonInfoMessages) * 100); } console.log('\n' + '='.repeat(60));  console.log('='.repeat(60)); console.log(`🕒 测试时间: ${new Date().toLocaleString()}`);      let grade = 'F'; if (report.userScore >= 90) grade = 'A+'; else if (report.userScore >= 80) grade = 'A'; else if (report.userScore >= 70) grade = 'B'; else if (report.userScore >= 60) grade = 'C'; else if (report.userScore >= 50) grade = 'D';  if (report.errorCount > 0) {  this.testResults .filter(r => r.type === 'error') .forEach((result, index) => {  }); } if (report.warnCount > 0) {  this.testResults .filter(r => r.type === 'warn') .forEach((result, index) => {  }); }  if (report.userScore >= 90) {  } else if (report.userScore >= 70) {  } else {   } console.log('='.repeat(60)); return report; } async runFullUXTest() { if (this.isRunning) { this.log('测试已在运行中...', 'warn'); return; } this.isRunning = true; this.testResults = []; this.currentStep = 0;  try { await this.testFirstTimeVisit(); await this.sleep(1000); await this.testUIInteractions(); await this.sleep(1000); await this.testErrorHandling(); await this.sleep(1000); await this.testResponsiveDesign(); const report = this.generateUXReport(); window.uxTestReport = report; this.log('🏁 用户体验测试完成', 'success'); return report; } catch (error) { this.log(`❌ 用户体验测试异常: ${error.message}`, 'error'); return null; } finally { this.isRunning = false; } } } document.addEventListener('DOMContentLoaded', async () => { await new Promise(resolve => setTimeout(resolve, 5000));  const validator = new UserExperienceValidator(); await validator.runFullUXTest(); window.uxValidator = validator; console.log('💡 提示: 使用 uxValidator.runFullUXTest() 重新运行用户体验测试'); }); if (document.readyState !== 'loading') { setTimeout(async () => {  const validator = new UserExperienceValidator(); await validator.runFullUXTest(); window.uxValidator = validator; console.log('💡 提示: 使用 uxValidator.runFullUXTest() 重新运行用户体验测试'); }, 5000); }