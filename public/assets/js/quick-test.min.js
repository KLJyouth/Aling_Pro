try {
function quickTest() {  const elements = [ 'loginModal', 'messageInput', 'sendButton', 'chatMessages', 'guestModeButton', 'userStatus', 'recordButton', 'imageGenButton' ];  elements.forEach(id => { const exists = !!document.getElementById(id);  });  const hasChat = !!window.chatInstance; const hasModules = hasChat && window.chatInstance.core && window.chatInstance.ui && window.chatInstance.api;    const guestBtn = document.getElementById('guestModeButton'); if (guestBtn) { guestBtn.click(); setTimeout(() => { const isGuest = localStorage.getItem('guestMode') === 'true';   const input = document.getElementById('messageInput'); const sendBtn = document.getElementById('sendButton'); if (input && sendBtn) { input.value = ''; input.dispatchEvent(new Event('input')); const emptyDisabled = sendBtn.disabled;  input.value = '测试'; input.dispatchEvent(new Event('input')); const hasContentEnabled = !sendBtn.disabled;  input.value = ''; } }, 500); }  } if (document.readyState === 'complete') { setTimeout(quickTest, 1000); } else { window.addEventListener('load', () => setTimeout(quickTest, 1000)); } window.quickTest = quickTest;
} catch (error) {
    console.error(error);
    // 处理错误
}
