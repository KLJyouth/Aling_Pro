<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>珑凌科技 | 量子安全·智能未来</title>
    <meta name="description" content="珑凌科技 - 全球领先的量子安全基础设施提供商，为企业级客户提供从底层加密到智能决策的全栈解决方案">
    <meta name="keywords" content="珑凌科技,量子安全,人工智能,量子计算,数据安全">
    
    <!-- 核心资源 -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" rel="stylesheet">
    <link href="assets/css/styles.css" rel="stylesheet">
    <link href="assets/css/minimalist-backgrounds.css" rel="stylesheet">
    <link href="assets/css/quantum-styles.css" rel="stylesheet">
    <link href="assets/css/quantum-chat-animations.css" rel="stylesheet">
    <link href="assets/css/modal-fixes.css" rel="stylesheet">
    <link href="assets/css/input-theme-fixes.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>/three.js/r128/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>
    <!-- 字体预加载 -->
    <!-- 字体预加载 -->load" href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;500;600&family=Fira+Code&display=swap" as="style">
    <link rel="preload" href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;500;600&family=Fira+Code&display=swap" as="style">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;700&family=Inter:wght@400;500;600&family=Fira+Code&display=swap">
    <!-- 已合并到优化文件中的脚本，移除以避免重复声明 -->
    <!-- 已合并到优化文件中的脚本，移除以避免重复声明 -->
    <!-- 优化合并的JavaScript文件 -->
    <!-- 优化合并的JavaScript文件 -->imized/core-quantum.js"></script>
    <script src="assets/js/optimized/core-quantum.js"></script>cript>
    <script src="assets/js/optimized/background-systems.js"></script>
    <script src="assets/js/optimized/ui-components.js"></script>ript>
    <script src="assets/js/optimized/validation-testing.js"></script>
    <script src="assets/js/frontend-fixes.js"></script>ript>
    <script src="assets/js/frontend-validation.js"></script>
    <script src="assets/js/chat-system.js"></script>pt>
    <script src="assets/js/homepage-fixes.js"></script>t>
    <script src="assets/js/fix-verification.js"></script>
    <!-- 已合并到优化文件中，移除以避免重复 -->
    <!-- 新增国际化和AI对话功能 -->
    <script src="assets/js/i18n/translations.js"></script>
    <script src="assets/js/i18n/i18n-manager.js"></script>
    <script src="assets/js/i18n/language-switcher.js"></script>
    <script src="assets/js/chat/ai-chat-handler.js"></script>
    
    <!-- 高对比度和无障碍样式 -->
    <script src="assets/js/homepage-ai-chat.js"></script>
    gh-contrast {
    <!-- 已合并到优化文件中，移除以避免重复 -->rast(150%) brightness(120%);

    <!-- 高对比度和无障碍样式 -->
    <style>.high-contrast * {
        .high-contrast {px 1px 1px rgba(0,0,0,0.5) !important;
            filter: contrast(150%) brightness(120%);
        }
        .high-contrast .bg-gradient-to-r,
        .high-contrast * {,
            text-shadow: 1px 1px 1px rgba(0,0,0,0.5) !important;{
        }
        
        .high-contrast .bg-gradient-to-r,
        .high-contrast .bg-gradient-to-br,
        .high-contrast .bg-gradient-to-tr {.high-contrast .text-gray-600,
            background: #000 !important;
            color: #fff !important;{
        }
        
        .high-contrast .text-gray-600,
        .high-contrast .text-gray-500,.high-contrast .bg-white {
        .high-contrast .text-gray-400 {rtant;
            color: #fff !important;
        }
        
        .high-contrast .bg-white {.high-contrast .bg-gray-50,
            background: #000 !important; {
            color: #fff !important;nt;
        }
        
        .high-contrast .bg-gray-50,
        .high-contrast .bg-gray-100 {.focus:focus {
            background: #333 !important;px solid #00ffff !important;
            color: #fff !important;
        }le>
        
        .focus:focus {    <!-- 自定义样式 -->
            outline: 2px solid #00ffff !important;ext/tailwindcss">
        }
    </style>{
ibility: auto;
    <!-- 自定义样式 -->
    <style type="text/tailwindcss">text-gradient {
        @layer utilities {lip: text;
            .content-auto {p: text;
                content-visibility: auto;
            }
            .text-gradient {bg-grid {
                background-clip: text;ound-image: linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
                -webkit-background-clip: text;t 1px);
                color: transparent;
            }
            .bg-grid {scrollbar-hidden::-webkit-scrollbar {
                background-image: linear-gradient(rgba(255, 255, 255, 0.05) 1px, transparent 1px),
                                  linear-gradient(90deg, rgba(255, 255, 255, 0.05) 1px, transparent 1px);
                background-size: 20px 20px;scrollbar-hidden {
            }yle: none;
            .scrollbar-hidden::-webkit-scrollbar {
                display: none;
            }
            .scrollbar-hidden {le>
                -ms-overflow-style: none;
                scrollbar-width: none;y {
            }ckground: linear-gradient(135deg, #0A0016 0%, #1A003C 100%);
        }
    </style>
    <style>
        body {
            background: linear-gradient(135deg, #0A0016 0%, #1A003C 100%);glass-card {
            color: rgba(255, 255, 255, 0.95);d: radial-gradient(circle at 10% 20%, rgba(26, 0, 60, 0.4) 0%, rgba(10, 0, 22, 0.2) 100%);
            overflow-x: hidden;
            position: relative;
        }255, 255, 0.12);
        .glass-card {
            background: radial-gradient(circle at 10% 20%, rgba(26, 0, 60, 0.4) 0%, rgba(10, 0, 22, 0.2) 100%);
            -webkit-backdrop-filter: blur(24px);0px rgba(108, 19, 255, 0.1),
            backdrop-filter: blur(24px);
            border: 1px solid rgba(255, 255, 255, 0.12);55, 0.15);
            border-radius: 24px;, 0.45, 0.94);
            box-shadow: 
                0 20px 40px rgba(108, 19, 255, 0.1),
                0 1px 3px rgba(0, 0, 0, 0.12),
                inset 0 1px 0 rgba(255, 255, 255, 0.15);
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);.glass-card::before {
            position: relative;
            overflow: hidden;solute;
        }
        100%;
        .glass-card::before {
            content: '';;
            position: absolute;inear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;.glass-card:hover {
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);slateY(-12px) scale(1.02);
            transition: left 0.6s ease;
        }
        0px rgba(108, 19, 255, 0.2),
        .glass-card:hover {
            transform: translateY(-12px) scale(1.02);2);
            border-color: rgba(0, 212, 255, 0.3);
            box-shadow: 
                0 30px 60px rgba(108, 19, 255, 0.2),.glass-card:hover::before {
                0 10px 20px rgba(0, 212, 255, 0.15),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }code-banner {
        y: 'Fira Code', monospace;
        .glass-card:hover::before {00D4FF 0%, #FF2B75 100%);
            left: 100%;
        }
        .code-banner {
            font-family: 'Fira Code', monospace;
            background: linear-gradient(90deg, #00D4FF 0%, #FF2B75 100%);ck;
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            position: relative;code-banner::before {
            display: inline-block;
            padding-left: 24px;lute;
            margin: 24px 0;
        }gba(0, 212, 255, 0.5);
        .code-banner::before {
            content: '//';code-comment {
            position: absolute;: 'Fira Code', monospace;
            left: 0;
            color: rgba(0, 212, 255, 0.5);
        }
        .code-comment {
            font-family: 'Fira Code', monospace;        /* 高级动画效果 */
            color: rgba(255, 255, 255, 0.4);atrix-rain {
            letter-spacing: 1px;slateY(-100%); opacity: 0; }
        }
ranslateY(100%); opacity: 0; }
        /* 高级动画效果 */
        @keyframes matrix-rain {
            0% { transform: translateY(-100%); opacity: 0; }        @keyframes quantum-flicker {
            50% { opacity: 1; }; filter: brightness(1); }
            100% { transform: translateY(100%); opacity: 0; }
        }

        @keyframes quantum-flicker {        @keyframes orbital-spin {
            0%, 100% { opacity: 0.95; filter: brightness(1); }ate(0deg) translateX(50px) rotate(0deg); }
            50% { opacity: 0.5; filter: brightness(1.5); }; }
        }

        @keyframes orbital-spin {        @keyframes wave {
            from { transform: rotate(0deg) translateX(50px) rotate(0deg); }ansform: translateY(0px); }
            to { transform: rotate(360deg) translateX(50px) rotate(-360deg); }
        }

        @keyframes wave {@keyframes float {
            0%, 100% { transform: translateY(0px); }nsform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-15px); }
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }@keyframes glow-pulse {
            33% { transform: translateY(-10px) rotate(1deg); }ow: 0 0 20px rgba(108, 19, 255, 0.3); }
            66% { transform: translateY(5px) rotate(-1deg); }
        }
        
        @keyframes glow-pulse {        /* 语音输入和表情面板样式 */
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.3); }{
            50% { box-shadow: 0 0 30px rgba(108, 19, 255, 0.6); }near-gradient(45deg, #ff4757, #ff6b7a) !important;
        }

        /* 语音输入和表情面板样式 */
        .voice-recording {        .voice-recording i {
            background: linear-gradient(45deg, #ff4757, #ff6b7a) !important;portant;
            animation: pulse 1.5s infinite;
        }
        @keyframes pulse {
        .voice-recording i {: scale(1); }
            color: white !important;; }
        }

        @keyframes pulse {
            0% { transform: scale(1); }        .emoji-panel {
            50% { transform: scale(1.1); }ilter: blur(12px);
            100% { transform: scale(1); }r(12px);
        }, 0.1);

        .emoji-panel {
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);        .emoji-btn {
            border: 1px solid rgba(255, 255, 255, 0.1);on: all 0.2s ease;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
        }d transparent;

        .emoji-btn {
            transition: all 0.2s ease;        .emoji-btn:hover {
            cursor: pointer;or: rgba(75, 85, 99, 0.5) !important;
            border: 1px solid transparent;
        }, 85, 247, 0.3);

        .emoji-btn:hover {
            background-color: rgba(75, 85, 99, 0.5) !important;        .emoji-grid {
            transform: scale(1.1);-width: thin;
            border-color: rgba(168, 85, 247, 0.3);168, 85, 247, 0.5) transparent;
        }

        .emoji-grid {        /* 量子球和聊天界面样式 */
            scrollbar-width: thin;tainer {
            scrollbar-color: rgba(168, 85, 247, 0.5) transparent; cubic-bezier(0.4, 0, 0.2, 1);
        }

        /* 量子球和聊天界面样式 */        #quantum-orb-container:hover {
        #quantum-orb-container {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }
        .quantum-orb {
        #quantum-orb-container:hover {relative;
            transform: scale(1.1);
        }

        .quantum-orb {        .orb-core {
            position: relative;und: linear-gradient(45deg, #6c13ff, #00d4ff);
            overflow: hidden;
        }

        .orb-core {        .orb-ring {
            background: linear-gradient(45deg, #6c13ff, #00d4ff);color: rgba(0, 212, 255, 0.6);
            transition: all 0.3s ease;
        }

        .orb-ring {        .orb-glow {
            border-color: rgba(0, 212, 255, 0.6);und: radial-gradient(circle, rgba(0, 212, 255, 0.4) 0%, transparent 70%);
            animation: spin 3s linear infinite;
        }

        .orb-glow {        /* 聊天界面样式 */
            background: radial-gradient(circle, rgba(0, 212, 255, 0.4) 0%, transparent 70%);ner {
            transition: opacity 0.3s ease;er: blur(20px);
        }r(20px);
4, 0, 0.2, 1);
        /* 聊天界面样式 */
        #chat-container {
            backdrop-filter: blur(20px);        .chat-messages {
            -webkit-backdrop-filter: blur(20px);dth: thin;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);0, 212, 255, 0.3) transparent;
        }

        .chat-messages {        .chat-messages::-webkit-scrollbar {
            scrollbar-width: thin;
            scrollbar-color: rgba(0, 212, 255, 0.3) transparent;
        }
        .chat-messages::-webkit-scrollbar-track {
        .chat-messages::-webkit-scrollbar {
            width: 4px;
        }
        .chat-messages::-webkit-scrollbar-thumb {
        .chat-messages::-webkit-scrollbar-track {
            background: transparent;
        }

        .chat-messages::-webkit-scrollbar-thumb {        .message {
            background: rgba(0, 212, 255, 0.3);ion: messageSlideIn 0.3s ease-out;
            border-radius: 2px;
        }
        @keyframes messageSlideIn {
        .message {
            animation: messageSlideIn 0.3s ease-out;acity: 0;
        }translateY(10px);

        @keyframes messageSlideIn {o {
            from {opacity: 1;
                opacity: 0;translateY(0);
                transform: translateY(10px);
            }
            to {
                opacity: 1;        /* 浮动聊天按钮样式 */
                transform: translateY(0);-button {
            }ur(10px);
        }r(10px);
nite;
        /* 浮动聊天按钮样式 */
        #floating-chat-button {
            backdrop-filter: blur(10px);        #floating-chat-button:hover {
            -webkit-backdrop-filter: blur(10px);
            animation: float 3s ease-in-out infinite;x rgba(0, 212, 255, 0.4);
        }

        #floating-chat-button:hover {        /* 登录模态框样式 */
            transform: scale(1.1);{
            box-shadow: 0 10px 25px rgba(0, 212, 255, 0.4);ilter: blur(8px);
        }ur(8px);

        /* 登录模态框样式 */
        #login-modal {        .login-modal-content {
            backdrop-filter: blur(8px);lur(20px);
            -webkit-backdrop-filter: blur(8px);r(20px);
        }t;

        .login-modal-content {
            backdrop-filter: blur(20px);        @keyframes modalSlideIn {
            -webkit-backdrop-filter: blur(20px);
            animation: modalSlideIn 0.3s ease-out;acity: 0;
        }scale(0.9) translateY(-20px);

        @keyframes modalSlideIn {o {
            from {opacity: 1;
                opacity: 0;scale(1) translateY(0);
                transform: scale(0.9) translateY(-20px);
            }
            to {
                opacity: 1;        /* 极简背景样式 */
                transform: scale(1) translateY(0);background {
            }0.5s ease;
        }

        /* 极简背景样式 */        .minimalist-grid {
        #minimalist-background {ge: 
            transition: opacity 0.5s ease;t(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
        }t 1px);

        .minimalist-grid {near infinite;
            background-image: 
                linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);        .minimalist-dots {
            background-size: 40px 40px;ge: radial-gradient(circle at 20px 20px, rgba(0, 212, 255, 0.1) 1px, transparent 1px);
            animation: gridShift 20s linear infinite;
        }-in-out infinite;

        .minimalist-dots {
            background-image: radial-gradient(circle at 20px 20px, rgba(0, 212, 255, 0.1) 1px, transparent 1px);        @keyframes gridShift {
            background-size: 80px 80px;anslate(0, 0); }
            animation: dotPulse 4s ease-in-out infinite;40px); }
        }

        @keyframes gridShift {        @keyframes dotPulse {
            0% { transform: translate(0, 0); }y: 0.3; }
            100% { transform: translate(40px, 40px); }
        }

        @keyframes dotPulse {        /* 通知样式增强 */
            0%, 100% { opacity: 0.3; }nContainer .notification {
            50% { opacity: 0.8; }
        }r(10px);
, 0.1);
        /* 通知样式增强 */
        #notificationContainer .notification {
            backdrop-filter: blur(10px);        .emoji-grid::-webkit-scrollbar {
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        .emoji-grid::-webkit-scrollbar-track {
        .emoji-grid::-webkit-scrollbar {
            width: 4px;
        }
        .emoji-grid::-webkit-scrollbar-thumb {
        .emoji-grid::-webkit-scrollbar-track {);
            background: transparent;
        }

        .emoji-grid::-webkit-scrollbar-thumb {        /* 聊天输入框增强样式 */
            background: rgba(168, 85, 247, 0.5);tainer {
            border-radius: 2px;
        }

        /* 聊天输入框增强样式 */        .chat-input {
        .chat-input-container {n: all 0.3s ease;
            position: relative;, 255, 255, 0.2);
        }

        .chat-input {        .chat-input:focus {
            transition: all 0.3s ease;gba(168, 85, 247, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.2);7, 0.1);
        }

        .chat-input:focus {        .char-counter {
            border-color: rgba(168, 85, 247, 0.5);0.75rem;
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);3, 175, 0.8);
        }

        .char-counter {
            font-size: 0.75rem;
            color: rgba(156, 163, 175, 0.8);
            position: absolute;        .char-counter.warning {
            bottom: -1.5rem;
            right: 0;
        }
        .char-counter.danger {
        .char-counter.warning {
            color: #fbbf24;
        }
        /* 按钮悬停效果 */
        .char-counter.danger {{
            color: #ef4444;n: all 0.2s ease;
        }

        /* 按钮悬停效果 */
        .action-btn {
            transition: all 0.2s ease;        .action-btn::before {
            position: relative;
            overflow: hidden;solute;
        }
;
        .action-btn::before {
            content: '';;
            position: absolute;: rgba(255, 255, 255, 0.1);
            top: 50%;
            left: 50%;e(-50%, -50%);
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;        .action-btn:hover::before {
            transform: translate(-50%, -50%);
            transition: all 0.3s ease;;
        }

        .action-btn:hover::before {        .action-btn:hover {
            width: 100%;slateY(-1px);
            height: 100%;
        }
        /* 在线状态指示器动画 */
        .action-btn:hover {ne-pulse {
            transform: translateY(-1px);; }
        }

        /* 在线状态指示器动画 */
        @keyframes online-pulse {        .online-indicator {
            0%, 100% { opacity: 1; }ne-pulse 2s infinite;
            50% { opacity: 0.5; }
        }
        /* 历史记录面板增强样式 */
        .online-indicator {
            animation: online-pulse 2s infinite;ter: blur(20px);
        }r(20px);
, 0.1);
        /* 历史记录面板增强样式 */
        .history-panel {
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);        .history-item {
            border: 1px solid rgba(255, 255, 255, 0.1); all 0.3s ease;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);ent;
        }

        .history-item {
            transition: all 0.3s ease;
            border: 1px solid transparent;        .history-item::before {
            position: relative;
            overflow: hidden;solute;
        }

        .history-item::before {3px;
            content: '';%;
            position: absolute;ransparent;
            left: 0;e;
            top: 0;
            width: 3px;
            height: 100%;        .history-item:hover {
            background: transparent;75, 85, 99, 0.3);
            transition: all 0.3s ease;.3);
        }

        .history-item:hover {
            background: rgba(75, 85, 99, 0.3);        .history-item:hover::before {
            border-color: rgba(168, 85, 247, 0.3);nt(to bottom, #a855f7, #3b82f6);
            transform: translateX(4px);
        }
        .history-item.chat::before {
        .history-item:hover::before {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
        }
        .history-item.login::before {
        .history-item.chat::before {
            background: #10b981;
        }
        .history-item.notification::before {
        .history-item.login::before {
            background: #3b82f6;
        }
        .filter-tag {
        .history-item.notification::before {n: all 0.2s ease;
            background: #f59e0b;
        }d rgba(255, 255, 255, 0.2);

        .filter-tag {
            transition: all 0.2s ease;        .filter-tag:hover {
            cursor: pointer;a(168, 85, 247, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.2););
        }

        .filter-tag:hover {
            background: rgba(168, 85, 247, 0.2);        .filter-tag.active {
            border-color: rgba(168, 85, 247, 0.5);ar-gradient(45deg, #a855f7, #3b82f6);
            transform: scale(1.05);
        }

        .filter-tag.active {
            background: linear-gradient(45deg, #a855f7, #3b82f6);        /* 搜索框样式 */
            border-color: transparent;ut {
            color: white; rgba(17, 24, 39, 0.6);
        }55, 0.2);

        /* 搜索框样式 */
        .search-input {
            background: rgba(17, 24, 39, 0.6);        .search-input:focus {
            border: 1px solid rgba(255, 255, 255, 0.2);17, 24, 39, 0.8);
            transition: all 0.3s ease;.5);
        }7, 0.1);

        .search-input:focus {
            background: rgba(17, 24, 39, 0.8);        /* 空状态样式 */
            border-color: rgba(168, 85, 247, 0.5);e {
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);.7;
        }pacity 0.3s ease;

        /* 空状态样式 */
        .empty-state {        .empty-state:hover {
            opacity: 0.7;
            transition: opacity 0.3s ease;
        }
        /* 导出和清空按钮样式 */
        .empty-state:hover {{
            opacity: 1;all 0.3s ease;
        }

        /* 导出和清空按钮样式 */
        .action-button {
            transition: all 0.3s ease;        .action-button::after {
            position: relative;
            overflow: hidden;solute;
        }
;
        .action-button::after {
            content: '';;
            position: absolute;: rgba(255, 255, 255, 0.1);
            top: 50%;
            left: 50%;e(-50%, -50%);
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;        .action-button:hover::after {
            transform: translate(-50%, -50%);
            transition: all 0.4s ease;;
        }

        .action-button:hover::after {        .action-button:hover {
            width: 300px;teY(-2px);
            height: 300px;0, 0, 0, 0.3);
        }

        .action-button:hover {        /* 响应式设计增强 */
            transform: translateY(-2px);idth: 768px) {
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }w !important;

        /* 响应式设计增强 */ant;
        @media (max-width: 768px) {t;
            .emoji-panel {50%);
                width: 90vw !important;
                max-width: 280px;
                left: 50% !important;            .history-panel {
                right: auto !important;!important;
                transform: translateX(-50%);;
            }

            .history-panel {
                width: 95vw !important;
                height: 80vh !important;            .chat-widget {
                left: 2.5vw !important;w !important;
                right: auto !important;;
            }

            .chat-widget {
                width: 95vw !important;
                height: 80vh !important;
                left: 2.5vw !important;
                right: auto !important;        /* 滚动条美化 */
                bottom: 2vh !important;ollbar::-webkit-scrollbar {
            }
        }

        /* 滚动条美化 */        .custom-scrollbar::-webkit-scrollbar-track {
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: rgba(17, 24, 39, 0.3);a855f7, #3b82f6);
            border-radius: 3px;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);, #2563eb);
            border-radius: 3px;
        }
        /* 加载动画 */
        .custom-scrollbar::-webkit-scrollbar-thumb:hover { loading-dots {
            background: linear-gradient(to bottom, #9333ea, #2563eb);2; }
        }
: 0.2; }
        /* 加载动画 */
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; }        .loading-dots span {
            50% { opacity: 1; }ng-dots 1.4s infinite;
            80%, 100% { opacity: 0.2; }
        }
        .loading-dots span:nth-child(2) {
        .loading-dots span {
            animation: loading-dots 1.4s infinite;
        }
        .loading-dots span:nth-child(3) {
        .loading-dots span:nth-child(2) {
            animation-delay: 0.2s;
        }
/* AI助手按钮发光动画 */
        .loading-dots span:nth-child(3) {ow {
            animation-delay: 0.4s;hadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
        }
        
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {@keyframes neon-flicker {
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }; text-shadow: 0 0 8px currentColor; }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); } 24px currentColor; }
        }
        
        @keyframes neon-flicker {@keyframes slide-in-left {
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }slateX(-100%); opacity: 0; }
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }
        
        @keyframes slide-in-left {@keyframes slide-in-right {
            from { transform: translateX(-100%); opacity: 0; }lateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        @keyframes slide-in-right {@keyframes scale-up {
            from { transform: translateX(100%); opacity: 0; } scale(0.8); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        @keyframes scale-up {@keyframes fade-in-up {
            from { transform: scale(0.8); opacity: 0; }ranslateY(30px); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }
        
        @keyframes fade-in-up {/* 动画类 */
            from { transform: translateY(30px); opacity: 0; }imation { animation: float 6s ease-in-out infinite; }
            to { transform: translateY(0); opacity: 1; }
        }ite; }
        te; }
        /* 动画类 */
        .float-animation { animation: float 6s ease-in-out infinite; } }
        .wave-animation { animation: wave 4s ease-in-out infinite; }
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }t; }
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }/* 触摸反馈 */
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }dback {
        .scale-up { animation: scale-up 0.6s ease-out; }ative;
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }
        
        /* 触摸反馈 */
        .touch-feedback {.touch-feedback::after {
            position: relative;
            overflow: hidden;solute;
        }
        ;
        .touch-feedback::after {
            content: '';;
            position: absolute;ius: 50%;
            top: 50%;5, 255, 255, 0.3);
            left: 50%;
            width: 0;6s;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);.touch-feedback:active::after {
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;;
        }
        
        .touch-feedback:active::after {/* 加载状态动画 */
            width: 300px;nner {
            height: 300px;e-block;
        }
        ;
        /* 加载状态动画 */olid rgba(108, 19, 255, 0.3);
        .loading-spinner {
            display: inline-block;6C13FF;
            width: 20px;-out infinite;
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);
            border-radius: 50%;@keyframes spin {
            border-top-color: #6C13FF;m: rotate(360deg); }
            animation: spin 1s ease-in-out infinite;
        }
        /* 进度条动画 */
        @keyframes spin {ar {
            to { transform: rotate(360deg); }elative;
        }
        
        /* 进度条动画 */
        .progress-bar {.progress-bar::after {
            position: relative;
            overflow: hidden;solute;
        }
        100%;
        .progress-bar::after {
            content: '';;
            position: absolute;inear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;@keyframes progress-shine {
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;}
        }
        
        @keyframes progress-shine {        /* 3D 变换和悬停效果 */
            0% { left: -100%; }
            100% { left: 100%; }tyle: preserve-3d;
        }
m 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        /* 3D 变换和悬停效果 */
        .quantum-card {
            transform-style: preserve-3d;        .quantum-card:hover {
            perspective: 1000px;ateZ(20px) rotateX(5deg) rotateY(5deg);
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }
        .quantum-text {
        .quantum-card:hover { linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);
        }

        .quantum-text {flicker 2s infinite;
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            -webkit-background-clip: text;
            background-clip: text;        .matrix-text {
            color: transparent;y: 'Fira Code', monospace;
            animation: quantum-flicker 2s infinite;5, 0.8);
        }

        .matrix-text {        /* 粒子系统容器 */
            font-family: 'Fira Code', monospace;ntainer {
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);e;
        }
;
        /* 粒子系统容器 */s: none;
        .particle-container {
            position: absolute;
            width: 100%;
            height: 100%;        /* 高级滚动动画 */
            pointer-events: none;al {
            overflow: hidden;
        }translateY(20px);
ezier(0.23, 1, 0.32, 1);
        /* 高级滚动动画 */
        .scroll-reveal {
            opacity: 0;        .scroll-reveal.visible {
            transform: translateY(20px);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);translateY(0);
        }

        .scroll-reveal.visible {        /* 全局动画性能优化 */
            opacity: 1;lerated,
            transform: translateY(0);,
        }
ry,
        /* 全局动画性能优化 */y,
        .hardware-accelerated,
        .product-card-enhanced,
        .footer-card-enhanced,form, opacity;
        .button-enhanced-primary,
        .button-enhanced-secondary,en;
        .feature-icon-enhanced,
        .stats-card-enhanced {
            will-change: transform, opacity;/* 页面切换动画 */
            transform: translateZ(0);ageSlideIn {
            backface-visibility: hidden;
        }acity: 0;
        translateY(30px);
        /* 页面切换动画 */
        @keyframes pageSlideIn {o {
            from {opacity: 1;
                opacity: 0;translateY(0);
                transform: translateY(30px);
            }
            to {
                opacity: 1;.page-slide-in {
                transform: translateY(0);ageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
            }
        }
        /* 滚动触发动画增强 */
        .page-slide-in {-enhanced {
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }translateY(40px) scale(0.95);
        1, 0.32, 1);
        /* 滚动触发动画增强 */
        .scroll-reveal-enhanced {
            opacity: 0;.scroll-reveal-enhanced.visible {
            transform: translateY(40px) scale(0.95);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);translateY(0) scale(1);
        }
        
        .scroll-reveal-enhanced.visible {.scroll-reveal-enhanced.visible.delay-1 {
            opacity: 1;
            transform: translateY(0) scale(1);
        }
        .scroll-reveal-enhanced.visible.delay-2 {
        .scroll-reveal-enhanced.visible.delay-1 {
            transition-delay: 0.1s;
        }
        .scroll-reveal-enhanced.visible.delay-3 {
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;
        }
        /* 悬停态性能优化 */
        .scroll-reveal-enhanced.visible.delay-3 {: hover) {
            transition-delay: 0.3s;ced:hover,
        }
        
        /* 悬停态性能优化 */px) translateZ(0);
        @media (hover: hover) {
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {/* 移动端优化 */
                transform: translateY(-8px) translateZ(0);-width: 768px) {
            }
        }
        
        /* 移动端优化 */mportant;
        @media (max-width: 768px) {
            .product-card-enhanced,
            .footer-card-enhanced,.product-card-enhanced:hover,
            .stats-card-enhanced {
                transform: none !important;
            }nt;
            
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,/* 减少移动端动画 */
            .stats-card-enhanced:hover {-enhanced:hover {
                transform: none !important;);
            }
            
            /* 减少移动端动画 */.button-enhanced-primary:hover,
            .feature-icon-enhanced:hover {r {
                transform: translateY(-4px);
            }
            
            .button-enhanced-primary:hover,
            .button-enhanced-secondary:hover {/* 减少动画首选项支持 */
                transform: translateY(-2px);-reduced-motion: reduce) {
            }
        }:before,
        
        /* 减少动画首选项支持 */ion-duration: 0.01ms !important;
        @media (prefers-reduced-motion: reduce) {t;
            *,
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;.product-card-enhanced,
                transition-duration: 0.01ms !important;
                scroll-behavior: auto !important;
            }d {
            ortant;
            .product-card-enhanced,
            .footer-card-enhanced,
            .stats-card-enhanced,
            .feature-icon-enhanced {/* 高性能滚动条样式 */
                transform: none !important;llbar {
            }
        };
        
        /* 高性能滚动条样式 */
        ::-webkit-scrollbar {::-webkit-scrollbar-track {
            width: 8px; 22, 0.3);
            height: 8px;
        }
        
        ::-webkit-scrollbar-track {::-webkit-scrollbar-thumb {
            background: rgba(10, 0, 22, 0.3);ient(180deg, #6C13FF 0%, #00D4FF 100%);
            border-radius: 4px;
        }gba(255, 255, 255, 0.1);
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);::-webkit-scrollbar-thumb:hover {
            border-radius: 4px;80deg, #8C33FF 0%, #20F4FF 100%);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        /* Firefox滚动条 */
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);rollbar-width: thin;
        }FF rgba(10, 0, 22, 0.3);
        
        /* Firefox滚动条 */
        html {/* 文本选择样式 */
            scrollbar-width: thin;{
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);d: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
        }
        
        /* 文本选择样式 */
        ::selection {::-moz-selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);near-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;
        }
        
        ::-moz-selection {/* 加载状态优化 */
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);te {
            color: white;lative;
        }
        
        /* 加载状态优化 */
        .loading-state {.loading-state::after {
            position: relative;
            overflow: hidden;solute;
        }
        100%;
        .loading-state::after {
            content: '';;
            position: absolute;inear-gradient(90deg, 
            top: 0;
            left: -100%;55, 0.1) 50%, 
            width: 100%;
            height: 100%;mer 1.5s infinite;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 50%, @keyframes loadingShimmer {
                transparent 100%);
            animation: loadingShimmer 1.5s infinite;}
        }
        
        @keyframes loadingShimmer {/* 历史记录面板增强样式 */
            0% { left: -100%; }
            100% { left: 100%; }ter: blur(20px);
        }r(20px);
        , 0.1);
        /* 历史记录面板增强样式 */
        .history-panel {
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);        .history-item {
            border: 1px solid rgba(255, 255, 255, 0.1); all 0.3s ease;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);ent;
        }

        .history-item {
            transition: all 0.3s ease;
            border: 1px solid transparent;        .history-item::before {
            position: relative;
            overflow: hidden;solute;
        }

        .history-item::before {3px;
            content: '';%;
            position: absolute;ransparent;
            left: 0;e;
            top: 0;
            width: 3px;
            height: 100%;        .history-item:hover {
            background: transparent;75, 85, 99, 0.3);
            transition: all 0.3s ease;.3);
        }

        .history-item:hover {
            background: rgba(75, 85, 99, 0.3);        .history-item:hover::before {
            border-color: rgba(168, 85, 247, 0.3);nt(to bottom, #a855f7, #3b82f6);
            transform: translateX(4px);
        }
        .history-item.chat::before {
        .history-item:hover::before {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
        }
        .history-item.login::before {
        .history-item.chat::before {
            background: #10b981;
        }
        .history-item.notification::before {
        .history-item.login::before {
            background: #3b82f6;
        }
        .filter-tag {
        .history-item.notification::before {n: all 0.2s ease;
            background: #f59e0b;
        }d rgba(255, 255, 255, 0.2);

        .filter-tag {
            transition: all 0.2s ease;        .filter-tag:hover {
            cursor: pointer;a(168, 85, 247, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.2););
        }

        .filter-tag:hover {
            background: rgba(168, 85, 247, 0.2);        .filter-tag.active {
            border-color: rgba(168, 85, 247, 0.5);ar-gradient(45deg, #a855f7, #3b82f6);
            transform: scale(1.05);
        }

        .filter-tag.active {
            background: linear-gradient(45deg, #a855f7, #3b82f6);        /* 搜索框样式 */
            border-color: transparent;ut {
            color: white; rgba(17, 24, 39, 0.6);
        }55, 0.2);

        /* 搜索框样式 */
        .search-input {
            background: rgba(17, 24, 39, 0.6);        .search-input:focus {
            border: 1px solid rgba(255, 255, 255, 0.2);17, 24, 39, 0.8);
            transition: all 0.3s ease;.5);
        }7, 0.1);

        .search-input:focus {
            background: rgba(17, 24, 39, 0.8);        /* 响应式设计增强 */
            border-color: rgba(168, 85, 247, 0.5);idth: 768px) {
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
        }w !important;

        /* 空状态样式 */ant;
        .empty-state {t;
            opacity: 0.7;50%);
            transition: opacity 0.3s ease;
        }
            .history-panel {
        .empty-state:hover {!important;
            opacity: 1;;
        }

        /* 导出和清空按钮样式 */
        .action-button {
            transition: all 0.3s ease;            .chat-widget {
            position: relative;w !important;
            overflow: hidden;;
        }

        .action-button::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;        /* 滚动条美化 */
            width: 0;ollbar::-webkit-scrollbar {
            height: 0;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transform: translate(-50%, -50%);        .custom-scrollbar::-webkit-scrollbar-track {
            transition: all 0.4s ease;
        }

        .action-button:hover::after {
            width: 300px;        .custom-scrollbar::-webkit-scrollbar-thumb {
            height: 300px;a855f7, #3b82f6);
        }

        .action-button:hover {
            transform: translateY(-2px);        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);, #2563eb);
        }

        /* 响应式设计增强 */        /* 加载动画 */
        @media (max-width: 768px) { loading-dots {
            .emoji-panel {2; }
                width: 90vw !important;
                max-width: 280px;: 0.2; }
                left: 50% !important;
                right: auto !important;
                transform: translateX(-50%);        .loading-dots span {
            }ng-dots 1.4s infinite;

            .history-panel {
                width: 95vw !important;        .loading-dots span:nth-child(2) {
                height: 80vh !important;
                left: 2.5vw !important;
                right: auto !important;
            }        .loading-dots span:nth-child(3) {

            .chat-widget {
                width: 95vw !important;
                height: 80vh !important;/* ...existing code... */
                left: 2.5vw !important;
                right: auto !important;
                bottom: 2vh !important;lass="min-h-screen font-body scrollbar-hidden">
            }
        }content" class="skip-nav">

        /* 滚动条美化 */
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;    <!-- 页面加载动画 -->
        }ader" class="fixed inset-0 z-[100] flex items-center justify-center bg-[#0a0016] transition-opacity duration-500">

        .custom-scrollbar::-webkit-scrollbar-track {ded-full animate-spin border-t-transparent"></div>
            background: rgba(17, 24, 39, 0.3);
            border-radius: 3px;8 animate-pulse">
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {class="sr-only">正在加载页面内容，请稍候...</span>
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;
        }    <!-- 动态背景容器 -->
oundContainer" class="fixed w-full h-full top-0 left-0 pointer-events-none z-0"></div>
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(to bottom, #9333ea, #2563eb);    <!-- 导航栏 -->
        }fixed top-0 w-full z-50 enhanced-nav-bar">

        /* 加载动画 */ items-center h-16">
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; }lass="flex items-center space-x-2 card-3d hologram nav-logo-container">
            50% { opacity: 1; }
            80%, 100% { opacity: 0.2; }ages/logo.svg" alt="珑凌科技" class="h-8 w-auto">
        }
class="quantum-text text-xl font-heading dragon-border text-glow-enhanced">珑凌科技</span>
        .loading-dots span {
            animation: loading-dots 1.4s infinite;
        }                <!-- 桌面端导航菜单 -->
en md:flex items-center space-x-8 particle-orbit">
        .loading-dots span:nth-child(2) {ext-tech-blue transition-all card-3d" data-i18n="nav.home">首页</a>
            animation-delay: 0.2s;">产品矩阵</a>
        }</a>

        .loading-dots span:nth-child(3) {</a>
            animation-delay: 0.4s;
        }<!-- 语言选择器 -->
        ng-selector-enhanced flex items-center gap-2 text-sm text-gray-300 cursor-pointer card-3d quantum-superposition">
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {s"></i>
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }
        }                    <!-- 控制台按钮 -->
        rated-detection-system.html" class="quantum-button-console px-4 py-2 rounded-full text-white hover:opacity-90 transition-all flex items-center space-x-2 card-3d hologram">
        @keyframes neon-flicker {
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }>
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }
                            <button id="loginBtn" class="quantum-button px-6 py-2 rounded-full text-white transition-all card-3d gangmiao-tech" data-i18n="nav.login">
        @keyframes slide-in-left {
            from { transform: translateX(-100%); opacity: 0; }on>
            to { transform: translateX(0); opacity: 1; }
        }
                        <!-- 移动端菜单按钮 -->
        @keyframes slide-in-right {d:hidden p-2 text-gray-300 hover:text-tech-blue transition-colors interactive-button" id="mobileMenuBtn">
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        @keyframes scale-up {
            from { transform: scale(0.8); opacity: 0; }        <!-- 移动端导航菜单 -->
            to { transform: scale(1); opacity: 1; }idden hidden" id="mobileMenu">
        }
        ed block">首页</a>
        @keyframes fade-in-up {矩阵</a>
            from { transform: translateY(30px); opacity: 0; }a>
            to { transform: translateY(0); opacity: 1; }
        }a>
        
        /* 动画类 */<!-- 移动端语言选择 -->
        .float-animation { animation: float 6s ease-in-out infinite; }-selector-enhanced py-2">
        .wave-animation { animation: wave 4s ease-in-out infinite; }interactive-button">
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }tion>
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }                <!-- 移动端控制台按钮 -->
        ed-detection-system.html" class="quantum-button-console w-full px-6 py-2 rounded-full text-white hover:opacity-90 transition-all flex items-center justify-center space-x-2">
        /* 触摸反馈 */
        .touch-feedback {
            position: relative;
            overflow: hidden;
        }                <button id="mobileLoginBtn" class="quantum-button w-full px-6 py-2 rounded-full bg-longling hover:bg-opacity-80 transition-all">
        
        .touch-feedback::after {on>
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;    <!-- 通知提示组件 -->
            height: 0;cationContainer" class="fixed top-4 right-4 z-50 space-y-4">
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;    <!-- 主要内容区域 -->
        }
        -- Hero部分 -->
        .touch-feedback:active::after {me" class="relative min-h-screen flex items-center dragon-scale compute-grid hero-enhanced">
            width: 300px;
            height: 300px;
        }
        lass="cloud-flow"></div>
        /* 加载状态动画 */div>
        .loading-spinner { pt-24">
            display: inline-block;items-center">
            width: 20px;0">
            height: 20px;t-heading hero-title-enhanced leading-tight">
            border: 2px solid rgba(108, 19, 255, 0.3);-600 bg-clip-text text-transparent animate-pulse">
            border-radius: 50%;
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;lass="block quantum-superposition bg-gradient-to-r from-purple-400 via-pink-500 to-red-500 bg-clip-text text-transparent font-extrabold">
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }class="hero-subtitle-enhanced text-xl md:text-2xl font-code text-gradient bg-gradient-to-r from-emerald-400 to-teal-400 bg-clip-text text-transparent">
        }
        
        /* 进度条动画 */ss="text-lg text-gray-300 max-w-xl leading-relaxed">
        .progress-bar {
            position: relative;界的安全边界，
            overflow: hidden;
        }
         class="flex flex-wrap gap-4 relative z-10">
        .progress-bar::after {
            content: '';
            position: absolute;>
            top: 0;lass="button-enhanced-secondary flex items-center gap-2">
            left: -100%;
            width: 100%;arrow-right"></i>
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;时数据统计 -->
        }d grid-cols-3 gap-6 mt-12 relative z-10">
        
        @keyframes progress-shine {el">
            0% { left: -100%; }
            100% { left: 100%; }
        }lass="text-sm text-gray-400 mt-2">

        /* 3D 变换和悬停效果 */
        .quantum-card {
            transform-style: preserve-3d;lass="stats-card-enhanced text-center">
            perspective: 1000px;e">
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }
lass="text-sm text-gray-400 mt-2">
        .quantum-card:hover {
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);
        }
lass="stats-card-enhanced text-center">
        .quantum-text {ta">
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            -webkit-background-clip: text;
            background-clip: text;lass="text-sm text-gray-400 mt-2">
            color: transparent;
            animation: quantum-flicker 2s infinite;
        }

        .matrix-text {
            font-family: 'Fira Code', monospace;lass="hidden md:block relative">
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);low-hidden card-3d dragon-border">
        }
odel" class="w-full h-[600px] quantum-superposition"></div>
        /* 粒子系统容器 */
        .particle-container {olute bottom-4 left-4 right-4 glass-card p-4 hologram">
            position: absolute;
            width: 100%;
            height: 100%;>Quantum Security Level</div>
            pointer-events: none;
            overflow: hidden;
        }lass="font-code text-sm text-right">
 Status</div>
        /* 高级滚动动画 */
        .scroll-reveal {
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }

        .scroll-reveal.visible {
            opacity: 1;
            transform: translateY(0);
        }        <!-- 产品矩阵 -->
products" class="py-32 px-8 relative overflow-hidden cloud-flow compute-grid">
        /* 全局动画性能优化 */
        .hardware-accelerated,
        .product-card-enhanced,
        .footer-card-enhanced,ading mb-16 text-center quantum-text dragon-border hologram">
        .button-enhanced-primary,
        .button-enhanced-secondary,
        .feature-icon-enhanced,class="grid gap-12">
        .stats-card-enhanced {
            will-change: transform, opacity;oduct-card-enhanced scroll-reveal">
            transform: translateZ(0);enter">
            backface-visibility: hidden;
        }-icon-enhanced">
        t-white text-2xl"></i>
        /* 页面切换动画 */
        @keyframes pageSlideIn {ass="text-2xl font-heading hero-title-enhanced">珑凌安全云</h3>
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {class="space-y-4">
                opacity: 1;-list-item">
                transform: translateY(0);t text-tech-blue hologram"></i>
            }
        }
        lass="feature-list-item">
        .page-slide-in {rows-alt text-tech-blue hologram"></i>
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }
        lass="feature-list-item">
        /* 滚动触发动画增强 */r-alt text-tech-blue hologram"></i>
        .scroll-reveal-enhanced {
            opacity: 0;
            transform: translateY(40px) scale(0.95);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);class="pt-4">
        }"button-enhanced-primary">
        
        .scroll-reveal-enhanced.visible {>
            opacity: 1;
            transform: translateY(0) scale(1);
        }lass="relative">
        
        .scroll-reveal-enhanced.visible.delay-1 {odel" class="w-full h-80 rounded-lg bg-gradient-to-br from-purple-900/20 to-blue-900/20 flex items-center justify-center">
            transition-delay: 0.1s;
        }er text-6xl text-tech-blue mb-4 animate-pulse"></i>
        
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;
        }
        
        .scroll-reveal-enhanced.visible.delay-3 {
            transition-delay: 0.3s;
        }                    <!-- 珑凌智脑 -->
        roduct-card-enhanced scroll-reveal">
        /* 悬停态性能优化 */enter">
        @media (hover: hover) {
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover," class="w-full h-80 rounded-lg bg-gradient-to-br from-pink-900/20 to-purple-900/20 flex items-center justify-center">
            .stats-card-enhanced:hover {
                transform: translateY(-8px) translateZ(0);n text-6xl text-pink-400 mb-4 animate-pulse"></i>
            }
        }
        
        /* 移动端优化 */
        @media (max-width: 768px) {lass="space-y-6">
            .product-card-enhanced,-icon-enhanced">
            .footer-card-enhanced,te text-2xl"></i>
            .stats-card-enhanced {
                transform: none !important;ass="text-2xl font-heading hero-title-enhanced">珑凌智脑</h3>
            }
            
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {class="space-y-4">
                transform: none !important;-list-item">
            }t-pink-400"></i>
            
            /* 减少移动端动画 */
            .feature-icon-enhanced:hover {lass="feature-list-item">
                transform: translateY(-4px);e text-pink-400"></i>
            }
            
            .button-enhanced-primary:hover,lass="feature-list-item">
            .button-enhanced-secondary:hover {-pink-400"></i>
                transform: translateY(-2px);
            }
        }
        class="pt-4">
        /* 减少动画首选项支持 */"button-enhanced-primary">
        @media (prefers-reduced-motion: reduce) {
            *,>
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
                scroll-behavior: auto !important;
            }
            
            .product-card-enhanced,        <!-- 技术实力 -->
            .footer-card-enhanced,technology" class="py-32 px-8 bg-grid relative quantum-tunnel cloud-flow">
            .stats-card-enhanced,
            .feature-icon-enhanced {>
                transform: none !important;
            }ading mb-16 text-center quantum-text dragon-border hologram">
        }
        
        /* 高性能滚动条样式 */class="grid grid-cols-1 md:grid-cols-3 gap-8 particle-orbit">                <!-- 技术创新 -->
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;-white text-2xl"></i>
        }
        ass="text-xl font-heading mb-4 hero-title-enhanced">技术创新</h3>
        ::-webkit-scrollbar-track {
            background: rgba(10, 0, 22, 0.3);
            border-radius: 4px;。
        }
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);                <!-- 安全保障 -->
            border-radius: 4px;roduct-card-enhanced scroll-reveal">
            border: 1px solid rgba(255, 255, 255, 0.1);
        }t-white text-2xl"></i>
        
        ::-webkit-scrollbar-thumb:hover {ass="text-xl font-heading mb-4 hero-title-enhanced">安全保障</h3>
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);
        }
        
        /* Firefox滚动条 */
        html {
            scrollbar-width: thin;
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);                <!-- 服务能力 -->
        }roduct-card-enhanced scroll-reveal">
        
        /* 文本选择样式 */-white text-2xl"></i>
        ::selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);ass="text-xl font-heading mb-4 hero-title-enhanced">服务能力</h3>
            color: white;
        }
        级响应服务。
        ::-moz-selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;
        }
        
        /* 加载状态优化 */
        .loading-state {        <!-- 技术优势 -->
            position: relative;advantages" class="py-20 bg-deep-purple/20">
            overflow: hidden;
        }xt-center mb-12">技术优势</h2>
         gap-8">
        .loading-state::after {
            content: '';deep-purple/30 rounded-lg p-6 text-center hover:bg-deep-purple/40 transition-colors">
            position: absolute;ms-center justify-center mb-6">
            top: 0;
            left: -100%;
            width: 100%;ass="text-xl font-bold mb-4">深度学习引擎</h3>
            height: 100%;</p>
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 50%,                     <!-- 自然语言处理 -->
                transparent 100%);deep-purple/30 rounded-lg p-6 text-center hover:bg-deep-purple/40 transition-colors">
            animation: loadingShimmer 1.5s infinite;ms-center justify-center mb-6">
        }
        
        @keyframes loadingShimmer {ass="text-xl font-bold mb-4">自然语言处理</h3>
            0% { left: -100%; }p>
            100% { left: 100%; }
        }
                            <!-- 知识图谱 -->
        /* 历史记录面板增强样式 */g-deep-purple/30 rounded-lg p-6 text-center hover:bg-deep-purple/40 transition-colors">
        .history-panel {ms-center justify-center mb-6">
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);ass="text-xl font-bold mb-4">知识图谱</h3>
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);用</p>
        }

        .history-item {                    <!-- 云原生架构 -->
            transition: all 0.3s ease;-deep-purple/30 rounded-lg p-6 text-center hover:bg-deep-purple/40 transition-colors">
            border: 1px solid transparent;ms-center justify-center mb-6">
            position: relative;
            overflow: hidden;
        }ass="text-xl font-bold mb-4">云原生架构</h3>
p>
        .history-item::before {
            content: '';
            position: absolute;
            left: 0;                <!-- 技术指标 -->
            top: 0;t-16 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            width: 3px;
            height: 100%;lue mb-2">99.9%</div>
            background: transparent;
            transition: all 0.3s ease;
        }lass="bg-deep-purple/30 rounded-lg p-6">
lue mb-2">&lt;50ms</div>
        .history-item:hover {
            background: rgba(75, 85, 99, 0.3);
            border-color: rgba(168, 85, 247, 0.3);lass="bg-deep-purple/30 rounded-lg p-6">
            transform: translateX(4px);lue mb-2">10000+</div>
        }

        .history-item:hover::before {lass="bg-deep-purple/30 rounded-lg p-6">
            background: linear-gradient(to bottom, #a855f7, #3b82f6);lue mb-2">95%</div>
        }

        .history-item.chat::before {
            background: #10b981;
        }

        .history-item.login::before {        <!-- 我们的服务 -->
            background: #3b82f6;ervices" class="py-20 bg-deep-purple/20">
        }
xt-center mb-12">我们的服务</h2>
        .history-item.notification::before {gap-8">
            background: #f59e0b;
        }deep-purple/30 rounded-lg p-6 hover:transform hover:scale-105 transition-all duration-300">
y-center mb-4">
        .filter-tag {
            transition: all 0.2s ease;
            cursor: pointer;ass="text-xl font-bold mb-4">AI对话系统</h3>
            border: 1px solid rgba(255, 255, 255, 0.2);自然、专业的对话服务，支持多场景应用</p>
        }
tech-blue mr-2"></i>多模型支持</li>
        .filter-tag:hover {
            background: rgba(168, 85, 247, 0.2);>
            border-color: rgba(168, 85, 247, 0.5);
            transform: scale(1.05);
        }
                    <!-- 智能分析系统 -->
        .filter-tag.active {deep-purple/30 rounded-lg p-6 hover:transform hover:scale-105 transition-all duration-300">
            background: linear-gradient(45deg, #a855f7, #3b82f6);y-center mb-4">
            border-color: transparent;
            color: white;
        }ass="text-xl font-bold mb-4">智能分析系统</h3>
供深度数据分析和决策支持</p>
        /* 搜索框样式 */
        .search-input {tech-blue mr-2"></i>数据挖掘</li>
            background: rgba(17, 24, 39, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.2);>
            transition: all 0.3s ease;
        }

        .search-input:focus {                    <!-- 自动化解决方案 -->
            background: rgba(17, 24, 39, 0.8);eep-purple/30 rounded-lg p-6 hover:transform hover:scale-105 transition-all duration-300">
            border-color: rgba(168, 85, 247, 0.5);y-center mb-4">
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
        }
ass="text-xl font-bold mb-4">自动化解决方案</h3>
        /* 空状态样式 */，提升运营效率</p>
        .empty-state {
            opacity: 0.7;tech-blue mr-2"></i>流程自动化</li>
            transition: opacity 0.3s ease;
        }

        .empty-state:hover {
            opacity: 1;
        }

        /* 导出和清空按钮样式 */
        .action-button {        <!-- 品牌故事 -->
            transition: all 0.3s ease;about" class="py-20 dragon-scale quantum-entanglement cloud-flow">
            position: relative;
            overflow: hidden;grid-cols-2 gap-12 items-center">
        }
pace-y-6 card-3d dragon-spiral">
        .action-button::after {m-text dragon-border hologram">关于珑凌科技</h2>
            content: '';量子安全和智能化解决方案，通过先进的人工智能和量子技术，帮助企业实现数字化转型和安全防护。我们的团队由行业资深专家组成，拥有丰富的量子科技项目经验和技术积累。</p>
            position: absolute;
            top: 50%;<div class="grid grid-cols-2 gap-6 my-8 particle-orbit">
            left: 50%;n">
            width: 0;hologram">100+</div>
            height: 0;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;lass="text-center card-3d quantum-superposition">
            transform: translate(-50%, -50%);hologram">50+</div>
            transition: all 0.4s ease;
        }
lass="text-center card-3d quantum-superposition">
        .action-button:hover::after {hologram">98%</div>
            width: 300px;
            height: 300px;
        }lass="text-center card-3d quantum-superposition">
hologram">24/7</div>
        .action-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }
                <div class="flex gap-4">
        /* 响应式设计增强 */py-3 bg-gradient-to-r from-longling to-tech-blue text-white rounded-lg hover:opacity-90 transition-opacity card-3d">
        @media (max-width: 768px) {
            .emoji-panel {>
                width: 90vw !important;lass="px-6 py-3 border border-tech-blue text-tech-blue rounded-lg hover:bg-tech-blue/10 transition-colors card-3d dragon-border">
                max-width: 280px;
                left: 50% !important;>
                right: auto !important;
                transform: translateX(-50%);
            }
            <!-- 右侧发展历程 -->
            .history-panel {ce-y-8 quantum-tunnel">
                width: 95vw !important;-2 border-tech-blue card-3d">
                height: 80vh !important; rounded-full hologram"></div>
                left: 2.5vw !important;
                right: auto !important;家</p>
            }
lass="relative pl-8 border-l-2 border-tech-blue card-3d">
            .chat-widget { rounded-full hologram"></div>
                width: 95vw !important;
                height: 80vh !important;p>
                left: 2.5vw !important;
                right: auto !important;lass="relative pl-8 border-l-2 border-tech-blue card-3d">
                bottom: 2vh !important; rounded-full hologram"></div>
            }
        }

        /* 滚动条美化 */
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {        <!-- 联系我们 -->
            background: rgba(17, 24, 39, 0.3);contact" class="py-32 px-8 bg-grid compute-grid quantum-tunnel">
            border-radius: 3px;
        }
>
        .custom-scrollbar::-webkit-scrollbar-thumb {ading mb-16 text-center quantum-text dragon-border hologram">
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;
        }class="grid md:grid-cols-2 gap-12">
-scale">
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {-superposition">与我们开启合作</h3>
            background: linear-gradient(to bottom, #9333ea, #2563eb);
        }

        /* 加载动画 */。
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; } class="space-y-4 particle-orbit">
            50% { opacity: 1; }4 card-3d">
            80%, 100% { opacity: 0.2; }hologram"></i>
        }

        .loading-dots span {lass="flex items-center gap-4 card-3d">
            animation: loading-dots 1.4s infinite; hologram"></i>
        }

        .loading-dots span:nth-child(2) {lass="flex items-center gap-4 card-3d">
            animation-delay: 0.2s;-blue hologram"></i>
        }

        .loading-dots span:nth-child(3) {
            animation-delay: 0.4s;
        }lass="glass-card p-8 card-3d quantum-entanglement dragon-border">
        
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {lass="block text-sm quantum-superposition">姓名</label>
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }der border-longling/20 rounded-lg focus:border-tech-blue focus:outline-none transition-colors card-3d hologram">
        }
        lass="space-y-2">
        @keyframes neon-flicker {class="block text-sm quantum-superposition">邮箱</label>
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; } border-longling/20 rounded-lg focus:border-tech-blue focus:outline-none transition-colors card-3d hologram">
        }
        lass="space-y-2">
        @keyframes slide-in-left {" class="block text-sm quantum-superposition">公司</label>
            from { transform: translateX(-100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }border border-longling/20 rounded-lg focus:border-tech-blue focus:outline-none transition-colors card-3d hologram">
        }
        lass="space-y-2">
        @keyframes slide-in-right {" class="block text-sm quantum-superposition">留言</label>
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }rder-longling/20 rounded-lg focus:border-tech-blue focus:outline-none transition-colors card-3d hologram"></textarea>
        }
        n type="submit" 
        @keyframes scale-up { py-3 bg-gradient-to-r from-longling to-tech-blue text-white rounded-lg hover:opacity-90 transition-opacity card-3d gangmiao-tech">
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }>
        }
        
        @keyframes fade-in-up {
            from { transform: translateY(30px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
                <!-- 港妙科技团队展示 -->
        /* 动画类 */y-16 px-8 bg-[#0a0016]/90 dragon-scale compute-grid quantum-tunnel">
        .float-animation { animation: float 6s ease-in-out infinite; }
        .wave-animation { animation: wave 4s ease-in-out infinite; }
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }>
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }ading mb-16 text-center quantum-text dragon-border hologram">
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }<!-- 团队介绍 -->
        rid md:grid-cols-2 gap-12 mb-20 particle-orbit">
        /* 触摸反馈 */entanglement">
        .touch-feedback {ition">专业团队 · 至新思想</h3>
            position: relative;
            overflow: hidden;
        }
        
        .touch-feedback::after { class="flex flex-wrap gap-4">
            content: '';ent-to-br from-purple-600/20 to-blue-600/20 rounded-lg flex items-center justify-center card-3d hologram">
            position: absolute;
            top: 50%;
            left: 50%;lass="w-32 h-32 bg-gradient-to-br from-blue-600/20 to-indigo-600/20 rounded-lg flex items-center justify-center card-3d hologram">
            width: 0;
            height: 0;
            border-radius: 50%;lass="w-32 h-32 bg-gradient-to-br from-indigo-600/20 to-purple-600/20 rounded-lg flex items-center justify-center card-3d hologram">
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }
        lass="glass-card p-8 card-3d dragon-spiral quantum-entanglement">
        .touch-feedback:active::after {ition">团队介绍 · 主要擅长</h3>
            width: 300px;
            height: 300px;
        }
        
        /* 加载状态动画 */ class="flex flex-wrap gap-4">
        .loading-spinner {ent-to-br from-green-600/20 to-teal-600/20 rounded-lg flex items-center justify-center card-3d hologram">
            display: inline-block;
            width: 20px;
            height: 20px;lass="w-32 h-32 bg-gradient-to-br from-teal-600/20 to-cyan-600/20 rounded-lg flex items-center justify-center card-3d hologram">
            border: 2px solid rgba(108, 19, 255, 0.3);
            border-radius: 50%;
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;
        }
        
        @keyframes spin {                <!-- 团队成员 -->
            to { transform: rotate(360deg); }rid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 quantum-entanglement">
        }
        ep-purple/30 rounded-lg p-6 hover:bg-deep-purple/40 transition-colors card-3d dragon-border hologram">
        /* 进度条动画 */
        .progress-bar {
            position: relative;
            overflow: hidden;拥有10年人工智能开发经验，专注于深度学习和自然语言处理领域</p>
        }
        
        .progress-bar::after {                    <!-- 团队成员卡片 2 -->
            content: '';ep-purple/30 rounded-lg p-6 hover:bg-deep-purple/40 transition-colors card-3d dragon-border hologram">
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;>曾参与多个大型AI项目的设计与实施，擅长产品战略规划</p>
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;                    <!-- 团队成员卡片 3 -->
        }ep-purple/30 rounded-lg p-6 hover:bg-deep-purple/40 transition-colors">
        auto mb-4 object-cover">
        @keyframes progress-shine {
            0% { left: -100%; }
            100% { left: 100%; }开发和技术创新</p>
        }

        /* 3D 变换和悬停效果 */
        .quantum-card {
            transform-style: preserve-3d;
            perspective: 1000px;        <!-- 合作伙伴 -->
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);s="py-20 bg-deep-purple/20">
        }
 mb-16 text-center quantum-text">合作伙伴</h2>
        .quantum-card:hover {
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);<!-- 合作伙伴类别切换 -->
        }justify-center gap-4 mb-12">
0 rounded-full text-tech-blue hover:bg-deep-purple/40 transition-colors active">
        .quantum-text {
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);>
            -webkit-background-clip: text;lass="px-6 py-2 bg-deep-purple/30 rounded-full text-gray-400 hover:bg-deep-purple/40 transition-colors">
            background-clip: text;
            color: transparent;>
            animation: quantum-flicker 2s infinite;lass="px-6 py-2 bg-deep-purple/30 rounded-full text-gray-400 hover:bg-deep-purple/40 transition-colors">
        }
>
        .matrix-text {
            font-family: 'Fira Code', monospace;
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);        <!-- 合作伙伴logo墙 -->
        }rid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">

        /* 粒子系统容器 */eep-purple/30 rounded-lg p-8 hover:bg-deep-purple/40 transition-all duration-300">
        .particle-container {scale hover:grayscale-0 transition-all duration-300">
            position: absolute;
            width: 100%;lass="bg-deep-purple/30 rounded-lg p-8 hover:bg-deep-purple/40 transition-all duration-300">
            height: 100%;scale hover:grayscale-0 transition-all duration-300">
            pointer-events: none;
            overflow: hidden;lass="bg-deep-purple/30 rounded-lg p-8 hover:bg-deep-purple/40 transition-all duration-300">
        }scale hover:grayscale-0 transition-all duration-300">

        /* 高级滚动动画 */lass="bg-deep-purple/30 rounded-lg p-8 hover:bg-deep-purple/40 transition-all duration-300">
        .scroll-reveal {scale hover:grayscale-0 transition-all duration-300">
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }        <!-- 合作数据展示 -->
16 grid grid-cols-1 md:grid-cols-3 gap-8">
        .scroll-reveal.visible {ter">
            opacity: 1;+</div>
            transform: translateY(0);
        }
lass="bg-deep-purple/30 rounded-lg p-6 text-center">
        /* 全局动画性能优化 */+</div>
        .hardware-accelerated,
        .product-card-enhanced,
        .footer-card-enhanced,lass="bg-deep-purple/30 rounded-lg p-6 text-center">
        .button-enhanced-primary,0+</div>
        .button-enhanced-secondary,
        .feature-icon-enhanced,
        .stats-card-enhanced {
            will-change: transform, opacity;
            transform: translateZ(0);
            backface-visibility: hidden;
        }<!-- 成功案例 -->
        s="py-20">
        /* 页面切换动画 */er mx-auto px-4">
        @keyframes pageSlideIn { mb-16 text-center quantum-text">成功案例</h2>
            from {
                opacity: 0;<!-- 案例展示 -->
                transform: translateY(30px);rid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            }
            to {glass-card overflow-hidden group">
                opacity: 1;
                transform: translateY(0);mages/case1.jpg" alt="金融行业案例" class="w-full h-48 object-cover">
            }city-60"></div>
        }
        lass="p-6">
        .page-slide-in {xt-xl font-bold mb-2">金融风控系统升级</h3>
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);升90%的异常交易识别率</p>
        }
        
        /* 滚动触发动画增强 */ech-blue text-tech-blue rounded hover:bg-tech-blue/10 transition-colors">
        .scroll-reveal-enhanced {
            opacity: 0;>
            transform: translateY(40px) scale(0.95);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }
        
        .scroll-reveal-enhanced.visible {            <!-- 案例2 -->
            opacity: 1;glass-card overflow-hidden group">
            transform: translateY(0) scale(1);
        }mages/case2.jpg" alt="医疗行业案例" class="w-full h-48 object-cover">
        city-60"></div>
        .scroll-reveal-enhanced.visible.delay-1 {
            transition-delay: 0.1s;lass="p-6">
        }xt-xl font-bold mb-2">医疗影像智能诊断</h3>
        准确率提升40%</p>
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;
        }ech-blue text-tech-blue rounded hover:bg-tech-blue/10 transition-colors">
        
        .scroll-reveal-enhanced.visible.delay-3 {>
            transition-delay: 0.3s;
        }
        
        /* 悬停态性能优化 */
        @media (hover: hover) {            <!-- 案例3 -->
            .product-card-enhanced:hover,glass-card overflow-hidden group">
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {mages/case3.jpg" alt="制造业案例" class="w-full h-48 object-cover">
                transform: translateY(-8px) translateZ(0);acity-60"></div>
            }
        }lass="p-6">
        xt-xl font-bold mb-2">智能制造解决方案</h3>
        /* 移动端优化 */控系统，设备故障预警准确率达95%</p>
        @media (max-width: 768px) {
            .product-card-enhanced,
            .footer-card-enhanced,tech-blue text-tech-blue rounded hover:bg-tech-blue/10 transition-colors">
            .stats-card-enhanced {
                transform: none !important;>
            }
            
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {
                transform: none !important;
            }
            <!-- 合作伙伴 -->
            /* 减少移动端动画 */s="py-20 bg-gray-900/50">
            .feature-icon-enhanced:hover {">
                transform: translateY(-4px); mb-12 text-center quantum-text">合作伙伴</h2>
            }ter">
            
            .button-enhanced-primary:hover,
            .button-enhanced-secondary:hover {
                transform: translateY(-2px);lass="partner-logo flex items-center justify-center p-4 glass-card">
            }
        }
        lass="partner-logo flex items-center justify-center p-4 glass-card">
        /* 减少动画首选项支持 */
        @media (prefers-reduced-motion: reduce) {
            *,lass="partner-logo flex items-center justify-center p-4 glass-card">
            *::before,
            *::after {
                animation-duration: 0.01ms !important;lass="partner-logo flex items-center justify-center p-4 glass-card">
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
                scroll-behavior: auto !important;lass="partner-logo flex items-center justify-center p-4 glass-card">
            }
            
            .product-card-enhanced,
            .footer-card-enhanced,
            .stats-card-enhanced,
            .feature-icon-enhanced {
                transform: none !important;<!-- 页脚 -->
            }ss="py-16 px-8 border-t border-gray-800">
        }
        1 md:grid-cols-4 gap-8">
        /* 高性能滚动条样式 */
        ::-webkit-scrollbar {pace-y-4">
            width: 8px;font-heading text-white">珑凌科技</h3>
            height: 8px;
        }、更智能的数字世界。
        
        ::-webkit-scrollbar-track { class="flex space-x-4">
            background: rgba(10, 0, 22, 0.3);gray-400 hover:text-tech-blue transition-colors">
            border-radius: 4px;
        }
        ref="#" class="text-gray-400 hover:text-tech-blue transition-colors">
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);
            border-radius: 4px;ref="#" class="text-gray-400 hover:text-tech-blue transition-colors">
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        ref="#" class="text-gray-400 hover:text-tech-blue transition-colors">
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);
        }
        
        /* Firefox滚动条 */
        html {            <!-- 产品服务 -->
            scrollbar-width: thin;pace-y-4">
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);font-heading text-white">产品服务</h3>
        }
        over:text-tech-blue transition-colors">珑凌安全云</a></li>
        /* 文本选择样式 */
        ::selection {i>
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;colors">集成检测系统</a></li>
        }
        
        ::-moz-selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);            <!-- 解决方案 -->
            color: white;pace-y-4">
        }font-heading text-white">解决方案</h3>
        
        /* 加载状态优化 */t-tech-blue transition-colors">金融安全</a></li>
        .loading-state {>
            position: relative;
            overflow: hidden;
        }
        
        .loading-state::after {
            content: '';
            position: absolute;            <!-- 联系我们 -->
            top: 0;pace-y-4">
            left: -100%;font-heading text-white">联系我们</h3>
            width: 100%;
            height: 100%;ce-x-2">
            background: linear-gradient(90deg, tech-blue"></i>
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 50%, 
                transparent 100%);lass="flex items-center space-x-2">
            animation: loadingShimmer 1.5s infinite;"></i>
        }
        
        @keyframes loadingShimmer {lass="flex items-center space-x-2">
            0% { left: -100%; }lue"></i>
            100% { left: 100%; }
        }
        
        /* 历史记录面板增强样式 */lass="mt-4">
        .history-panel {-gray-400 text-sm mb-2">扫码关注公众号</p>
            backdrop-filter: blur(20px); items-center justify-center">
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }

        .history-item {
            transition: all 0.3s ease;        <!-- 版权信息 -->
            border: 1px solid transparent;order-t border-gray-800 mt-12 pt-8">
            position: relative;-between items-center space-y-4 md:space-y-0">
            overflow: hidden;
        }
blue transition-colors">隐私政策</a>
        .history-item::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            width: 3px;
            height: 100%;
            background: transparent;
            transition: all 0.3s ease;    <!-- 移动端底部导航 -->
        }idden fixed bottom-0 left-0 right-0 bg-deep-purple/90 backdrop-blur-lg border-t border-gray-800 z-40">

        .history-item:hover {er p-2 text-gray-400 hover:text-tech-blue transition-colors">
            background: rgba(75, 85, 99, 0.3);
            border-color: rgba(168, 85, 247, 0.3);>
            transform: translateX(4px);
        }ref="#products" class="flex flex-col items-center p-2 text-gray-400 hover:text-tech-blue transition-colors">

        .history-item:hover::before {>
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
        }ref="#services" class="flex flex-col items-center p-2 text-gray-400 hover:text-tech-blue transition-colors">

        .history-item.chat::before {n>
            background: #10b981;
        }ref="#contact" class="flex flex-col items-center p-2 text-gray-400 hover:text-tech-blue transition-colors">

        .history-item.login::before {
            background: #3b82f6;
        }

        .history-item.notification::before {
            background: #f59e0b;    <!-- 快捷返回顶部按钮 -->
        }oTop" class="fixed bottom-24 right-8 z-40 w-12 h-12 bg-gradient-to-r from-longling to-tech-blue rounded-full hidden md:flex items-center justify-center text-white hover:opacity-90 transition-all" aria-label="返回顶部">

        .filter-tag {
            transition: all 0.2s ease;
            cursor: pointer;    <!-- 页面加载进度条 -->
            border: 1px solid rgba(255, 255, 255, 0.2);gress" class="fixed top-0 left-0 right-0 h-1 bg-gradient-to-r from-longling to-tech-blue transform scale-x-0 origin-left transition-transform z-50"></div>
        }
    <!-- AI助手悬浮按钮增强版 -->
        .filter-tag:hover {ttom-8 right-8 z-50 flex flex-col items-end space-y-4">
            background: rgba(168, 85, 247, 0.2);
            border-color: rgba(168, 85, 247, 0.5);p-blur-lg rounded-lg p-4 space-y-2 mb-2">
            transform: scale(1.05);le/50 rounded transition-colors flex items-center gap-2">
        }

        .filter-tag.active {
            background: linear-gradient(45deg, #a855f7, #3b82f6);lass="w-full px-4 py-2 text-left text-gray-300 hover:bg-deep-purple/50 rounded transition-colors flex items-center gap-2">
            border-color: transparent;
            color: white;
        }
lass="w-full px-4 py-2 text-left text-gray-300 hover:bg-deep-purple/50 rounded transition-colors flex items-center gap-2">
        /* 搜索框样式 */
        .search-input {
            background: rgba(17, 24, 39, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }n class="ai-assistant rounded-full animate-glow touch-feedback" id="xiaoDun" aria-label="打开AI助手菜单">
2"></i>
        .search-input:focus {
            background: rgba(17, 24, 39, 0.8);
            border-color: rgba(168, 85, 247, 0.5);
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);    <!-- 辅助功能工具栏 -->
        }bilityToolbar" class="fixed top-0 left-0 right-0 bg-black/90 backdrop-blur-lg border-b border-gray-700 z-[99] transform -translate-y-full transition-transform duration-300" role="toolbar" aria-label="辅助功能工具栏">

        /* 空状态样式 */-between">
        .empty-state {
            opacity: 0.7;-white">辅助功能:</span>
            transition: opacity 0.3s ease;
        }<!-- 字体大小控制 -->
x items-center space-x-2">
        .empty-state:hover {字体:</label>
            opacity: 1;g-gray-700 hover:bg-gray-600 rounded text-xs" aria-label="减小字体">A-</button>
        }
ton>
        /* 导出和清空按钮样式 */
        .action-button {
            transition: all 0.3s ease;<!-- 对比度控制 -->
            position: relative;ex items-center space-x-2">
            overflow: hidden;对比度:</label>
        }bg-gray-700 hover:bg-gray-600 rounded text-xs" aria-label="切换高对比度">高对比度</button>

        .action-button::after {
            content: '';<!-- 动画控制 -->
            position: absolute;lex items-center space-x-2">
            top: 50%;动画:</label>
            left: 50%;g-gray-700 hover:bg-gray-600 rounded text-xs" aria-label="开关动画">禁用动画</button>
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.1);<!-- 朗读功能 -->
            border-radius: 50%;lex items-center space-x-2">
            transform: translate(-50%, -50%);="px-3 py-1 bg-purple-600 hover:bg-purple-700 rounded text-xs flex items-center space-x-1" aria-label="开启朗读">
            transition: all 0.4s ease;
        }

        .action-button:hover::after {
            width: 300px;
            height: 300px;
        }<button id="closeAccessibilityToolbar" class="text-gray-400 hover:text-white p-1" aria-label="关闭工具栏">

        .action-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }

        /* 响应式设计增强 */    <!-- 登录模态框 -->
        @media (max-width: 768px) {
            .emoji-panel {Modal" class="fixed inset-0 z-50 hidden items-center justify-center">
                width: 90vw !important;
                max-width: 280px;sition-all duration-300 scale-95 opacity-0">
                left: 50% !important;
                right: auto !important;
                transform: translateX(-50%);<!-- 登录模式选择 -->
            }x mb-6 bg-gray-800/50 rounded-lg p-1">
xt-sm rounded-md transition-all duration-200 bg-gradient-to-r from-longling to-tech-blue text-white" data-i18n="login.normalLogin">
            .history-panel {
                width: 95vw !important;>
                height: 80vh !important;d="guestLoginTab" class="flex-1 py-2 text-sm rounded-md transition-all duration-200 text-gray-400 hover:text-white" data-i18n="login.guestMode">
                left: 2.5vw !important;
                right: auto !important;>
            }

            .chat-widget {            <!-- 账号登录表单 -->
                width: 95vw !important;Form" class="space-y-4">
                height: 80vh !important;
                left: 2.5vw !important;e" class="block text-sm text-gray-300">邮箱</label>
                right: auto !important;
                bottom: 2vh !important;rder-longling/20 rounded-lg focus:border-tech-blue focus:outline-none transition-colors text-white"
            }
        }
lass="space-y-2">
        /* 滚动条美化 */assword" class="block text-sm text-gray-300">密码</label>
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;ord" id="login-password" name="password" required
        }g/20 rounded-lg focus:border-tech-blue focus:outline-none transition-colors text-white pr-12"

        .custom-scrollbar::-webkit-scrollbar-track {ranslate-y-1/2 text-gray-400 hover:text-gray-300 transition-colors"
            background: rgba(17, 24, 39, 0.3);
            border-radius: 3px;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;<!-- 错误提示区域 -->
        }rror" class="error-message text-red-400 text-sm hidden p-3 bg-red-900/20 border border-red-500/20 rounded-lg" role="alert">

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(to bottom, #9333ea, #2563eb);
        }
                <!-- 登录选项 -->
        /* 加载动画 */lex items-center justify-between text-sm">
        @keyframes loading-dots {r">
            0%, 20% { opacity: 0.2; }h-4 rounded border-longling/20 bg-deep-purple/50 text-tech-blue focus:ring-tech-blue">
            50% { opacity: 1; }
            80%, 100% { opacity: 0.2; }
        }"#" id="forgotPasswordLink" class="text-tech-blue hover:text-opacity-80 transition-colors">忘记密码？</a>

        .loading-dots span {
            animation: loading-dots 1.4s infinite;                <!-- 登录按钮 -->
        }"submit" id="loginButton"
t-to-r from-longling to-tech-blue text-white rounded-lg hover:opacity-90 transition-opacity disabled:opacity-50 disabled:cursor-not-allowed">
        .loading-dots span:nth-child(2) {
            animation-delay: 0.2s;n ml-2"></i>
        }

        .loading-dots span:nth-child(3) {                <!-- 访客模式按钮 -->
            animation-delay: 0.4s;utton" id="guestModeButton"
        }tech-blue/50 text-tech-blue rounded-lg hover:bg-tech-blue/10 transition-colors">
        
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }                <!-- 注册提示 -->
        }ext-center text-sm pt-4 border-t border-gray-700">
        
        @keyframes neon-flicker {-tech-blue hover:text-opacity-80 transition-colors ml-1">立即注册</a>
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }
                    <!-- 关闭按钮 -->
        @keyframes slide-in-left {loseLoginModal" class="absolute top-4 right-4 quantum-text hover:opacity-80 transition-opacity p-2 rounded-full hover:bg-white/10" title="关闭">
            from { transform: translateX(-100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        @keyframes slide-in-right {
            from { transform: translateX(100%); opacity: 0; }    <!-- 智能客服聊天窗口 -->
            to { transform: translateX(0); opacity: 1; }et" class="fixed bottom-24 right-8 z-40 hidden transform transition-all duration-300 scale-95 opacity-0">
        }
        
        @keyframes scale-up {lex items-center justify-between p-4 border-b border-gray-700">
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }8 rounded-full bg-gradient-to-r from-longling to-tech-blue flex items-center justify-center">
        
        @keyframes fade-in-up {
            from { transform: translateY(30px); opacity: 0; }lass="absolute -bottom-1 -right-1 w-3 h-3 bg-green-400 rounded-full border-2 border-white"></div>
            to { transform: translateY(0); opacity: 1; }
        }
        h3 class="font-heading text-sm">AI智能助手</h3>
        /* 动画类 */enter">
        .float-animation { animation: float 6s ease-in-out infinite; }mr-1"></span>
        .wave-animation { animation: wave 4s ease-in-out infinite; }
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }lass="flex items-center space-x-2">
        .scale-up { animation: scale-up 0.6s ease-out; }-gray-400 hover:text-white transition-colors p-1 rounded" title="设置">
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }
        
        /* 触摸反馈 */d="chatHistory" class="text-gray-400 hover:text-white transition-colors p-1 rounded" title="历史记录">
        .touch-feedback {
            position: relative;
            overflow: hidden;d="closeChatWidget" class="text-gray-400 hover:text-white transition-colors p-1 rounded" title="关闭">
        }
        
        .touch-feedback::after {
            content: '';
            position: absolute;
            top: 50%;<!-- 聊天消息区域 -->
            left: 50%;ssages" class="flex-1 p-4 overflow-y-auto scrollbar-hidden space-y-3">
            width: 0;
            height: 0;gradient-to-r from-longling to-tech-blue flex items-center justify-center flex-shrink-0">
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);lass="bg-gray-800/50 rounded-lg p-3 text-sm max-w-xs">
            transition: width 0.6s, height 0.6s;
        }0 mt-1">
        ).toLocaleTimeString()}</span>
        .touch-feedback:active::after {
            width: 300px;
            height: 300px;
        }
        
        /* 加载状态动画 */<!-- 快捷操作按钮 -->
        .loading-spinner {ctions" class="px-4 pb-2">
            display: inline-block;
            width: 20px;n text-xs px-2 py-1 bg-gray-700/50 rounded text-gray-300 hover:bg-gray-600/50 transition-colors">
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);>
            border-radius: 50%;lass="quick-action-btn text-xs px-2 py-1 bg-gray-700/50 rounded text-gray-300 hover:bg-gray-600/50 transition-colors">
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;>
        }lass="quick-action-btn text-xs px-2 py-1 bg-gray-700/50 rounded text-gray-300 hover:bg-gray-600/50 transition-colors">
        
        @keyframes spin {>
            to { transform: rotate(360deg); }
        }
        
        /* 进度条动画 */<!-- 输入区域 -->
        .progress-bar {-4 border-t border-gray-700">
            position: relative;
            overflow: hidden;ive">
        }
        "chatInput" 
        .progress-bar::after {
            content: '';"输入您的问题..." 
            position: absolute;0/50 border border-gray-600 rounded-lg px-3 py-2 text-sm focus:outline-none focus:border-tech-blue pr-20"
            top: 0;
            left: -100%;
            width: 100%;div class="absolute right-2 top-1/2 -translate-y-1/2 flex space-x-1">
            height: 100%;ransition-colors p-1" title="语音输入">
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;
        }d="emojiBtn" class="text-gray-400 hover:text-white transition-colors p-1" title="表情">
        
        @keyframes progress-shine {
            0% { left: -100%; }
            100% { left: 100%; }
        }n 
sendChatMessage" 
        /* 3D 变换和悬停效果 */-r from-longling to-tech-blue px-4 py-2 rounded-lg hover:opacity-90 transition-opacity flex items-center justify-center disabled:opacity-50"
        .quantum-card {
            transform-style: preserve-3d;
            perspective: 1000px;
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }solute;

        .quantum-card:hover {100%;
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);
        };
inear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
        .quantum-text {
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            -webkit-background-clip: text;
            background-clip: text;@keyframes progress-shine {
            color: transparent;
            animation: quantum-flicker 2s infinite;}
        }
               placeholder="输入您的问题..." 
        .matrix-text {0/50 border border-gray-600 rounded-lg px-3 py-2 text-sm focus:outline-none focus:border-tech-blue"
            font-family: 'Fira Code', monospace;
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);
        }button 
sendChatMessage" 
        /* 粒子系统容器 */-r from-longling to-tech-blue px-4 py-2 rounded-lg hover:opacity-90 transition-opacity flex items-center justify-center"
        .particle-container {
            position: absolute;
            width: 100%;   <i class="fas fa-paper-plane text-sm"></i>
            height: 100%;
            pointer-events: none;
            overflow: hidden;
        }

        /* 高级滚动动画 */
        .scroll-reveal {    <!-- AI助手浮动按钮 -->
            opacity: 0;istantBtn" class="fixed bottom-24 right-24 z-40 w-14 h-14 bg-gradient-to-r from-longling to-tech-blue rounded-full flex items-center justify-center text-white shadow-lg hover:scale-110 transition-all duration-300 animate-pulse">
            transform: translateY(20px);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);4 bg-red-500 rounded-full flex items-center justify-center">
        }

        .scroll-reveal.visible {
            opacity: 1;
            transform: translateY(0);    <!-- 历史记录面板 -->
        }yPanel" class="fixed top-0 left-0 w-80 h-full bg-white shadow-2xl transform -translate-x-full transition-transform duration-300 z-50">

        /* 全局动画性能优化 */-4 border-b bg-gradient-to-r from-longling to-tech-blue text-white">
        .hardware-accelerated,
        .product-card-enhanced,
        .footer-card-enhanced,
        .button-enhanced-primary,
        .button-enhanced-secondary,
        .feature-icon-enhanced,class="flex items-center space-x-2">
        .stats-card-enhanced {t-white/80 hover:text-white transition-colors p-1 rounded" title="导出历史">
            will-change: transform, opacity;
            transform: translateZ(0);
            backface-visibility: hidden;d="clearHistory" class="text-white/80 hover:text-white transition-colors p-1 rounded" title="清空历史">
        }
        
        /* 页面切换动画 */d="closeHistoryPanel" class="text-white/80 hover:text-white transition-colors p-1 rounded" title="关闭面板">
        @keyframes pageSlideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {<!-- 搜索框 -->
                opacity: 1;relative">
                transform: translateY(0); id="historySearch" placeholder="搜索历史记录..." 
            }-white/30 rounded-lg focus:ring-2 focus:ring-white/50 focus:border-transparent text-white placeholder-white/70">
        }
        /70 hover:text-white hidden">
        .page-slide-in {
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }
        
        /* 滚动触发动画增强 */
        .scroll-reveal-enhanced {        <!-- 过滤器标签 -->
            opacity: 0;4 border-b bg-gray-50">
            transform: translateY(40px) scale(0.95);>
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);ve px-3 py-1 text-xs rounded-full bg-longling text-white" data-filter="all">
        }
        on>
        .scroll-reveal-enhanced.visible {lass="filter-btn px-3 py-1 text-xs rounded-full bg-gray-200 text-gray-700 hover:bg-gray-300" data-filter="chat">
            opacity: 1;
            transform: translateY(0) scale(1);>
        }lass="filter-btn px-3 py-1 text-xs rounded-full bg-gray-200 text-gray-700 hover:bg-gray-300" data-filter="login">
        
        .scroll-reveal-enhanced.visible.delay-1 {>
            transition-delay: 0.1s;lass="filter-btn px-3 py-1 text-xs rounded-full bg-gray-200 text-gray-700 hover:bg-gray-300" data-filter="notification">
        }
        >
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;
        }
                <!-- 历史记录列表 -->
        .scroll-reveal-enhanced.visible.delay-3 {yList" class="flex-1 overflow-y-auto">
            transition-delay: 0.3s;
        }
        tyHistory" class="text-center py-8 text-gray-500">
        /* 悬停态性能优化 */
        @media (hover: hover) {
            .product-card-enhanced:hover,00 mt-1">开始使用智能助手后，记录会出现在这里</p>
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {
                transform: translateY(-8px) translateZ(0);<!-- 历史记录项目将动态加载到这里 -->
            }
        }
        <!-- 加载更多 -->
        /* 移动端优化 */MoreHistory" class="p-4 text-center hidden">
        @media (max-width: 768px) {ext-sm">
            .product-card-enhanced,
            .footer-card-enhanced,
            .stats-card-enhanced {>
                transform: none !important;
            }
            
            .product-card-enhanced:hover,        <!-- 底部工具栏 -->
            .footer-card-enhanced:hover,4 border-t bg-gray-50">
            .stats-card-enhanced:hover {items-center text-xs text-gray-500">
                transform: none !important;
            }
            ry" class="text-tech-blue hover:text-opacity-80">
            /* 减少移动端动画 */
            .feature-icon-enhanced:hover {
                transform: translateY(-4px);on>
            }d="historySettings" class="text-tech-blue hover:text-opacity-80">
            
            .button-enhanced-primary:hover,
            .button-enhanced-secondary:hover {on>
                transform: translateY(-2px);
            }
        }
        
        /* 减少动画首选项支持 */
        @media (prefers-reduced-motion: reduce) {    <!-- 无障碍工具栏 -->
            *,ibilityToolbar" class="fixed top-0 right-0 bg-white shadow-lg rounded-bl-lg p-2 z-50 transform translate-x-full transition-transform duration-300">
            *::before,
            *::after {t" class="p-2 rounded hover:bg-gray-100 text-gray-700" title="切换高对比度">
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;d="increaseFontSize" class="p-2 rounded hover:bg-gray-100 text-gray-700" title="增大字体">
                scroll-behavior: auto !important;
            }
            d="decreaseFontSize" class="p-2 rounded hover:bg-gray-100 text-gray-700" title="减小字体">
            .product-card-enhanced,
            .footer-card-enhanced,
            .stats-card-enhanced,d="toggleScreenReader" class="p-2 rounded hover:bg-gray-100 text-gray-700" title="屏幕阅读器">
            .feature-icon-enhanced {
                transform: none !important;
            }
        }
        
        /* 高性能滚动条样式 */    <script>
        ::-webkit-scrollbar {面加载完成后移除加载动画
            width: 8px;istener('load', () => {
            height: 8px;yId('pageLoader');
        }
        
        ::-webkit-scrollbar-track {isplay = 'none';
            background: rgba(10, 0, 22, 0.3);
            border-radius: 4px;
        }
                // 🎨 初始化Three.js量子球背景系统
        ::-webkit-scrollbar-thumb {r = document.getElementById('backgroundContainer');
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);
            border-radius: 4px;tiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        renderer.setSize(window.innerWidth, window.innerHeight);
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);
        }
                // 🌟 量子球核心系统
        /* Firefox滚动条 */Sphere {
        html {
            scrollbar-width: thin;
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);metry = new THREE.SphereGeometry(2, 64, 64);
        }
        
        /* 文本选择样式 */
        ::selection {e,
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;
        }s.mesh = new THREE.Mesh(geometry, material);
        
        ::-moz-selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;                // 内部光球
        }oreGeometry = new THREE.SphereGeometry(1.2, 32, 32);
        
        /* 加载状态优化 */
        .loading-state {e,
            position: relative;
            overflow: hidden;
        }s.core = new THREE.Mesh(coreGeometry, coreMaterial);
        
        .loading-state::after {
            content: '';                // 外部光环
            position: absolute;ingGeometry = new THREE.RingGeometry(2.5, 3, 64);
            top: 0;
            left: -100%;
            width: 100%;leSide,
            height: 100%;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 50%, s.ring = new THREE.Mesh(ringGeometry, ringMaterial);
                transparent 100%);
            animation: loadingShimmer 1.5s infinite;
        }
        
        @keyframes loadingShimmer {            update() {
            0% { left: -100%; }esh.rotation.y += 0.005;
            100% { left: 100%; }
        }
        
        /* 历史记录面板增强样式 */
        .history-panel {// 呼吸效果
            backdrop-filter: blur(20px);ulse = Math.sin(Date.now() * 0.001) * 0.1 + 1;
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }
        // 🌌 量子粒子云系统
        .history-item {ParticleCloud {
            transition: all 0.3s ease;
            border: 1px solid transparent;les = [];
            position: relative;();
            overflow: hidden;
        }
            createParticles() {
        .history-item::before { i < 100; i++) {
            content: '';SphereGeometry(0.02, 8, 8);
            position: absolute;
            left: 0;00D4FF,
            top: 0;
            width: 3px;om() * 0.8 + 0.2
            height: 100%;
            background: transparent;st particle = new THREE.Mesh(geometry, material);
            transition: all 0.3s ease;
        }// 随机位置分布在球体周围
 5 + Math.random() * 10;
        .history-item:hover {* 2;
            background: rgba(75, 85, 99, 0.3);
            border-color: rgba(168, 85, 247, 0.3);
            transform: translateX(4px);particle.position.x = radius * Math.sin(phi) * Math.cos(theta);
        }

        .history-item:hover::before {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);particle.userData = {
        }s,

        .history-item.chat::before {
            background: #10b981;th.random() * 0.02 + 0.01
        }

        .history-item.login::before {this.particles.push(particle);
            background: #3b82f6;
        }

        .history-item.notification::before {
            background: #f59e0b;            update() {
        }articles.forEach(particle => {
cle.userData.speed;
        .filter-tag {0.5;
            transition: all 0.2s ease;
            cursor: pointer;const radius = particle.userData.baseRadius;
            border: 1px solid rgba(255, 255, 255, 0.2);icle.userData.phi) * Math.cos(particle.userData.theta);
        }

        .filter-tag:hover {
            background: rgba(168, 85, 247, 0.2);// 闪烁效果
            border-color: rgba(168, 85, 247, 0.5);e.material.opacity = 0.3 + Math.sin(Date.now() * 0.005 + particle.userData.theta) * 0.3;
            transform: scale(1.05);
        }

        .filter-tag.active {
            background: linear-gradient(45deg, #a855f7, #3b82f6);        // ⚡ 量子波形系统
            border-color: transparent;umWaveForm {
            color: white;
        }
Geometry = new THREE.PlaneGeometry(8, 6, 128, 96);
        /* 搜索框样式 */
        .search-input {
            background: rgba(17, 24, 39, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.2);e,
            transition: all 0.3s ease;
        }
s.waveMesh = new THREE.Mesh(waveGeometry, waveMaterial);
        .search-input:focus {
            background: rgba(17, 24, 39, 0.8);h.PI / 6;
            border-color: rgba(168, 85, 247, 0.5);
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
        }this.time = 0;

        /* 空状态样式 */
        .empty-state {            update() {
            opacity: 0.7;ime += 0.02;
            transition: opacity 0.3s ease;this.waveMesh.geometry.attributes.position.array;
        }
for (let i = 0; i < positions.length; i += 3) {
        .empty-state:hover {
            opacity: 1; 1];
        }
// 复杂波形计算
        /* 导出和清空按钮样式 */litude = 0.8;
        .action-button {
            transition: all 0.3s ease;(x * frequency + this.time) * Math.cos(y * frequency + this.time);
            position: relative;
            overflow: hidden;
        }positions[i + 2] = amplitude * (wave1 + wave2);

        .action-button::after {
            content: '';this.waveMesh.geometry.attributes.position.needsUpdate = true;
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;        // 🔗 量子连接线系统
            height: 0;ConnectionLines {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;= [];
            transform: translate(-50%, -50%);ctionLines();
            transition: all 0.4s ease;
        }
            createConnectionLines() {
        .action-button:hover::after {0; i++) {
            width: 300px;
            height: 300px;50;
        }
for (let j = 0; j < numPoints; j++) {
        .action-button:hover {
            transform: translateY(-2px);* Math.PI * 4) * 2;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }
points.push(new THREE.Vector3(
        /* 响应式设计增强 */* radius,
        @media (max-width: 768px) {
            .emoji-panel {
                width: 90vw !important;
                max-width: 280px;
                left: 50% !important;
                right: auto !important;const curve = new THREE.CatmullRomCurve3(points);
                transform: translateX(-50%);, 64, 0.02, 8, false);
            }

            .history-panel {
                width: 95vw !important;
                height: 80vh !important;
                left: 2.5vw !important;
                right: auto !important;const line = new THREE.Mesh(tubeGeometry, tubeMaterial);
            } 0.02 };

            .chat-widget {
                width: 95vw !important;
                height: 80vh !important;
                left: 2.5vw !important;
                right: auto !important;            update() {
                bottom: 2vh !important;ines.forEach(line => {
            }userData.rotationSpeed;
        }

        /* 滚动条美化 */// 透明度闪烁
        .custom-scrollbar::-webkit-scrollbar {erial.opacity = 0.3 + Math.sin(Date.now() * 0.003) * 0.3;
            width: 6px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(17, 24, 39, 0.3);        // 🌟 量子光束系统
            border-radius: 3px;mLightBeams {
        }
= [];
        .custom-scrollbar::-webkit-scrollbar-thumb {Beams();
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;
        }            createLightBeams() {
i < 8; i++) {
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {E.CylinderGeometry(0.01, 0.5, 20, 8);
            background: linear-gradient(to bottom, #9333ea, #2563eb);
        }
e,
        /* 加载动画 */
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; }
            50% { opacity: 1; }const beam = new THREE.Mesh(geometry, material);
            80%, 100% { opacity: 0.2; }
        } * Math.PI * 2) * 8,

        .loading-dots span {
            animation: loading-dots 1.4s infinite;
        }am.lookAt(0, 0, 0);
le: i / 8 * Math.PI * 2 };
        .loading-dots span:nth-child(2) {
            animation-delay: 0.2s;this.beams.push(beam);
        }

        .loading-dots span:nth-child(3) {
            animation-delay: 0.4s;
        }            update() {
        eams.forEach((beam, index) => {
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {eam.userData.angle) * 8;
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }
        }
        // 光束强度变化
        @keyframes neon-flicker {rial.opacity = 0.2 + Math.sin(Date.now() * 0.005 + index) * 0.2;
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }
        
        @keyframes slide-in-left {        // 🌌 量子传送效果
            from { transform: translateX(-100%); opacity: 0; }mTeleportEffect {
            to { transform: translateX(0); opacity: 1; }
        }s = [];
        ();
        @keyframes slide-in-right {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }            createPortals() {
        }0; i < 3; i++) {
        w THREE.RingGeometry(1, 1.5, 32);
        @keyframes scale-up {
            from { transform: scale(0.8); opacity: 0; } 0xFF2B75,
            to { transform: scale(1); opacity: 1; }
        }
        oubleSide
        @keyframes fade-in-up {
            from { transform: translateY(30px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }const portal = new THREE.Mesh(portalGeometry, portalMaterial);
        }
         Math.PI * 2) * 12,
        /* 动画类 */
        .float-animation { animation: float 6s ease-in-out infinite; }
        .wave-animation { animation: wave 4s ease-in-out infinite; }
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }rtal.userData = {
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }osition.y,
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }3
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }this.portals.push(portal);
        
        /* 触摸反馈 */
        .touch-feedback {
            position: relative;
            overflow: hidden;            update() {
        }time = Date.now() * 0.001;
        
        .touch-feedback::after {this.portals.forEach((portal, index) => {
            content: '';
            position: absolute;.userData.baseY + Math.sin(time + portal.userData.phase) * 2;
            top: 50%;
            left: 50%;// 传送门闪烁效果
            width: 0;erial.opacity = 0.4 + Math.sin(time * 3 + index) * 0.3;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }        // 🔮 量子能量核心
        mEnergyCore {
        .touch-feedback:active::after {
            width: 300px;= [];
            height: 300px;yCores();
        }
        
        /* 加载状态动画 */            createEnergyCores() {
        .loading-spinner { < 6; i++) {
            display: inline-block;THREE.SphereGeometry(0.3, 16, 16);
            width: 20px;
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);e,
            border-radius: 50%;
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;
        }const core = new THREE.Mesh(coreGeometry, coreMaterial);
        
        @keyframes spin { * Math.PI * 2) * 4,
            to { transform: rotate(360deg); }
        }
        
        /* 进度条动画 */re.userData = {
        .progress-bar {* Math.PI * 2,
            position: relative; * 0.02
            overflow: hidden;
        }
        this.cores.push(core);
        .progress-bar::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;            update() {
            width: 100%;ores.forEach(core => {
            height: 100%;ore.userData.speed;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);gle) * 4;
            animation: progress-shine 2s infinite;
        }
        // 能量核心脉动
        @keyframes progress-shine {se = Math.sin(Date.now() * 0.005) * 0.3 + 1;
            0% { left: -100%; }
            100% { left: 100%; }+ Math.sin(Date.now() * 0.008) * 0.3;
        }

        /* 3D 变换和悬停效果 */
        .quantum-card {
            transform-style: preserve-3d;        // 初始化所有量子系统
            perspective: 1000px;mSphere = new QuantumSphere();
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);loud();
        }
ectionLines();
        .quantum-card:hover {
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);ffect();
        }

        .quantum-text {camera.position.z = 15;
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            -webkit-background-clip: text;        // 🎮 鼠标交互
            background-clip: text; = 0, mouseY = 0;
            color: transparent;usemove', (e) => {
            animation: quantum-flicker 2s infinite;) * 0.0005;
        };

        .matrix-text {
            font-family: 'Fira Code', monospace;        // 📱 触摸交互支持
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);entListener('touchmove', (e) => {
        }
ches[0].clientX - window.innerWidth/2) * 0.0005;
        /* 粒子系统容器 */;
        .particle-container {
            position: absolute;
            width: 100%;
            height: 100%;        // 🌊 滚动交互
            pointer-events: none;Y = 0;
            overflow: hidden;istener('scroll', () => {
        }

        /* 高级滚动动画 */// 根据滚动位置调整量子球效果
        .scroll-reveal {ress = Math.min(scrollY / window.innerHeight, 1);
            opacity: 0;
            transform: translateY(20px);// 调整量子球的位置和大小
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);ere && quantumSphere.mesh) {
        }Progress * -3;
ogress * 0.01;
        .scroll-reveal.visible {
            opacity: 1;
            transform: translateY(0);// 调整粒子云密度
        }leCloud && particleCloud.particles) {
 {
        /* 全局动画性能优化 */ 0.6 - scrollProgress * 0.4);
        .hardware-accelerated,
        .product-card-enhanced,
        .footer-card-enhanced,
        .button-enhanced-primary,
        .button-enhanced-secondary,        // 📊 性能监控
        .feature-icon-enhanced,ount = 0;
        .stats-card-enhanced {ormance.now();
            will-change: transform, opacity;
            transform: translateZ(0);function checkPerformance() {
            backface-visibility: hidden;
        }Time = performance.now();
        
        /* 页面切换动画 */if (currentTime - lastTime >= 1000) {
        @keyframes pageSlideIn {
            from {
                opacity: 0;entTime;
                transform: translateY(30px);
            }// 根据FPS自动调整质量
            to {{
                opacity: 1;
                transform: translateY(0);cleCloud && particleCloud.particles.length > 50) {
            }
        }
        
        .page-slide-in {e if (fps > 50 && particleCloud && particleCloud.particles.length < 100) {
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }('🚀 性能良好，可以增强效果');
        
        /* 滚动触发动画增强 */
        .scroll-reveal-enhanced {
            opacity: 0;
            transform: translateY(40px) scale(0.95);        // 响应式处理
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);ddEventListener('resize', () => {
        }indow.innerHeight;
        
        .scroll-reveal-enhanced.visible {th, window.innerHeight);
            opacity: 1;
            transform: translateY(0) scale(1);
        }        // 🎬 主动画循环
        imate() {
        .scroll-reveal-enhanced.visible.delay-1 {Frame(animate);
            transition-delay: 0.1s;
        }// 性能监控
        rformance();
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;// 更新所有量子系统
        }re.update();
        
        .scroll-reveal-enhanced.visible.delay-3 {
            transition-delay: 0.3s;date();
        }
        e();
        /* 悬停态性能优化 */
        @media (hover: hover) {
            .product-card-enhanced:hover,// 相机跟随鼠标
            .footer-card-enhanced:hover,sition.x += (mouseX - camera.position.x) * 0.05;
            .stats-card-enhanced:hover {;
                transform: translateY(-8px) translateZ(0);
            }
        }renderer.render(scene, camera);
        
        /* 移动端优化 */
        @media (max-width: 768px) {animate();
            .product-card-enhanced,
            .footer-card-enhanced,        console.log('🎨 3D量子球系统初始化完成 - 包含波形、连接线、光束、传送门、能量核心效果');
            .stats-card-enhanced {
                transform: none !important;        // 🌟 量子球集成器初始化
            }tumChatIntegrator !== 'undefined') {
            ntegrator();
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,leCloud && waveForm) {
            .stats-card-enhanced:hover {Reference({
                transform: none !important;
            }
            
            /* 减少移动端动画 */nLines,
            .feature-icon-enhanced:hover {
                transform: translateY(-4px);ect,
            }
            
            .button-enhanced-primary:hover,,
            .button-enhanced-secondary:hover {r
                transform: translateY(-2px);
            }sole.log('✨ 量子球引用已成功设置');
        }
        ole.warn('⚠️ 部分量子球组件未初始化，将在后续设置引用');
        /* 减少动画首选项支持 */
        @media (prefers-reduced-motion: reduce) {/ 量子球集成器使用init方法，不是initialize
            *,;
            *::before,
            *::after {ole.warn('⚠️ 量子球集成器未找到，首页将独立运行');
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;        // 粒子系统初始化（保留原有代码）
                scroll-behavior: auto !important;ainer = document.querySelector('.particle-container');
            }
            
            .product-card-enhanced,
            .footer-card-enhanced,        for (let i = 0; i < particleCount; i++) {
            .stats-card-enhanced,nt('div');
            .feature-icon-enhanced {
                transform: none !important;rticle);
            }
        }
        
        /* 高性能滚动条样式 */        function animateParticles() {
        ::-webkit-scrollbar {e => {
            width: 8px;00;
            height: 8px;
        } * 2 + 3;
        
        ::-webkit-scrollbar-track {
            background: rgba(10, 0, 22, 0.3);                particle.animate([
            border-radius: 4px;translate(${x}%, ${y}%)`, opacity: 1 },
        }city: 0 }
        
        ::-webkit-scrollbar-thumb {duration: duration * 1000,
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);
            border-radius: 4px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);        animateParticles();
        }
                // 量子3D模型初始化
        /* Firefox滚动条 */ontainer = document.getElementById('quantum-model');
        html {
            scrollbar-width: thin;
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);ew THREE.Scene();
        }tiveCamera(75, modelContainer.offsetWidth / modelContainer.offsetHeight, 0.1, 1000);
        
        /* 文本选择样式 */
        ::selection {renderer.setSize(modelContainer.offsetWidth, modelContainer.offsetHeight);
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;domElement);
        }
                    // 创建量子球几何体
        ::-moz-selection {umSpheres = [];
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);HREE.Group();
            color: white;
        }            // 主量子球
        ainGeometry = new THREE.SphereGeometry(1, 32, 32);
        /* 加载状态优化 */
        .loading-state {
            position: relative;e,
            overflow: hidden;
        }ue
        
        .loading-state::after {st mainSphere = new THREE.Mesh(mainGeometry, mainMaterial);
            content: '';
            position: absolute;
            top: 0;            // 创建轨道量子粒子
            left: -100%;= 0; i < 8; i++) {
            width: 100%;new THREE.SphereGeometry(0.1, 8, 8);
            height: 100%;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 50%, 
                transparent 100%);
            animation: loadingShimmer 1.5s infinite;st particle = new THREE.Mesh(particleGeometry, particleMaterial);
        }
        // 设置初始位置
        @keyframes loadingShimmer {le = (i / 8) * Math.PI * 2;
            0% { left: -100%; }
            100% { left: 100%; }.x = Math.cos(angle) * radius;
        }
        ;
        /* 历史记录面板增强样式 */
        .history-panel {quantumSpheres.push(particle);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);            scene.add(quantumGroup);
        }

        .history-item {            // 动画循环
            transition: all 0.3s ease;n animateQuantumModel() {
            border: 1px solid transparent;eQuantumModel);
            position: relative;
            overflow: hidden;                // 主球旋转
        }ere.rotation.x += 0.01;

        .history-item::before {
            content: '';                // 粒子轨道运动
            position: absolute;heres.forEach((particle, index) => {
            left: 0;
            top: 0;antumSpheres.length) * Math.PI * 2;
            width: 3px;
            height: 100%;
            background: transparent;particle.position.x = Math.cos(angle) * radius;
            transition: all 0.3s ease;
        }) * 1.5;

        .history-item:hover {// 粒子自旋
            background: rgba(75, 85, 99, 0.3);e.rotation.x += 0.02;
            border-color: rgba(168, 85, 247, 0.3);
            transform: translateX(4px);
        }
                // 整体缓慢旋转
        .history-item:hover::before {oup.rotation.y += 0.005;
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
        }                renderer.render(scene, camera);

        .history-item.chat::before {
            background: #10b981;            animateQuantumModel();
        }
            // 响应式调整
        .history-item.login::before {ddEventListener('resize', () => {
            background: #3b82f6;
        }fsetWidth / modelContainer.offsetHeight;

        .history-item.notification::before {offsetWidth, modelContainer.offsetHeight);
            background: #f59e0b;
        }

        .filter-tag {
            transition: all 0.2s ease;        // AI大脑模型动画
            cursor: pointer;elContainer = document.getElementById('aiModel');
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        function pulseAiModel() {
        .filter-tag:hover {lPulse === 1 ? 1.05 : 1;
            background: rgba(168, 85, 247, 0.2);iModelPulse})`;
            border-color: rgba(168, 85, 247, 0.5);
            transform: scale(1.05);
        }        setInterval(pulseAiModel, 1000);

        .filter-tag.active {        // 登录按钮事件 - 增强版
            background: linear-gradient(45deg, #a855f7, #3b82f6);mentById('loginBtn').addEventListener('click', (e) => {
            border-color: transparent;
            color: white;按钮被点击');
        }

        /* 搜索框样式 */
        .search-input {        // 登录模态窗口控制函数
            background: rgba(17, 24, 39, 0.6);LoginModal() {
            border: 1px solid rgba(255, 255, 255, 0.2);getElementById('loginModal');
            transition: all 0.3s ease;;
        }
if (modal) {
        .search-input:focus {assList.remove('hidden');
            background: rgba(17, 24, 39, 0.8);
            border-color: rgba(168, 85, 247, 0.5);
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);// 动画效果
        }out(() => {
sList.remove('scale-95', 'opacity-0');
        /* 空状态样式 */
        .empty-state {
            opacity: 0.7;
            transition: opacity 0.3s ease;
        }
        function hideLoginModal() {
        .empty-state:hover {getElementById('loginModal');
            opacity: 1;;
        }
if (modal) {
        /* 导出和清空按钮样式 */d.classList.remove('scale-100', 'opacity-100');
        .action-button {
            transition: all 0.3s ease;
            position: relative;setTimeout(() => {
            overflow: hidden;t.add('hidden');
        };

        .action-button::after {
            content: '';
            position: absolute;
            top: 50%;        // 绑定关闭按钮事件
            left: 50%;tElementById('closeLoginModal')?.addEventListener('click', hideLoginModal);
            width: 0;
            height: 0;        // 点击背景关闭模态窗口
            background: rgba(255, 255, 255, 0.1);lementById('loginModal')?.addEventListener('click', (e) => {
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: all 0.4s ease;
        }

        .action-button:hover::after {        // ESC键关闭模态窗口
            width: 300px;ventListener('keydown', (e) => {
            height: 300px;
        }

        .action-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);// 密码显示/隐藏切换
        }ElementById('togglePassword')?.addEventListener('click', function() {

        /* 响应式设计增强 */
        @media (max-width: 768px) {
            .emoji-panel {if (passwordInput.type === 'password') {
                width: 90vw !important;
                max-width: 280px;-slash';
                left: 50% !important;
                right: auto !important;wordInput.type = 'password';
                transform: translateX(-50%);
            }

            .history-panel {
                width: 95vw !important;        // 登录表单提交处理
                height: 80vh !important;tElementById('loginForm')?.addEventListener('submit', async (e) => {
                left: 2.5vw !important;
                right: auto !important;
            }const email = document.getElementById('username').value;
').value;
            .chat-widget {
                width: 95vw !important;if (!email || !password) {
                height: 80vh !important;登录信息');
                left: 2.5vw !important;
                right: auto !important;
                bottom: 2vh !important;
            }try {
        }onst response = await fetch('/api/auth/login', {

        /* 滚动条美化 */
        .custom-scrollbar::-webkit-scrollbar {nt-Type': 'application/json',
            width: 6px;
        }dy: JSON.stringify({ email, password })

        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(17, 24, 39, 0.3);if (response.ok) {
            border-radius: 3px;wait response.json();
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {辑
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;t error = await response.json();
        }败，请检查用户名和密码');

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {ch (error) {
            background: linear-gradient(to bottom, #9333ea, #2563eb);('登录请求失败:', error);
        }

        /* 加载动画 */
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; }        function showLoginError(message) {
            50% { opacity: 1; }getElementById('loginError');
            80%, 100% { opacity: 0.2; };
        }
if (errorElement && errorText) {
        .loading-dots span {age;
            animation: loading-dots 1.4s infinite;idden');
        }
// 3秒后自动隐藏错误信息
        .loading-dots span:nth-child(2) {=> {
            animation-delay: 0.2s;lassList.add('hidden');
        }

        .loading-dots span:nth-child(3) {
            animation-delay: 0.4s;
        }        function hideLoginModal() {
        getElementById('loginModal');
        /* AI助手按钮发光动画 */d');
        @keyframes ai-glow {
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }if (!modal || !modalContent) {
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }到');
        }
        
        @keyframes neon-flicker {
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }console.log('🎭 隐藏登录模态框');
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }ove('scale-100', 'opacity-100');
        }
        
        @keyframes slide-in-left {t.add('hidden');
            from { transform: translateX(-100%); opacity: 0; };
            to { transform: translateX(0); opacity: 1; }'';
        }
        
        @keyframes slide-in-right {
            from { transform: translateX(100%); opacity: 0; }        document.getElementById('closeLoginModal').addEventListener('click', hideLoginModal);
            to { transform: translateX(0); opacity: 1; }
        }        // 点击模态框外部关闭
        ElementById('loginModal').addEventListener('click', (e) => {
        @keyframes scale-up {
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }
        
        @keyframes fade-in-up {        // 登录模式切换
            from { transform: translateY(30px); opacity: 0; }malLoginTab = document.getElementById('normalLoginTab');
            to { transform: translateY(0); opacity: 1; }
        }
        
        /* 动画类 */        normalLoginTab.addEventListener('click', () => {
        .float-animation { animation: float 6s ease-in-out infinite; }-r', 'from-longling', 'to-tech-blue', 'text-white');
        .wave-animation { animation: wave 4s ease-in-out infinite; }
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }, 'from-longling', 'to-tech-blue', 'text-white');
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }        guestLoginTab.addEventListener('click', () => {
        -r', 'from-longling', 'to-tech-blue', 'text-white');
        /* 触摸反馈 */
        .touch-feedback {r', 'from-longling', 'to-tech-blue', 'text-white');
            position: relative;
            overflow: hidden;
        }
        
        .touch-feedback::after {        // 访客模式处理增强
            content: '';ndleGuestMode() {
            position: absolute;
            top: 50%;e.setItem('guestMode', 'true');
            left: 50%;ingify({
            width: 0;
            height: 0;
            border-radius: 50%; Date().toISOString(),
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }// 保存访客登录历史
        ry('login', { 
        .touch-feedback:active::after {
            width: 300px;e().toISOString(),
            height: 300px;
        }
        
        /* 加载状态动画 */showNotification('已进入访客模式，部分功能受限', 'info');
        .loading-spinner {
            display: inline-block;ode();
            width: 20px;
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);        function updateUIForGuestMode() {
            border-radius: 50%;ElementById('loginBtn');
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;xtContent = '访客模式 ▼';
        }w-600');
        o-r', 'from-longling', 'to-tech-blue', 'bg-green-600');
        @keyframes spin {
            to { transform: rotate(360deg); }
        }// 显示访客模式提示
        deNotification();
        /* 进度条动画 */
        .progress-bar {
            position: relative;        function showGuestModeNotification() {
            overflow: hidden;teElement('div');
        }ellow-500/90 text-white p-4 rounded-lg shadow-lg z-50 max-w-sm';
        
        .progress-bar::after {-start space-x-3">
            content: ''; mt-1"></i>
            position: absolute;
            top: 0;h4 class="font-bold mb-1">访客模式</h4>
            left: -100%;受限。</p>
            width: 100%;:no-underline" onclick="this.closest('.fixed').remove()">
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);n>
            animation: progress-shine 2s infinite;
        }
        
        @keyframes progress-shine {cument.body.appendChild(notification);
            0% { left: -100%; }
            100% { left: 100%; }// 5秒后自动移除
        }(() => {
on.parentNode) {
        /* 3D 变换和悬停效果 */
        .quantum-card {
            transform-style: preserve-3d;00);
            perspective: 1000px;
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }        // 检查登录状态
checkLoginStatus() {
        .quantum-card:hover {e.getItem('token');
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);tMode');
        }

        .quantum-text {if (token && userData) {
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            -webkit-background-clip: text;onst user = JSON.parse(userData);
            background-clip: text;
            color: transparent;
            animation: quantum-flicker 2s infinite;('解析用户数据失败:', error);
        }

        .matrix-text {e if (guestMode === 'true' && userData) {
            font-family: 'Fira Code', monospace;
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);onst user = JSON.parse(userData);
        }

        /* 粒子系统容器 */('解析访客数据失败:', error);
        .particle-container {
            position: absolute;
            width: 100%;
            height: 100%;
            pointer-events: none;
            overflow: hidden;        function clearLoginStatus() {
        }token');
e');
        /* 高级滚动动画 */
        .scroll-reveal {in');
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }        // 页面加载时检查登录状态
entListener('DOMContentLoaded', () => {
        .scroll-reveal.visible {
            opacity: 1;res();
            transform: translateY(0);
        }
// 初始化量子聊天集成器
        /* 全局动画性能优化 */itializeQuantumChatIntegrator === 'function') {
        .hardware-accelerated,
        .product-card-enhanced,nitializeQuantumChatIntegrator();
        .footer-card-enhanced,
        .button-enhanced-primary,
        .button-enhanced-secondary,('❌ 量子聊天集成器初始化失败:', error);
        .feature-icon-enhanced,
        .stats-card-enhanced {e {
            will-change: transform, opacity;ole.warn('⚠️ initializeQuantumChatIntegrator 函数不存在');
            transform: translateZ(0);
            backface-visibility: hidden;
        }
                // 初始化页面功能
        /* 页面切换动画 */nitializePageFeatures() {
        @keyframes pageSlideIn {
            from {f minimalistBackground !== 'undefined') {
                opacity: 0;ound.constructor;
                transform: translateY(30px);
            }
            to {// 初始化返回顶部按钮
                opacity: 1;p();
                transform: translateY(0);
            }// 初始化页面滚动动画
        }imations();
        
        .page-slide-in {// 初始化移动端菜单
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);enu();
        }
        // 初始化主题切换
        /* 滚动触发动画增强 */oggle();
        .scroll-reveal-enhanced {
            opacity: 0;// 初始化量子聊天系统
            transform: translateY(40px) scale(0.95);hatSystem();
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }// 初始化登录模态框
        dal();
        .scroll-reveal-enhanced.visible {
            opacity: 1;
            transform: translateY(0) scale(1);        // 返回顶部功能
        }initBackToTop() {
        ocument.getElementById('backToTop');
        .scroll-reveal-enhanced.visible.delay-1 {
            transition-delay: 0.1s;window.addEventListener('scroll', () => {
        }
        ve('hidden');
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;ToTopBtn.classList.add('hidden');
        }
        
        .scroll-reveal-enhanced.visible.delay-3 {
            transition-delay: 0.3s;            backToTopBtn.addEventListener('click', () => {
        }
        
        /* 悬停态性能优化 */r: 'smooth'
        @media (hover: hover) {
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {
                transform: translateY(-8px) translateZ(0);        // 滚动动画初始化
            }nitScrollAnimations() {
        }ment.querySelectorAll('.animate-on-scroll');
        
        /* 移动端优化 */const observer = new IntersectionObserver((entries) => {
        @media (max-width: 768px) {
            .product-card-enhanced,ng) {
            .footer-card-enhanced,add('animate-in');
            .stats-card-enhanced {
                transform: none !important;
            }
            threshold: 0.1,
            .product-card-enhanced:hover,x 0px -50px 0px'
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {
                transform: none !important;            animatedElements.forEach(el => observer.observe(el));
            }
            
            /* 减少移动端动画 */        // 移动端菜单初始化
            .feature-icon-enhanced:hover {itMobileMenu() {
                transform: translateY(-4px);= document.getElementById('mobileMenuToggle');
            }
            
            .button-enhanced-primary:hover,if (mobileMenuToggle && mobileMenu) {
            .button-enhanced-secondary:hover {('click', () => {
                transform: translateY(-2px);
            }
        }
                        // 点击菜单项后关闭菜单
        /* 减少动画首选项支持 */erySelectorAll('a').forEach(link => {
        @media (prefers-reduced-motion: reduce) {
            *,;
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
                scroll-behavior: auto !important;        // 主题切换功能
            }initThemeToggle() {
            Storage.getItem('theme') || 'dark';
            .product-card-enhanced,eme);
            .footer-card-enhanced,
            .stats-card-enhanced,const themeToggle = document.getElementById('themeToggle');
            .feature-icon-enhanced {
                transform: none !important;dEventListener('click', () => {
            }ment.getAttribute('data-theme');
        }
        
        /* 高性能滚动条样式 */document.documentElement.setAttribute('data-theme', newTheme);
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;showNotification(`已切换到${newTheme === 'dark' ? '深色' : '浅色'}主题`, 'info');
        }
        
        ::-webkit-scrollbar-track {
            background: rgba(10, 0, 22, 0.3);
            border-radius: 4px;        // 初始化量子聊天系统
        }tQuantumChatSystem() {
        ;
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);// 等待量子聊天集成器加载完成
            border-radius: 4px; {
            border: 1px solid rgba(255, 255, 255, 0.1);ntumChatIntegrator) {
        }
        / 创建量子聊天集成器实例
        ::-webkit-scrollbar-thumb:hover {ChatIntegrator = new QuantumChatIntegrator();
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);
        }// 初始化聊天系统
        ntumChatIntegrator.initializeChatSystem().then(() => {
        /* Firefox滚动条 */
        html {
            scrollbar-width: thin;// 连接量子球
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);uantumChatIntegrator.connectToQuantumOrb();
        }
        // 测试triggerChatEvent方法（用于验证）
        /* 文本选择样式 */Integrator.triggerChatEvent === 'function') {
        ::selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;
        }// 显示浮动聊天按钮
        ingButton = document.getElementById('floating-chat-button');
        ::-moz-selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);yle.display = 'flex';
            color: white;
        }
        atch(error => {
        /* 加载状态优化 */❌ 量子聊天系统初始化失败:', error);
        .loading-state {
            position: relative;
            overflow: hidden;tch (error) {
        }('❌ 创建量子聊天集成器失败:', error);
        
        .loading-state::after {e {
            content: '';ole.warn('⚠️ QuantumChatIntegrator 类未找到，延迟重试...');
            position: absolute;
            top: 0;
            left: -100%;0);
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg,         // 初始化登录模态框
                transparent 0%, itLoginModal() {
                rgba(255, 255, 255, 0.1) 50%, 框...');
                transparent 100%);
            animation: loadingShimmer 1.5s infinite;const loginBtn = document.getElementById('loginBtn');
        }eLoginBtn');
        
        @keyframes loadingShimmer {Modal');
            0% { left: -100%; }
            100% { left: 100%; }
        }// 登录按钮点击事件
        mobileLoginBtn].forEach(btn => {
        /* 历史记录面板增强样式 */
        .history-panel {dEventListener('click', (e) => {
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }

        .history-item {// 关闭模态框
            transition: all 0.3s ease;eModalBtn) {
            border: 1px solid transparent;dEventListener('click', hideLoginModal);
            position: relative;
            overflow: hidden;
        }// 点击背景关闭模态框
al) {
        .history-item::before {dEventListener('click', (e) => {
            content: '';
            position: absolute;
            left: 0;
            top: 0;
            width: 3px;
            height: 100%;
            background: transparent;// 登录表单提交
            transition: all 0.3s ease;Form) {
        }dEventListener('submit', handleLogin);

        .history-item:hover {
            background: rgba(75, 85, 99, 0.3);// ESC键关闭模态框
            border-color: rgba(168, 85, 247, 0.3);EventListener('keydown', (e) => {
            transform: translateX(4px);loginModal.classList.contains('hidden')) {
        }

        .history-item:hover::before {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
        }
        // 显示登录模态框
        .history-item.chat::before {howLoginModal() {
            background: #10b981;ment.getElementById('loginModal');
        }
assList.remove('hidden');
        .history-item.login::before {
            background: #3b82f6;idden';
        }
// 聚焦到用户名输入框
        .history-item.notification::before {) => {
            background: #f59e0b;Input = document.getElementById('username');
        }
cus();
        .filter-tag {
            transition: all 0.2s ease;0);
            cursor: pointer;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        // 隐藏登录模态框
        .filter-tag:hover {ideLoginModal() {
            background: rgba(168, 85, 247, 0.2);...');
            border-color: rgba(168, 85, 247, 0.5);etElementById('loginModal');
            transform: scale(1.05);
        }ntent = loginModal.querySelector('.modal-content');

        .filter-tag.active {// 添加动画效果
            background: linear-gradient(45deg, #a855f7, #3b82f6);Content) {
            border-color: transparent;assList.remove('scale-100', 'opacity-100');
            color: white;
        }

        /* 搜索框样式 */setTimeout(() => {
        .search-input {ssList.add('hidden');
            background: rgba(17, 24, 39, 0.6);;
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }// 清空表单
oginForm = document.getElementById('loginForm');
        .search-input:focus {
            background: rgba(17, 24, 39, 0.8);set();
            border-color: rgba(168, 85, 247, 0.5);
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
        }console.log('✅ 登录模态框已隐藏');

        /* 空状态样式 */
        .empty-state {ole.warn('⚠️ 登录模态框元素未找到');
            opacity: 0.7;
            transition: opacity 0.3s ease;
        }
        // 将函数添加到window对象以确保全局访问
        .empty-state:hover {showLoginModal;
            opacity: 1;
        }
        // 处理登录表单提交
        /* 导出和清空按钮样式 */ndleLogin(e) {
        .action-button {
            transition: all 0.3s ease;
            position: relative;const email = document.getElementById('username').value;
            overflow: hidden;').value;
        }
if (!email || !password) {
        .action-button::after {有必填字段', 'error');
            content: '';issing required fields');
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;console.log('🔐 尝试登录:', email);
            height: 0;
            background: rgba(255, 255, 255, 0.1);// 这里应该调用实际的登录API
            border-radius: 50%;
            transform: translate(-50%, -50%);) => {
            transition: all 0.4s ease;on('登录成功！', 'success');
        }

        .action-button:hover::after {// 更新登录状态
            width: 300px;inState(true, { email: email });
            height: 300px;
        }000);

        .action-button:hover {
            transform: translateY(-2px);        // 更新登录状态
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);updateLoginState(isLoggedIn, userInfo = null) {
        };
eLoginBtn');
        /* 响应式设计增强 */
        @media (max-width: 768px) {if (isLoggedIn && userInfo) {
            .emoji-panel {
                width: 90vw !important;, mobileLoginBtn].forEach(btn => {
                max-width: 280px;
                left: 50% !important;xtContent = '用户中心';
                right: auto !important;
                transform: translateX(-50%);ref = 'profile.html';
            }

            .history-panel {
                width: 95vw !important;
                height: 80vh !important;// 保存登录状态到本地存储
                left: 2.5vw !important;etItem('isLoggedIn', 'true');
                right: auto !important;gify(userInfo));
            }
se {
            .chat-widget {复登录按钮
                width: 95vw !important;, mobileLoginBtn].forEach(btn => {
                height: 80vh !important;
                left: 2.5vw !important;xtContent = '登录';
                right: auto !important;
                bottom: 2vh !important;;
            }
        }

        /* 滚动条美化 */
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;// 清除登录状态
        }age.removeItem('isLoggedIn');

        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(17, 24, 39, 0.3);
            border-radius: 3px;
        }        // 增强通知函数
showNotification(message, type = 'info') {
        .custom-scrollbar::-webkit-scrollbar-thumb {v');
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;-green-500',
        }
500',
        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(to bottom, #9333ea, #2563eb);
        }
notification.className = `${colors[type]} text-white p-4 rounded-lg shadow-lg transition-transform transform translate-y-4 max-w-sm`;
        /* 加载动画 */
        @keyframes loading-dots {-center gap-2">
            0%, 20% { opacity: 0.2; }ess' ? 'check' : type === 'error' ? 'exclamation' : type === 'warning' ? 'exclamation-triangle' : 'info'}-circle"></i>
            50% { opacity: 1; }
            80%, 100% { opacity: 0.2; }
        }

        .loading-dots span {const container = document.getElementById('notificationContainer');
            animation: loading-dots 1.4s infinite;
        }pendChild(notification);

        .loading-dots span:nth-child(2) {                setTimeout(() => {
            animation-delay: 0.2s;lassList.add('translate-y-0');
        }

        .loading-dots span:nth-child(3) {                setTimeout(() => {
            animation-delay: 0.4s;lassList.remove('translate-y-0');
        }
        emove();
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }
        }
                // 密码可见性切换
        @keyframes neon-flicker {etElementById('togglePassword').addEventListener('click', function() {
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }
        if (passwordInput.type === 'password') {
        @keyframes slide-in-left {
            from { transform: translateX(-100%); opacity: 0; }e');
            to { transform: translateX(0); opacity: 1; }');
        }
        wordInput.type = 'password';
        @keyframes slide-in-right {ash');
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        @keyframes scale-up {        // 访客模式按钮
            from { transform: scale(0.8); opacity: 0; }getElementById('guestModeButton').addEventListener('click', handleGuestMode);
            to { transform: scale(1); opacity: 1; }
        }        // 移动端菜单按钮事件
        ElementById('mobileMenuBtn').addEventListener('click', () => {
        @keyframes fade-in-up {
            from { transform: translateY(30px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        
        /* 动画类 */        // 通知示例
        .float-animation { animation: float 6s ease-in-out infinite; }n showNotification(message) {
        .wave-animation { animation: wave 4s ease-in-out infinite; }eateElement('div');
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }-4 rounded-lg shadow-lg transition-transform transform translate-y-4';
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }tionContainer').appendChild(notification);
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }            setTimeout(() => {
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }lassList.add('translate-y-0');
        
        /* 触摸反馈 */
        .touch-feedback {            setTimeout(() => {
            position: relative;lassList.remove('translate-y-0');
            overflow: hidden;
        }emove();
        
        .touch-feedback::after {
            content: '';
            position: absolute;
            top: 50%;        // 示例：页面加载时显示通知
            left: 50%;Listener('load', () => {
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);        // 表单提交示例
            transform: translate(-50%, -50%);getElementById('contactForm').addEventListener('submit', (e) => {
            transition: width 0.6s, height 0.6s;
        }w FormData(e.target);
        );
        .touch-feedback:active::after {
            width: 300px;            console.log('表单提交数据:', data);
            height: 300px;
        }            // 模拟网络请求
        t(() => {
        /* 加载状态动画 */on('感谢您的留言，我们会尽快与您联系！');
        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);        // 登录表单处理
            border-radius: 50%;getElementById('loginForm').addEventListener('submit', async (e) => {
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;w FormData(e.target);
        }
        ord');
        @keyframes spin {e');
            to { transform: rotate(360deg); }
        }            // 清除之前的错误
        rror();
        /* 进度条动画 */rue);
        .progress-bar {
            position: relative;            try {
            overflow: hidden;/ 模拟登录API调用
        }se = await simulateLogin(email, password);
        
        .progress-bar::after {if (response.success) {
            content: '';
            position: absolute;age.setItem('token', response.token);
            top: 0;fy(response.user));
            left: -100%;
            width: 100%;setItem('remember_login', 'true');
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;// 保存登录历史
        }tory('login', { 
        
        @keyframes progress-shine { Date().toISOString(),
            0% { left: -100%; }
            100% { left: 100%; }
        }
showNotification('登录成功！欢迎回来', 'success');
        /* 3D 变换和悬停效果 */
        .quantum-card {(true, response.user);
            transform-style: preserve-3d;
            perspective: 1000px;LoginError(response.error || '用户名或密码错误');
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }ch (error) {
('登录错误:', error);
        .quantum-card:hover {试');
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);
        }nLoading(false);

        .quantum-text {
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            -webkit-background-clip: text;        // 忘记密码功能
            background-clip: text;getElementById('forgotPasswordLink').addEventListener('click', (e) => {
            color: transparent;
            animation: quantum-flicker 2s infinite;odal();
        }

        .matrix-text {        // 注册功能
            font-family: 'Fira Code', monospace;t.getElementById('registerLink').addEventListener('click', (e) => {
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);
        };

        /* 粒子系统容器 */
        .particle-container {        function showRegisterModal() {
            position: absolute;ateElement('div');
            width: 100%; backdrop-blur-sm flex items-center justify-center z-50';
            height: 100%;
            pointer-events: none;deep-purple/90 backdrop-blur-lg border border-longling/20 rounded-xl p-8 max-w-md w-full mx-4 transform transition-all max-h-[90vh] overflow-y-auto">
            overflow: hidden;
        }号</h3>
s="text-gray-400 hover:text-white transition-colors">
        /* 高级滚动动画 */
        .scroll-reveal {
            opacity: 0;
            transform: translateY(20px);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);<form id="registerForm">
        }
block text-gray-300 mb-2 text-sm">用户名</label>
        .scroll-reveal.visible {maxlength="20"
            opacity: 1;ounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none"
            transform: translateY(0);
        }t-1">只能包含字母、数字和下划线</div>

        /* 全局动画性能优化 */
        .hardware-accelerated,<div class="mb-4">
        .product-card-enhanced,block text-gray-300 mb-2 text-sm">邮箱</label>
        .footer-card-enhanced,
        .button-enhanced-primary,/50 border border-longling/20 rounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none"
        .button-enhanced-secondary,
        .feature-icon-enhanced,
        .stats-card-enhanced {
            will-change: transform, opacity;<div class="mb-4">
            transform: translateZ(0);block text-gray-300 mb-2 text-sm">密码</label>
            backface-visibility: hidden;
        }ord" id="regPassword" required minlength="6"
        ngling/20 rounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none pr-12"
        /* 页面切换动画 */
        @keyframes pageSlideIn {eRegPassword" class="absolute right-3 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-white transition-colors">
            from {
                opacity: 0;
                transform: translateY(30px);
            }lass="text-xs text-gray-400 mt-1">建议包含大小写字母、数字和特殊字符</div>
            to {
                opacity: 1;
                transform: translateY(0);<div class="mb-4">
            }block text-gray-300 mb-2 text-sm">确认密码</label>
        }ength="6"
        20 rounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none"
        .page-slide-in {
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }
        <!-- 用户协议 -->
        /* 滚动触发动画增强 */b-6">
        .scroll-reveal-enhanced {flex items-start gap-3 cursor-pointer">
            opacity: 0;lass="w-4 h-4 mt-1 rounded border-longling/20 bg-deep-purple/50 text-tech-blue focus:ring-tech-blue">
            transform: translateY(40px) scale(0.95);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }="#" class="text-tech-blue hover:text-opacity-80">《用户协议》</a>
        
        .scroll-reveal-enhanced.visible {a href="#" class="text-tech-blue hover:text-opacity-80">《隐私政策》</a>
            opacity: 1;
            transform: translateY(0) scale(1);
        }
        
        .scroll-reveal-enhanced.visible.delay-1 {<div id="registerError" class="hidden mb-4 p-3 bg-red-500/20 border border-red-500/50 rounded-lg">
            transition-delay: 0.1s;
        }ngle text-red-400"></i>
        
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;
        }
        <button type="submit" id="registerBtn"
        .scroll-reveal-enhanced.visible.delay-3 {t-to-r from-longling to-tech-blue text-white rounded-lg hover:opacity-90 transition-opacity disabled:opacity-50 mb-4">
            transition-delay: 0.3s;
        }idden ml-2"></i>
        
        /* 悬停态性能优化 */
        @media (hover: hover) {<!-- 登录链接 -->
            .product-card-enhanced:hover,ext-center text-sm">
            .footer-card-enhanced:hover,有账号？</span>
            .stats-card-enhanced:hover {d').remove(); showLoginModal();" class="text-tech-blue hover:text-opacity-80 transition-colors ml-1">立即登录</a>
                transform: translateY(-8px) translateZ(0);
            }
        }
        
        /* 移动端优化 */
        @media (max-width: 768px) {            document.body.appendChild(modal);
            .product-card-enhanced,
            .footer-card-enhanced,            // 密码可见性切换
            .stats-card-enhanced {etElementById('toggleRegPassword').addEventListener('click', function() {
                transform: none !important;
            }
            
            .product-card-enhanced:hover,if (passwordInput.type === 'password') {
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {e');
                transform: none !important;');
            }
            wordInput.type = 'password';
            /* 减少移动端动画 */ash');
            .feature-icon-enhanced:hover {
                transform: translateY(-4px);
            }
            
            .button-enhanced-primary:hover,            // 用户名验证
            .button-enhanced-secondary:hover {.getElementById('regUsername').addEventListener('input', (e) => {
                transform: translateY(-2px);
            }
        }
                    // 注册表单处理
        /* 减少动画首选项支持 */getElementById('registerForm').addEventListener('submit', async (e) => {
        @media (prefers-reduced-motion: reduce) {
            *,cument.getElementById('regUsername').value.trim();
            *::before,
            *::after {
                animation-duration: 0.01ms !important;sword').value;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
                scroll-behavior: auto !important;
            });
            
            .product-card-enhanced,                // 验证输入
            .footer-card-enhanced,rname.length < 3 || username.length > 20) {
            .stats-card-enhanced,
            .feature-icon-enhanced {
                transform: none !important;
            }
        }                if (!/^[a-zA-Z0-9_]+$/.test(username)) {
        );
        /* 高性能滚动条样式 */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;                if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
        }
        
        ::-webkit-scrollbar-track {
            background: rgba(10, 0, 22, 0.3);
            border-radius: 4px;                if (password.length < 6) {
        }少需要6位字符');
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);
            border-radius: 4px;                if (password !== confirmPassword) {
            border: 1px solid rgba(255, 255, 255, 0.1);;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);                if (!agreeTerms) {
        }ror('请先同意用户协议和隐私政策');
        
        /* Firefox滚动条 */
        html {
            scrollbar-width: thin;                // 设置loading状态
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3); true;
        }= '注册中...';
        n');
        /* 文本选择样式 */
        ::selection {                try {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);/ 模拟注册API调用
            color: white;omise(resolve => setTimeout(resolve, 2000));
        }
        // 模拟API响应
        ::-moz-selection {tingUsers = ['admin', 'test', 'demo']; // 模拟已存在的用户名
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);com']; // 模拟已存在的邮箱
            color: white;
        }if (existingUsers.includes(username.toLowerCase())) {
        
        /* 加载状态优化 */
        .loading-state {
            position: relative;
            overflow: hidden;if (existingEmails.includes(email.toLowerCase())) {
        }
        
        .loading-state::after {
            content: '';
            position: absolute;// 模拟成功注册
            top: 0;rData = {
            left: -100%;,
            width: 100%;ame,
            height: 100%;
            background: linear-gradient(90deg, 
                transparent 0%,  new Date().toISOString()
                rgba(255, 255, 255, 0.1) 50%, 
                transparent 100%);
            animation: loadingShimmer 1.5s infinite;                    // 保存用户数据（模拟）
        }setItem('registered_user', JSON.stringify(userData));
        
        @keyframes loadingShimmer {// 保存注册历史
            0% { left: -100%; }tory('login', { 
            100% { left: 100%; }
        } Date().toISOString(),
        
        /* 历史记录面板增强样式 */
        .history-panel {
            backdrop-filter: blur(20px);                    showNotification('注册成功！请登录您的账号', 'success');
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);// 自动填充登录表单
        }tElementById('username').value = email;
'';
        .history-item {
            transition: all 0.3s ease;
            border: 1px solid transparent;tch (error) {
            position: relative;rror('网络错误，请稍后重试');
            overflow: hidden;
        }abled = false;
 '创建账号';
        .history-item::before {');
            content: '';
            position: absolute;
            left: 0;
            top: 0;            function showRegisterError(message) {
            width: 3px;ElementById('registerError');
            height: 100%;
            background: transparent;
            transition: all 0.3s ease;idden');
        }

        .history-item:hover {
            background: rgba(75, 85, 99, 0.3);        function showForgotPasswordModal() {
            border-color: rgba(168, 85, 247, 0.3);ment('div');
            transform: translateX(4px); backdrop-blur-sm flex items-center justify-center z-50';
        }
deep-purple/90 backdrop-blur-lg border border-longling/20 rounded-xl p-8 max-w-md w-full mx-4 transform transition-all">
        .history-item:hover::before {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);码</h3>
        }s="text-gray-400 hover:text-white transition-colors">

        .history-item.chat::before {
            background: #10b981;
        }
<!-- 第一步：输入邮箱 -->
        .history-item.login::before {
            background: #3b82f6;xt-gray-300 mb-4">请输入您的注册邮箱，我们将发送重置密码的验证码</p>
        }

        .history-item.notification::before {mail" id="forgotEmail" required
            background: #f59e0b; border border-longling/20 rounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none"
        }

        .filter-tag {d="forgotError" class="hidden mb-4 p-3 bg-red-500/20 border border-red-500/50 rounded-lg">
            transition: all 0.2s ease;
            cursor: pointer;ngle text-red-400"></i>
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .filter-tag:hover {n type="submit" id="sendCodeBtn"
            background: rgba(168, 85, 247, 0.2);t-to-r from-longling to-tech-blue text-white rounded-lg hover:opacity-90 transition-opacity disabled:opacity-50">
            border-color: rgba(168, 85, 247, 0.5);
            transform: scale(1.05);dden ml-2"></i>
        }

        .filter-tag.active {
            background: linear-gradient(45deg, #a855f7, #3b82f6);
            border-color: transparent;<!-- 第二步：输入验证码和新密码 -->
            color: white;"hidden">
        }4">验证码已发送到您的邮箱，请查收并输入验证码及新密码</p>

        /* 搜索框样式 */
        .search-input {ext" id="verificationCode" required
            background: rgba(17, 24, 39, 0.6);der border-longling/20 rounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none"
            border: 1px solid rgba(255, 255, 255, 0.2);
            transition: all 0.3s ease;
        }lass="mb-4">
assword" id="newPassword" required
        .search-input:focus {rder border-longling/20 rounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none"
            background: rgba(17, 24, 39, 0.8);
            border-color: rgba(168, 85, 247, 0.5);
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);lass="mb-4">
        }assword" id="confirmPassword" required
 border-longling/20 rounded-lg text-white placeholder-gray-400 focus:border-tech-blue focus:outline-none"
        /* 空状态样式 */
        .empty-state {
            opacity: 0.7;d="resetError" class="hidden mb-4 p-3 bg-red-500/20 border border-red-500/50 rounded-lg">
            transition: opacity 0.3s ease;
        }ngle text-red-400"></i>

        .empty-state:hover {
            opacity: 1;
        }lass="flex gap-3">
" onclick="showStep1()"
        /* 导出和清空按钮样式 */ray-500 text-gray-300 rounded-lg hover:bg-gray-500/20 transition-colors">
        .action-button {
            transition: all 0.3s ease;on>
            position: relative;ype="submit" id="resetPasswordBtn"
            overflow: hidden;r from-longling to-tech-blue text-white rounded-lg hover:opacity-90 transition-opacity disabled:opacity-50">
        }
idden ml-2"></i>
        .action-button::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.1);            document.body.appendChild(modal);
            border-radius: 50%;
            transform: translate(-50%, -50%);            // 步骤切换函数
            transition: all 0.4s ease;owStep1 = function() {
        }ep1').classList.remove('hidden');

        .action-button:hover::after {
            width: 300px;
            height: 300px;            window.showStep2 = function() {
        }ep1').classList.add('hidden');
');
        .action-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);            // 第一步表单处理
        }etElementById('forgotPasswordForm').addEventListener('submit', async (e) => {

        /* 响应式设计增强 */ent.getElementById('forgotEmail').value;
        @media (max-width: 768px) {
            .emoji-panel {
                width: 90vw !important;);
                max-width: 280px;
                left: 50% !important;                // 验证邮箱格式
                right: auto !important;\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
                transform: translateX(-50%);;
            }

            .history-panel {
                width: 95vw !important;                // 设置loading状态
                height: 80vh !important; true;
                left: 2.5vw !important;= '发送中...';
                right: auto !important;n');
            }
                try {
            .chat-widget {/ 模拟发送验证码API
                width: 95vw !important;mise(resolve => setTimeout(resolve, 2000));
                height: 80vh !important;
                left: 2.5vw !important;// 模拟成功响应
                right: auto !important;cess = Math.random() > 0.3; // 70%成功率用于演示
                bottom: 2vh !important;
            }if (success) {
        }cation('验证码已发送到您的邮箱，请查收', 'success');

        /* 滚动条美化 */
        .custom-scrollbar::-webkit-scrollbar {ForgotError('forgotError', '邮箱地址不存在或发送失败，请检查后重试');
            width: 6px;
        }ch (error) {
or('forgotError', '网络错误，请稍后重试');
        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(17, 24, 39, 0.3);abled = false;
            border-radius: 3px; '发送验证码';
        });

        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;            // 第二步表单处理
        }etElementById('resetPasswordForm').addEventListener('submit', async (e) => {

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {nt.getElementById('verificationCode').value;
            background: linear-gradient(to bottom, #9333ea, #2563eb);e;
        }).value;

        /* 加载动画 */
        @keyframes loading-dots {);
            0%, 20% { opacity: 0.2; }
            50% { opacity: 1; }                // 验证输入
            80%, 100% { opacity: 0.2; }e.length !== 6) {
        }etError', '请输入6位验证码');

        .loading-dots span {
            animation: loading-dots 1.4s infinite;
        }                if (newPassword.length < 6) {
or', '密码至少需要6位字符');
        .loading-dots span:nth-child(2) {
            animation-delay: 0.2s;
        }
                if (newPassword !== confirmPassword) {
        .loading-dots span:nth-child(3) {入的密码不一致');
            animation-delay: 0.4s;
        }
        
        /* AI助手按钮发光动画 */                // 设置loading状态
        @keyframes ai-glow { true;
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }= '重置中...';
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }n');
        }
                        try {
        @keyframes neon-flicker {/ 模拟重置密码API
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }omise(resolve => setTimeout(resolve, 2000));
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }// 模拟成功响应
        cess = Math.random() > 0.2; // 80%成功率用于演示
        @keyframes slide-in-left {
            from { transform: translateX(-100%); opacity: 0; }if (success) {
            to { transform: translateX(0); opacity: 1; }cation('密码重置成功，请使用新密码登录', 'success');
        }
        
        @keyframes slide-in-right {getElementById('loginForm').reset();
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }ForgotError('resetError', '验证码错误或已过期，请重新获取');
        
        @keyframes scale-up {ch (error) {
            from { transform: scale(0.8); opacity: 0; }or('resetError', '网络错误，请稍后重试');
            to { transform: scale(1); opacity: 1; }
        }abled = false;
         '重置密码';
        @keyframes fade-in-up {');
            from { transform: translateY(30px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
                    // 验证码输入自动格式化
        /* 动画类 */lementById('verificationCode').addEventListener('input', (e) => {
        .float-animation { animation: float 6s ease-in-out infinite; }
        .wave-animation { animation: wave 4s ease-in-out infinite; }
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }        function showForgotError(elementId, message) {
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }Id(elementId);
        .scale-up { animation: scale-up 0.6s ease-out; }t');
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }
        idden');
        /* 触摸反馈 */
        .touch-feedback {
            position: relative;        // 键盘快捷键系统
            overflow: hidden;nitKeyboardShortcuts() {
        }down', (e) => {
        
        .touch-feedback::after {.key === 'k') {
            content: '';
            position: absolute;document.getElementById('loginModal');
            top: 50%;) {
            left: 50%;
            width: 0;
            height: 0;) => {
            border-radius: 50%;Input = document.getElementById('username');
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }eturn;
        
        .touch-feedback:active::after {
            width: 300px;                // ESC: 关闭模态框
            height: 300px; 'Escape') {
        }
        nModal = document.getElementById('loginModal');
        /* 加载状态动画 */)) {
        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);                    // 关闭聊天窗口
            border-radius: 50%;tWidgetElement = document.getElementById('chatWidget');
            border-top-color: #6C13FF;hidden')) {
            animation: spin 1s ease-in-out infinite;
        }ve('scale-100', 'opacity-100');
        
        @keyframes spin {
            to { transform: rotate(360deg); }ent.classList.add('hidden');
        }
        
        /* 进度条动画 */
        .progress-bar {
            position: relative;                    // 关闭历史记录面板
            overflow: hidden;ryPanel = document.getElementById('historyPanel');
        }ate-x-full')) {
        
        .progress-bar::after {
            content: '';
            position: absolute;
            top: 0;                    // 关闭任何动态创建的模态框
            left: -100%;dals = document.querySelectorAll('.fixed.inset-0.z-50');
            width: 100%;
            height: 100%;' && modal.id !== 'chatWidget') {
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;
        }
        urn;
        @keyframes progress-shine {
            0% { left: -100%; }
            100% { left: 100%; }                // Ctrl+/: 显示快捷键帮助
        }.key === '/') {

        /* 3D 变换和悬停效果 */tsHelp();
        .quantum-card {
            transform-style: preserve-3d;
            perspective: 1000px;
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);                // Ctrl+Enter: 在聊天窗口发送消息
        }== 'Enter') {
mentById('chatInput');
        .quantum-card:hover {{
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);
        }document.getElementById('sendChatMessage');

        .quantum-text {
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;                // Alt+C: 打开/关闭聊天窗口
            animation: quantum-flicker 2s infinite;ey === 'c') {
        }
n = document.getElementById('aiAssistantBtn');
        .matrix-text {
            font-family: 'Fira Code', monospace;
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);
        }
                // Alt+H: 打开/关闭历史记录面板
        /* 粒子系统容器 */ === 'h') {
        .particle-container {
            position: absolute;= document.getElementById('historyPanel');
            width: 100%;
            height: 100%;assList.toggle('-translate-x-full');
            pointer-events: none;
            overflow: hidden;eturn;
        }

        /* 高级滚动动画 */                // F1: 显示帮助信息
        .scroll-reveal { 'F1') {
            opacity: 0;);
            transform: translateY(20px);tsHelp();
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }

        .scroll-reveal.visible {
            opacity: 1;            // 添加快捷键提示到页面
            transform: translateY(0);tHint = document.createElement('div');
        }0 text-xs text-gray-500 bg-black/20 backdrop-blur-sm rounded px-2 py-1 opacity-0 transition-opacity pointer-events-none';

        /* 全局动画性能优化 */
        .hardware-accelerated,tHint);
        .product-card-enhanced,
        .footer-card-enhanced,            // 3秒后显示提示，5秒后隐藏
        .button-enhanced-primary, {
        .button-enhanced-secondary,lassList.remove('opacity-0');
        .feature-icon-enhanced,
        .stats-card-enhanced {
            will-change: transform, opacity;
            transform: translateZ(0);            setTimeout(() => {
            backface-visibility: hidden;lassList.remove('opacity-100');
        }
        
        /* 页面切换动画 */
        @keyframes pageSlideIn {
            from {        // 显示快捷键帮助
                opacity: 0;howKeyboardShortcutsHelp() {
                transform: translateY(30px);lement('div');
            } backdrop-blur-sm flex items-center justify-center z-50';
            to {
                opacity: 1;-purple/90 backdrop-blur-lg border border-longling/20 rounded-xl p-8 max-w-lg w-full mx-4 transform transition-all">
                transform: translateY(0);
            } items-center">
        }
        
        .page-slide-in {
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);on onclick="this.closest('.fixed').remove()" class="text-gray-400 hover:text-white transition-colors">
        }
        
        /* 滚动触发动画增强 */
        .scroll-reveal-enhanced {
            opacity: 0;<div class="space-y-4 text-sm">
            transform: translateY(40px) scale(0.95);1 gap-3">
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1); items-center p-3 bg-gray-800/30 rounded-lg">
        }
        ed text-xs font-mono">Ctrl + K</kbd>
        .scroll-reveal-enhanced.visible {
            opacity: 1;lass="flex justify-between items-center p-3 bg-gray-800/30 rounded-lg">
            transform: translateY(0) scale(1);
        }d text-xs font-mono">ESC</kbd>
        
        .scroll-reveal-enhanced.visible.delay-1 {lass="flex justify-between items-center p-3 bg-gray-800/30 rounded-lg">
            transition-delay: 0.1s;
        } text-xs font-mono">Alt + C</kbd>
        
        .scroll-reveal-enhanced.visible.delay-2 {lass="flex justify-between items-center p-3 bg-gray-800/30 rounded-lg">
            transition-delay: 0.2s;
        } text-xs font-mono">Alt + H</kbd>
        
        .scroll-reveal-enhanced.visible.delay-3 {lass="flex justify-between items-center p-3 bg-gray-800/30 rounded-lg">
            transition-delay: 0.3s;
        } text-xs font-mono">Ctrl + Enter</kbd>
        
        /* 悬停态性能优化 */lass="flex justify-between items-center p-3 bg-gray-800/30 rounded-lg">
        @media (hover: hover) {
            .product-card-enhanced:hover,d text-xs font-mono">Ctrl + / 或 F1</kbd>
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {
                transform: translateY(-8px) translateZ(0);
            }<div class="mt-6 p-4 bg-tech-blue/10 border border-tech-blue/20 rounded-lg">
        }
        tech-blue mr-2"></i>
        /* 移动端优化 */
        @media (max-width: 768px) {
            .product-card-enhanced,ss="text-gray-300 text-xs">
            .footer-card-enhanced,按 <kbd class="px-1 py-0.5 bg-gray-700 rounded text-xs">F1</kbd> 查看此帮助。
            .stats-card-enhanced {
                transform: none !important;
            }
            
            .product-card-enhanced:hover,<div class="mt-6 text-center">
            .footer-card-enhanced:hover,est('.fixed').remove()" 
            .stats-card-enhanced:hover {g to-tech-blue text-white rounded-lg hover:opacity-90 transition-opacity">
                transform: none !important;
            }n>
            
            /* 减少移动端动画 */
            .feature-icon-enhanced:hover {
                transform: translateY(-4px);
            }            document.body.appendChild(helpModal);
            
            .button-enhanced-primary:hover,            // 点击外部关闭
            .button-enhanced-secondary:hover {.addEventListener('click', (e) => {
                transform: translateY(-2px);
            }
        }
        
        /* 减少动画首选项支持 */
        @media (prefers-reduced-motion: reduce) {
            *,        // 登录相关辅助函数
            *::before,owLoginError(message) {
            *::after {getElementById('loginError');
                animation-duration: 0.01ms !important;;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;idden');
                scroll-behavior: auto !important;
            }
                    function hideLoginError() {
            .product-card-enhanced,cument.getElementById('loginError');
            .footer-card-enhanced,
            .stats-card-enhanced,
            .feature-icon-enhanced {
                transform: none !important;        function setLoginLoading(loading) {
            }tElementById('loginButton');
        }
        text');
        /* 高性能滚动条样式 */
        ::-webkit-scrollbar {if (loading) {
            width: 8px;n.disabled = true;
            height: 8px;dden');
        }
        
        ::-webkit-scrollbar-track {nButton.disabled = false;
            background: rgba(10, 0, 22, 0.3);');
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);        // 模拟登录API
            border-radius: 4px;tion simulateLogin(email, password) {
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        'admin@longling.tech' && password === '123456') {
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);ss: true,
        }oken_' + Date.now(),
        
        /* Firefox滚动条 */ 1,
        html {me: 'admin',
            scrollbar-width: thin;
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);
        }
        
        /* 文本选择样式 */if (email === 'demo@longling.tech' && password === 'demo') {
        ::selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);ss: true,
            color: white;oken_' + Date.now(),
        }
         2,
        ::-moz-selection {me: 'demo',
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;
        }
        
        /* 加载状态优化 */{
        .loading-state {lve({
            position: relative;ss: false,
            overflow: hidden;误'
        }
        
        .loading-state::after {00);
            content: '';
            position: absolute;
            top: 0;
            left: -100%;        function updateLoginStatus(isLoggedIn, user = null) {
            width: 100%;n');
            height: 100%;
            background: linear-gradient(90deg, dIn && user) {
                transparent 0%, = `${user.username} ▼`;
                rgba(255, 255, 255, 0.1) 50%, 
                transparent 100%);to-r', 'from-longling', 'to-tech-blue');
            animation: loadingShimmer 1.5s infinite;
        }nBtn.textContent = '登录';
        g-green-600', 'bg-yellow-600');
        @keyframes loadingShimmer { 'to-tech-blue');
            0% { left: -100%; }
            100% { left: 100%; }
        }
        
        /* 历史记录面板增强样式 */        // AI助手相关功能
        .history-panel {chatWidget === 'undefined') {
            backdrop-filter: blur(20px);ById('chatWidget');
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);onst closeChatWidget = document.getElementById('closeChatWidget');
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }atMessage');

        .history-item {
            transition: all 0.3s ease;        // 显示/隐藏聊天窗口
            border: 1px solid transparent;wChatWidget() {
            position: relative;move('hidden');
            overflow: hidden;
        }ssList.remove('scale-95', 'opacity-0');

        .history-item::before {
            content: '';ut.focus();
            position: absolute;
            left: 0;
            top: 0;        function hideChatWidget() {
            width: 3px;move('scale-100', 'opacity-100');
            height: 100%;
            background: transparent;
            transition: all 0.3s ease;ssList.add('hidden');
        }

        .history-item:hover {
            background: rgba(75, 85, 99, 0.3);        aiAssistantBtn.addEventListener('click', showChatWidget);
            border-color: rgba(168, 85, 247, 0.3);;
            transform: translateX(4px);
        }        // 字符计数
ut.addEventListener('input', function() {
        .history-item:hover::before {harCount');
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
        }
if (this.value.length > 450) {
        .history-item.chat::before {ext-red-400');
            background: #10b981;
        }Count.classList.remove('text-red-400');

        .history-item.login::before {
            background: #3b82f6;
        }        // Enter键发送消息
EventListener('keypress', function(e) {
        .history-item.notification::before {
            background: #f59e0b;
        }

        .filter-tag {
            transition: all 0.2s ease;
            cursor: pointer;        // 快捷操作按钮
            border: 1px solid rgba(255, 255, 255, 0.2);querySelectorAll('.quick-action-btn').forEach(btn => {
        }

        .filter-tag:hover {
            background: rgba(168, 85, 247, 0.2);
            border-color: rgba(168, 85, 247, 0.5);
            transform: scale(1.05);
        }        // 发送消息函数增强
ndMessage() {
        .filter-tag.active {Input.value.trim();
            background: linear-gradient(45deg, #a855f7, #3b82f6);
            border-color: transparent;
            color: white;            // 添加用户消息
        }eToChat('user', message);

        /* 搜索框样式 */Id('charCount').textContent = '0/500';
        .search-input {
            background: rgba(17, 24, 39, 0.6);            // 显示AI思考状态
            border: 1px solid rgba(255, 255, 255, 0.2);ing();
            transition: all 0.3s ease;
        }            // 检查是否为特殊命令
startsWith('/')) {
        .search-input:focus {ge);
            background: rgba(17, 24, 39, 0.8);
            border-color: rgba(168, 85, 247, 0.5);
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);
        }            // 模拟AI回复
t(() => {
        /* 空状态样式 */();
        .empty-state {generateAIResponse(message);
            opacity: 0.7;
            transition: opacity 0.3s ease;
        }

        .empty-state:hover {        // 显示AI思考状态
            opacity: 1;owAIThinking() {
        } document.createElement('div');
2 ai-thinking';
        /* 导出和清空按钮样式 */
        .action-button {ed-full bg-gradient-to-r from-longling to-tech-blue flex items-center justify-center flex-shrink-0">
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;lass="bg-gray-800/50 rounded-lg p-3 text-sm max-w-xs">
        }

        .action-button::after {g-dots">
            content: '';an>
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;atMessages.appendChild(thinkingElement);
            transform: translate(-50%, -50%);llHeight;
            transition: all 0.4s ease;
        }
        function hideAIThinking() {
        .action-button:hover::after { document.querySelector('.ai-thinking');
            width: 300px;
            height: 300px;move();
        }

        .action-button:hover {
            transform: translateY(-2px);        // 生成AI回复
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);generateAIResponse(message) {
        }

        /* 响应式设计增强 */珑凌科技智能助手，我很乐意为您服务！',
        @media (max-width: 768px) {,
            .emoji-panel {
                width: 90vw !important;
                max-width: 280px;oducts: [
                left: 50% !important;提供量子安全和人工智能解决方案，包括：\n• 量子密钥分发系统\n• AI对话系统\n• 智能分析平台\n• 自动化解决方案',
                right: auto !important;
                transform: translateX(-50%);
            }
chnology: [
            .history-panel {技术：\n• 量子密钥分发\n• 深度学习算法\n• 自然语言处理\n• 知识图谱构建\n• 云原生架构',
                width: 95vw !important;
                height: 80vh !important;术支持。'
                left: 2.5vw !important;
                right: auto !important;ntact: [
            }以下方式联系我们：\n📧 邮箱：contact@longling.tech\n📞 电话：400-888-8888\n📍 地址：广西壮族自治区防城港市',

            .chat-widget {
                width: 95vw !important;
                height: 80vh !important;lp: [
                left: 2.5vw !important;以帮您：\n• 了解产品和服务\n• 获取技术支持\n• 商务咨询\n• 使用帮助\n\n您可以输入 /help 查看更多命令。',
                right: auto !important;
                bottom: 2vh !important;
            }
        }fault: [
好的问题！让我为您详细解答...',
        /* 滚动条美化 */
        .custom-scrollbar::-webkit-scrollbar {...',
            width: 6px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(17, 24, 39, 0.3);            const messageLower = message.toLowerCase();
            border-radius: 3px;
        }let category = 'default';
('你好') || messageLower.includes('您好') || messageLower.includes('hello')) {
        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);ludes('产品') || messageLower.includes('服务') || messageLower.includes('解决方案')) {
            border-radius: 3px;
        }cludes('技术') || messageLower.includes('专利') || messageLower.includes('算法')) {

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {udes('联系') || messageLower.includes('电话') || messageLower.includes('邮箱')) {
            background: linear-gradient(to bottom, #9333ea, #2563eb);
        }ncludes('帮助') || messageLower.includes('help') || messageLower.includes('怎么')) {

        /* 加载动画 */
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; }            const categoryResponses = responses[category];
            50% { opacity: 1; }m() * categoryResponses.length)];
            80%, 100% { opacity: 0.2; }
        }
        // 处理特殊命令
        .loading-dots span {handleSpecialCommand(command) {
            animation: loading-dots 1.4s infinite;
        }
const cmd = command.toLowerCase().trim();
        .loading-dots span:nth-child(2) {
            animation-delay: 0.2s;
        }            switch (cmd) {
p':
        .loading-dots span:nth-child(3) {= `📋 可用命令：\n/help - 显示帮助\n/demo - 产品演示\n/human - 转人工客服\n/clear - 清空聊天\n/time - 显示时间\n/weather - 天气信息\n/status - 系统状态\n/joke - 随机笑话\n/quote - 励志名言\n/tips - 使用小贴士`;
            animation-delay: 0.4s;
        }o':
        = '🎬 产品演示功能开发中，敬请期待！您可以先浏览我们的官网了解更多信息。';
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {an':
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); } '🙋‍♀️ 正在为您转接人工客服，请稍候...';
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }
        }at('system', '抱歉，人工客服目前忙线中，您可以留下联系方式，我们会尽快回复您。');
        
        @keyframes neon-flicker {
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }ar':
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }(() => {
        }ages = chatMessages.querySelectorAll('.flex.items-start.space-x-2:not(.ai-thinking)');
        
        @keyframes slide-in-left {
            from { transform: translateX(-100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }':
        = `⏰ 当前时间：${new Date().toLocaleString('zh-CN')}`;
        @keyframes slide-in-right {
            from { transform: translateX(100%); opacity: 0; }ther':
            to { transform: translateX(0); opacity: 1; }🌤️ 天气查询功能开发中，建议您查看天气应用获取准确信息。';
        }
        tus':
        @keyframes scale-up {'💚 系统运行正常\n🔒 安全连接\n⚡ 响应速度：优秀\n📊 服务可用性：99.9%\n🤖 AI助手版本：v2.0';
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }e':
        }es = [
        ？因为光明会产生Bug！😄',
        @keyframes fade-in-up {3.我马上就改好 🤭',
            from { transform: translateY(30px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        sponse = jokes[Math.floor(Math.random() * jokes.length)];
        /* 动画类 */
        .float-animation { animation: float 6s ease-in-out infinite; }te':
        .wave-animation { animation: wave 4s ease-in-out infinite; }es = [
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }随者的重要标志。" - 史蒂夫·乔布斯 ✨',
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }sponse = quotes[Math.floor(Math.random() * quotes.length)];
        
        /* 触摸反馈 */s':
        .touch-feedback {s = [
            position: relative;快捷键 Ctrl+Enter 快速发送消息',
            overflow: hidden;
        }话',
        
        .touch-feedback::after {
            content: '';sponse = tips[Math.floor(Math.random() * tips.length)];
            position: absolute;
            top: 50%;
            left: 50%;onse = `❓ 未知命令：${command}\n输入 /help 查看可用命令。`;
            width: 0;
            height: 0;
            border-radius: 50%;            addMessageToChat('ai', response);
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;        // 添加消息到聊天窗口
        }MessageToChat(sender, message) {
        ement('div');
        .touch-feedback:active::after {2';
            width: 300px;
            height: 300px;const isUser = sender === 'user';
        }caleTimeString();
        
        /* 加载状态动画 */messageElement.innerHTML = `
        .loading-spinner {ded-full bg-gradient-to-r ${isUser ? 'from-blue-500 to-purple-500' : 'from-longling to-tech-blue'} flex items-center justify-center flex-shrink-0">
            display: inline-block;
            width: 20px;
            height: 20px;lass="bg-gray-800/50 rounded-lg p-3 text-sm max-w-xs">
            border: 2px solid rgba(108, 19, 255, 0.3);
            border-radius: 50%;xs text-gray-500 mt-1">
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }chatMessages.appendChild(messageElement);
        }ollHeight;
        
        /* 进度条动画 */            // 保存到历史记录
        .progress-bar {ory('chat', { sender, message, timestamp });
            position: relative;
            overflow: hidden;
        }        sendChatMessage.addEventListener('click', sendMessage);
        
        .progress-bar::after {        // 语音输入功能
            content: '';tening = false;
            position: absolute;
            top: 0;
            left: -100%;        // 初始化语音识别
            width: 100%;nitSpeechRecognition() {
            height: 100%;in window || 'SpeechRecognition' in window) {
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);chRecognition;
            animation: progress-shine 2s infinite;
        }
        recognition.lang = 'zh-CN';
        @keyframes progress-shine {lse;
            0% { left: -100%; }lse;
            100% { left: 100%; }
        }                recognition.onstart = function() {

        /* 3D 变换和悬停效果 */cument.getElementById('voiceInputBtn');
        .quantum-card {</i>';
            transform-style: preserve-3d;
            perspective: 1000px;说话...', 'info');
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }
                recognition.onresult = function(event) {
        .quantum-card:hover {0].transcript;
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);cript;
        }

        .quantum-text {
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);                recognition.onerror = function(event) {
            -webkit-background-clip: text;r);
            background-clip: text;);
            color: transparent;
            animation: quantum-flicker 2s infinite;
        }                recognition.onend = function() {

        .matrix-text {ument.getElementById('voiceInputBtn');
            font-family: 'Fira Code', monospace;
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);
        }

        /* 粒子系统容器 */
        .particle-container {
            position: absolute;        // 语音输入按钮事件
            width: 100%;tElementById('voiceInputBtn')?.addEventListener('click', function() {
            height: 100%;
            pointer-events: none;n('您的浏览器不支持语音识别功能', 'warning');
            overflow: hidden;
        }

        /* 高级滚动动画 */            if (isListening) {
        .scroll-reveal {op();
            opacity: 0;
            transform: translateY(20px);gnition.start();
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }

        .scroll-reveal.visible {        // 表情功能
            opacity: 1;mojis = [
            transform: translateY(0);'😄', '😁', '😆', '😅', '😂', '🤣', '😊', '😇',
        }

        /* 全局动画性能优化 */
        .hardware-accelerated,
        .product-card-enhanced,
        .footer-card-enhanced,
        .button-enhanced-primary,
        .button-enhanced-secondary,
        .feature-icon-enhanced,
        .stats-card-enhanced {
            will-change: transform, opacity;        let isEmojiPanelOpen = false;
            transform: translateZ(0);
            backface-visibility: hidden;        // 表情按钮事件
        }getElementById('emojiBtn')?.addEventListener('click', function(e) {
        
        /* 页面切换动画 */
        @keyframes pageSlideIn {
            from {
                opacity: 0;        function toggleEmojiPanel() {
                transform: translateY(30px);ument.getElementById('emojiPanel');
            }
            to {if (existingPanel) {
                opacity: 1;move();
                transform: translateY(0);e;
            }
        }
        
        .page-slide-in {            const emojiPanel = document.createElement('div');
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }ute bottom-full right-0 mb-2 bg-gray-800 rounded-lg p-3 shadow-lg z-50 w-64 max-h-48 overflow-y-auto';
        
        /* 滚动触发动画增强 */text-gray-400 mb-2">选择表情</div>
        .scroll-reveal-enhanced {
            opacity: 0;
            transform: translateY(40px) scale(0.95);i-btn w-8 h-8 text-xl hover:bg-gray-700 rounded text-center" data-emoji="${emoji}">${emoji}</button>
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        }
        
        .scroll-reveal-enhanced.visible {
            opacity: 1;            const chatInputContainer = document.getElementById('chatInput').parentElement;
            transform: translateY(0) scale(1);
        }
        
        .scroll-reveal-enhanced.visible.delay-1 {
            transition-delay: 0.1s;            // 表情点击事件
        }l.querySelectorAll('.emoji-btn').forEach(btn => {
        
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;mentById('chatInput');
        }
        
        .scroll-reveal-enhanced.visible.delay-3 {);
            transition-delay: 0.3s;alse;
        }
        
        /* 悬停态性能优化 */
        @media (hover: hover) {
            .product-card-enhanced:hover,        // 点击其他地方关闭表情面板
            .footer-card-enhanced:hover,ntListener('click', function() {
            .stats-card-enhanced:hover {
                transform: translateY(-8px) translateZ(0);document.getElementById('emojiPanel');
            }
        }move();
        alse;
        /* 移动端优化 */
        @media (max-width: 768px) {
            .product-card-enhanced,
            .footer-card-enhanced,
            .stats-card-enhanced {        // 快捷键支持
                transform: none !important;.getElementById('chatInput')?.addEventListener('keydown', function(e) {
            }
            
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {
                transform: none !important;        // 初始化语音识别
            }Recognition();
            
            /* 减少移动端动画 */        // 历史记录面板控制
            .feature-icon-enhanced:hover {ryPanel = document.getElementById('historyPanel');
                transform: translateY(-4px);
            }
                    // 显示历史记录面板
            .button-enhanced-primary:hover,owHistoryPanel() {
            .button-enhanced-secondary:hover {move('hidden');
                transform: translateY(-2px);
            }lassList.remove('-translate-x-full');
        }
        
        /* 减少动画首选项支持 */
        @media (prefers-reduced-motion: reduce) {
            *,
            *::before,        // 隐藏历史记录面板
            *::after {deHistoryPanel() {
                animation-duration: 0.01ms !important;d('-translate-x-full');
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;lassList.add('hidden');
                scroll-behavior: auto !important;
            }
            
            .product-card-enhanced,
            .footer-card-enhanced,        // AI助手按钮点击 - 显示历史面板或聊天窗口
            .stats-card-enhanced,aiAssistantBtn').addEventListener('click', () => {
            .feature-icon-enhanced {
                transform: none !important;
            }
        }
        HistoryPanel();
        /* 高性能滚动条样式 */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;        // 关闭历史面板
        }getElementById('closeHistoryPanel')?.addEventListener('click', hideHistoryPanel);
        
        ::-webkit-scrollbar-track {        // 历史记录存储和管理
            background: rgba(10, 0, 22, 0.3);eToHistory(type, data) {
            border-radius: 4px;Storage.getItem('userHistory') || '[]');
        }
        (),
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);
            border-radius: 4px;new Date().toISOString()
            border: 1px solid rgba(255, 255, 255, 0.1);
        }story.unshift(record);
        
        ::-webkit-scrollbar-thumb:hover {// 限制历史记录数量
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);.length > 100) {
        }
        
        /* Firefox滚动条 */
        html {localStorage.setItem('userHistory', JSON.stringify(history));
            scrollbar-width: thin;
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);
        }
                // 加载历史记录
        /* 文本选择样式 */loadHistoryRecords(filter = 'all') {
        ::selection {etItem('userHistory') || '[]');
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;
        }
        let filteredHistory = history;
        ::-moz-selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);istory.filter(record => record.type === filter);
            color: white;
        }
                    if (filteredHistory.length === 0) {
        /* 加载状态优化 */hidden');
        .loading-state {
            position: relative;tyHistory);
            overflow: hidden;
        }
        
        .loading-state::after {            emptyHistory.classList.add('hidden');
            content: '';
            position: absolute;
            top: 0;            filteredHistory.forEach(record => {
            left: -100%;toryElement(record);
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, 
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 50%,         // 创建历史记录元素
                transparent 100%);eateHistoryElement(record) {
            animation: loadingShimmer 1.5s infinite;ent('div');
        }50 rounded-lg border-l-4 border-tech-blue cursor-pointer hover:bg-gray-100 transition-colors';
        
        @keyframes loadingShimmer {const time = new Date(record.timestamp).toLocaleString();
            0% { left: -100%; }
            100% { left: 100%; }
        }
                    switch (record.type) {
        /* 历史记录面板增强样式 */
        .history-panel {fa-comments';
            backdrop-filter: blur(20px);记录:</strong> ${record.data.message.substring(0, 50)}${record.data.message.length > 50 ? '...' : ''}`;
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);n':
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);a-sign-in-alt';
        }</strong> ${record.data.username}`;

        .history-item {fication':
            transition: all 0.3s ease;;
            border: 1px solid transparent;g>通知:</strong> ${record.data.message}`;
            position: relative;
            overflow: hidden;
        } = 'fa-file';
g>记录:</strong> ${JSON.stringify(record.data)}`;
        .history-item::before {
            content: '';
            position: absolute;            element.innerHTML = `
            left: 0;items-start justify-between">
            top: 0;x-1">
            width: 3px;</i>
            height: 100%;
            background: transparent;-sm text-gray-800">${content}</div>
            transition: all 0.3s ease;v>
        }

        .history-item:hover {n class="delete-record text-gray-400 hover:text-red-500 transition-colors ml-2" data-id="${record.id}">
            background: rgba(75, 85, 99, 0.3);
            border-color: rgba(168, 85, 247, 0.3);
            transform: translateX(4px);
        }

        .history-item:hover::before {            // 删除记录事件
            background: linear-gradient(to bottom, #a855f7, #3b82f6);uerySelector('.delete-record').addEventListener('click', (e) => {
        }
record.id);
        .history-item.chat::before {
            background: #10b981;
        }            return element;

        .history-item.login::before {
            background: #3b82f6;        // 删除历史记录
        }deleteHistoryRecord(id) {
alStorage.getItem('userHistory') || '[]');
        .history-item.notification::before {
            background: #f59e0b;);
        }

        .filter-tag {
            transition: all 0.2s ease;
            cursor: pointer;        // 更新历史记录计数
            border: 1px solid rgba(255, 255, 255, 0.2);dateHistoryCount() {
        }localStorage.getItem('userHistory') || '[]');

        .filter-tag:hover {
            background: rgba(168, 85, 247, 0.2);xtContent = `共 ${history.length} 条记录`;
            border-color: rgba(168, 85, 247, 0.5);
            transform: scale(1.05);
        }
        // 历史记录搜索
        .filter-tag.active {getElementById('historySearch').addEventListener('input', function() {
            background: linear-gradient(45deg, #a855f7, #3b82f6);
            border-color: transparent;ll('.history-item');
            color: white;
        }historyItems.forEach(item => {
ent.toLowerCase();
        /* 搜索框样式 */
        .search-input {;
            background: rgba(17, 24, 39, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.2);.style.display = 'none';
            transition: all 0.3s ease;
        }

        .search-input:focus {            // 显示/隐藏清除搜索按钮
            background: rgba(17, 24, 39, 0.8); = document.getElementById('clearSearch');
            border-color: rgba(168, 85, 247, 0.5);
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);sList.remove('hidden');
        }
rBtn.classList.add('hidden');
        /* 空状态样式 */
        .empty-state {
            opacity: 0.7;
            transition: opacity 0.3s ease;        // 清除搜索
        }t.getElementById('clearSearch')?.addEventListener('click', function() {

        .empty-state:hover {
            opacity: 1;istory-item').forEach(item => {
        }

        /* 导出和清空按钮样式 */
        .action-button {
            transition: all 0.3s ease;        // 过滤器按钮
            position: relative;.querySelectorAll('.filter-btn').forEach(btn => {
            overflow: hidden;
        }
querySelectorAll('.filter-btn').forEach(b => {
        .action-button::after {hite');
            content: '';
            position: absolute;
            top: 50%;s.classList.add('active', 'bg-longling', 'text-white');
            left: 50%;
            width: 0;
            height: 0;                // 加载过滤后的记录
            background: rgba(255, 255, 255, 0.1);r = this.dataset.filter;
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: all 0.4s ease;
        }
        // 清空历史记录
        .action-button:hover::after {getElementById('clearHistory')?.addEventListener('click', function() {
            width: 300px;
            height: 300px;y');
        }

        .action-button:hover {录已清空', 'warning');
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }
        // 导出历史记录
        /* 响应式设计增强 */getElementById('exportHistory')?.addEventListener('click', function() {
        @media (max-width: 768px) {
            .emoji-panel {
                width: 90vw !important;ication/json'});
                max-width: 280px;
                left: 50% !important;const link = document.createElement('a');
                right: auto !important;;
                transform: translateX(-50%);OString().split('T')[0]}.json`;
            }

            .history-panel {.removeChild(link);
                width: 95vw !important;
                height: 80vh !important;showNotification('历史记录已导出', 'success');
                left: 2.5vw !important;
                right: auto !important;
            }        // 刷新历史记录
getElementById('refreshHistory')?.addEventListener('click', function() {
            .chat-widget {
                width: 95vw !important;录已刷新', 'info');
                height: 80vh !important;
                left: 2.5vw !important;
                right: auto !important;        // 无障碍功能示例
                bottom: 2vh !important;etElementById('toggleHighContrast').addEventListener('click', () => {
            }
        }

        /* 滚动条美化 */        document.getElementById('increaseFontSize').addEventListener('click', () => {
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }
        document.getElementById('decreaseFontSize').addEventListener('click', () => {
        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(17, 24, 39, 0.3);
            border-radius: 3px;
        }        document.getElementById('toggleAnimations').addEventListener('click', () => {

        .custom-scrollbar::-webkit-scrollbar-thumb {画' : '禁用动画';
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;
        }        document.getElementById('toggleScreenReader').addEventListener('click', () => {

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {);
            background: linear-gradient(to bottom, #9333ea, #2563eb);
        }
        // 页面滚动到锚点平滑滚动
        /* 加载动画 */SelectorAll('a[href^="#"]').forEach(anchor => {
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; }
            50% { opacity: 1; }
            80%, 100% { opacity: 0.2; }                document.querySelector(this.getAttribute('href')).scrollIntoView({
        }

        .loading-dots span {
            animation: loading-dots 1.4s infinite;
        }

        .loading-dots span:nth-child(2) {        // 初始化时给首页添加入场动画
            animation-delay: 0.2s;entById('home').classList.add('page-slide-in');
        }
// 初始化键盘快捷键
        .loading-dots span:nth-child(3) {dShortcuts();
            animation-delay: 0.4s;
        }// 等待量子球集成器准备就绪后设置首页量子球系统引用
        quantumChatIntegratorReady', (event) => {
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {引用...');
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }// 设置量子球系统引用
        }uantumParticleSystem) {
        
        @keyframes neon-flicker {tumParticleSystem.scene ? window.quantumParticleSystem.scene.children[0] : null,
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }s || [],
        
        @keyframes slide-in-left {
            from { transform: translateX(-100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }integrator.setQuantumSphereReference(quantumSphereObjects);
        }
        
        @keyframes slide-in-right {ole.warn('⚠️ 量子球粒子系统未找到');
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }t>
        
        @keyframes scale-up {    <!-- 量子动画系统 - 必需的DOM元素 -->
            from { transform: scale(0.8); opacity: 0; }ass="fixed inset-0 z-50 bg-black bg-opacity-50" style="display: none; align-items: center; justify-content: center;">
            to { transform: scale(1); opacity: 1; }
        }" class="relative mx-auto mb-4">
        
        @keyframes fade-in-up {
            from { transform: translateY(30px); opacity: 0; }d="validationText" class="text-white text-lg font-medium">
            to { transform: translateY(0); opacity: 1; }
        }
        
        /* 动画类 */
        .float-animation { animation: float 6s ease-in-out infinite; }
        .wave-animation { animation: wave 4s ease-in-out infinite; }    <style>
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }量子动画系统样式 */
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; } {
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }ter: blur(10px);
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }.quantum-ball {
        bsolute;
        /* 触摸反馈 */
        .touch-feedback {;
            position: relative;: 50%;
            overflow: hidden;gradient(circle at 30% 30%, #ffffff, #4f46e5);
        }
        ite;
        .touch-feedback::after {
            content: '';
            position: absolute;@keyframes quantumFloat {
            top: 50%; translateY(0px) scale(1); opacity: 0.8; }
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;@keyframes quantumSpin {
            background: rgba(255, 255, 255, 0.3);tate(0deg); }
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }
        @keyframes quantumPulse {
        .touch-feedback:active::after {: 0 0 20px rgba(79, 70, 229, 0.6); }
            width: 300px;
            height: 300px;
        }le>
        
        /* 加载状态动画 */    <!-- 登录系统验证器和增强功能 -->
        .loading-spinner {
            display: inline-block;ow.validateLoginSystem = function() {
            width: 20px;
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);
            border-radius: 50%;// 检查登录按钮
            border-top-color: #6C13FF;inBtn = document.getElementById('loginBtn');
            animation: spin 1s ease-in-out infinite;
        }('✅ 登录按钮找到');
        ;
        @keyframes spin {
            to { transform: rotate(360deg); }// 测试点击事件
        }
        onst rect = loginBtn.getBoundingClientRect();
        /* 进度条动画 */.y, width: rect.width, height: rect.height });
        .progress-bar {
            position: relative;
            overflow: hidden;rror('❌ 登录按钮位置检查失败:', e);
        }
        
        .progress-bar::after {e {
            content: '';ole.error('❌ 登录按钮未找到');
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;// 检查登录模态框
            height: 100%;nModal = document.getElementById('loginModal');
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;✅ 登录模态框找到');
        };
        
        @keyframes progress-shine {const isHidden = loginModal.classList.contains('hidden');
            0% { left: -100%; }
            100% { left: 100%; }
        }
ole.error('❌ 登录模态框未找到');
        /* 3D 变换和悬停效果 */
        .quantum-card {
            transform-style: preserve-3d;
            perspective: 1000px;// 检查showLoginModal函数
            transition: transform 0.5s cubic-bezier(0.23, 1, 0.32, 1);odal === 'function') {
        }
;
        .quantum-card:hover {
            transform: translateZ(20px) rotateX(5deg) rotateY(5deg);ole.error('❌ showLoginModal函数不存在');
        }

        .quantum-text {
            background: linear-gradient(90deg, #FF2B75, #6C13FF, #00D4FF);// 检查hideLoginModal函数
            -webkit-background-clip: text;odal === 'function') {
            background-clip: text;
            color: transparent;;
            animation: quantum-flicker 2s infinite;
        }ole.error('❌ hideLoginModal函数不存在');

        .matrix-text {
            font-family: 'Fira Code', monospace;
            text-shadow: 0 0 8px rgba(0, 212, 255, 0.8);console.log('📊 验证结果汇总:', results);
        }
// 提供快速修复选项
        /* 粒子系统容器 */kFixLogin = function() {
        .particle-container {;
            position: absolute;
            width: 100%;const loginBtn = document.getElementById('loginBtn');
            height: 100%;l');
            pointer-events: none;
            overflow: hidden;if (loginBtn && loginModal) {
        }
oveEventListener('click', window.loginClickHandler);
        /* 高级滚动动画 */
        .scroll-reveal {
            opacity: 0;按钮被点击');
            transform: translateY(20px);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);f (typeof showLoginModal === 'function') {
        }

        .scroll-reveal.visible {动显示模态框
            opacity: 1;.classList.remove('hidden');
            transform: translateY(0);
        }

        /* 全局动画性能优化 */ch (error) {
        .hardware-accelerated,('❌ 显示登录模态框失败:', error);
        .product-card-enhanced,
        .footer-card-enhanced,
        .button-enhanced-primary,ginBtn.addEventListener('click', window.loginClickHandler);
        .button-enhanced-secondary,
        .feature-icon-enhanced,
        .stats-card-enhanced {
            will-change: transform, opacity;// 确保关闭模态框功能
            transform: translateZ(0);uttons = loginModal.querySelectorAll('[onclick*="hideLoginModal"], .modal-close');
            backface-visibility: hidden;
        }ck', function() {
        
        /* 页面切换动画 */;
        @keyframes pageSlideIn {
            from {
                opacity: 0;
                transform: translateY(30px);
            }console.log('🎉 快速修复完成！');
            to {
                opacity: 1;
                transform: translateY(0);return results;
            }
        }
        // 自动运行验证
        .page-slide-in {addEventListener('DOMContentLoaded', function() {
            animation: pageSlideIn 0.8s cubic-bezier(0.23, 1, 0.32, 1);
        } 自动运行登录系统验证...');
        
        /* 滚动触发动画增强 */quickFixLogin() 进行快速修复');
        .scroll-reveal-enhanced {
            opacity: 0;// 验证背景管理系统
            transform: translateY(40px) scale(0.95);('🎨 验证统一背景管理器...');
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);ager !== 'undefined') {
        }
         {
        .scroll-reveal-enhanced.visible {;
            opacity: 1;ndow.backgroundManager.currentMode);
            transform: translateY(0) scale(1);;
        }
        // 提供背景模式切换功能
        .scroll-reveal-enhanced.visible.delay-1 {BackgroundMode = function(mode) {
            transition-delay: 0.1s;
        }itchMode(mode);
        
        .scroll-reveal-enhanced.visible.delay-2 {
            transition-delay: 0.2s;
        }
        window.testBackgroundToggle = function() {
        .scroll-reveal-enhanced.visible.delay-3 {
            transition-delay: 0.3s;quantum', 'hybrid'];
        }
        
        /* 悬停态性能优化 */const interval = setInterval(() => {
        @media (hover: hover) {s[currentIndex]);
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {if (currentIndex === 0) {
                transform: translateY(-8px) translateZ(0);l);
            });
        }
        00);
        /* 移动端优化 */
        @media (max-width: 768px) { {
            .product-card-enhanced,ole.warn('⚠️ 背景管理器实例未找到，尝试创建...');
            .footer-card-enhanced,
            .stats-card-enhanced {indow.backgroundManager = new UnifiedBackgroundManager();
                transform: none !important;
            }
            rror('❌ 创建背景管理器失败:', e);
            .product-card-enhanced:hover,
            .footer-card-enhanced:hover,
            .stats-card-enhanced:hover {e {
                transform: none !important;ole.error('❌ 统一背景管理器类未加载');
            }
            00);
            /* 减少移动端动画 */
            .feature-icon-enhanced:hover {
                transform: translateY(-4px);            // 增强的登录模态框错误处理
            }edShowLoginModal() {
            
            .button-enhanced-primary:hover,onsole.log('🔐 显示登录模态框...');
            .button-enhanced-secondary:hover {etElementById('loginModal');
                transform: translateY(-2px);
            }'❌ 登录模态框元素未找到');
        }
        
        /* 减少动画首选项支持 */
        @media (prefers-reduced-motion: reduce) {loginModal.classList.remove('hidden');
            *,
            *::before,idden';
            *::after {
                animation-duration: 0.01ms !important;// 聚焦到邮箱输入框
                animation-iteration-count: 1 !important;() => {
                transition-duration: 0.01ms !important;ut = document.getElementById('username');
                scroll-behavior: auto !important;
            }cus();
            邮箱输入框');
            .product-card-enhanced,
            .footer-card-enhanced,ole.warn('⚠️ 邮箱输入框未找到');
            .stats-card-enhanced,
            .feature-icon-enhanced {0);
                transform: none !important;
            }return true;
        }{
        ('❌ 显示登录模态框失败:', error);
        /* 高性能滚动条样式 */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }function enhancedHideLoginModal() {
        
        ::-webkit-scrollbar-track {onsole.log('🔐 隐藏登录模态框...');
            background: rgba(10, 0, 22, 0.3);etElementById('loginModal');
            border-radius: 4px;
        }'❌ 登录模态框元素未找到');
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(180deg, #6C13FF 0%, #00D4FF 100%);
            border-radius: 4px;const modalContent = loginModal.querySelector('.modal-content');
            border: 1px solid rgba(255, 255, 255, 0.1);
        }// 添加动画效果
        Content) {
        ::-webkit-scrollbar-thumb:hover {assList.remove('scale-100', 'opacity-100');
            background: linear-gradient(180deg, #8C33FF 0%, #20F4FF 100%);
        }
        
        /* Firefox滚动条 */setTimeout(() => {
        html {ssList.add('hidden');
            scrollbar-width: thin;;
            scrollbar-color: #6C13FF rgba(10, 0, 22, 0.3);
        }
        // 清空表单
        /* 文本选择样式 */oginForm = document.getElementById('login-form');
        ::selection {
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);set();
            color: white;单已重置');
        }
        
        ::-moz-selection {console.log('✅ 登录模态框已隐藏');
            background: linear-gradient(135deg, rgba(108, 19, 255, 0.3) 0%, rgba(0, 212, 255, 0.3) 100%);
            color: white;
        }return true;
        {
        /* 加载状态优化 */('❌ 隐藏登录模态框失败:', error);
        .loading-state {
            position: relative;
            overflow: hidden;
        }
        // 替换原有函数
        .loading-state::after {owLoginModal = enhancedShowLoginModal;
            content: '';
            position: absolute;
            top: 0;
            left: -100%;    <!-- 量子球容器 -->
            width: 100%;um-orb-container" class="fixed top-20 right-6 z-40 w-16 h-16 cursor-pointer opacity-80 hover:opacity-100 transition-all duration-300" style="display: none;">
            height: 100%;
            background: linear-gradient(90deg, dient-to-br from-tech-blue to-purple-500 rounded-full shadow-lg animate-pulse"></div>
                transparent 0%, 
                rgba(255, 255, 255, 0.1) 50%, 
                transparent 100%);
            animation: loadingShimmer 1.5s infinite;
        }
            <!-- 聊天容器 -->
        @keyframes loadingShimmer {-container" class="fixed bottom-6 right-6 z-40 w-80 h-96 bg-gradient-to-br from-gray-900 to-gray-800 rounded-lg shadow-2xl border border-tech-blue opacity-0 transform translate-y-8 transition-all duration-300" style="display: none;">
            0% { left: -100%; }
            100% { left: 100%; }
        }over:text-white transition-colors">
        
        /* 历史记录面板增强样式 */
        .history-panel {
            backdrop-filter: blur(20px);lass="chat-messages p-4 h-64 overflow-y-auto">
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.1);y-700 p-3 rounded-lg text-white text-sm">
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }

        .history-item {
            transition: all 0.3s ease;lass="chat-input p-4 border-t border-gray-700">
            border: 1px solid transparent;
            position: relative;d="chat-input" placeholder="输入消息..." class="flex-1 bg-gray-700 text-white p-2 rounded border border-gray-600 focus:border-tech-blue focus:outline-none">
            overflow: hidden;
        }

        .history-item::before {
            content: '';
            position: absolute;
            left: 0;
            top: 0;    <!-- 浮动聊天按钮 -->
            width: 3px;ng-chat-button" class="fixed bottom-6 right-6 z-50 w-14 h-14 bg-gradient-to-br from-tech-blue to-purple-500 rounded-full shadow-lg cursor-pointer hover:scale-110 transition-all duration-300 flex items-center justify-center">
            height: 100%;
            background: transparent;een-400 rounded-full animate-pulse"></div>
            transition: all 0.3s ease;
        }
    <!-- 登录模态框 -->
        .history-item:hover {-modal" class="fixed inset-0 z-50 hidden items-center justify-center p-4 bg-black bg-opacity-50">
            background: rgba(75, 85, 99, 0.3);x-w-md border border-tech-blue">
            border-color: rgba(168, 85, 247, 0.3);
            transform: translateX(4px);
        }录</h2>
00 hover:text-white transition-colors">
        .history-item:hover::before {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
        }

        .history-item.chat::before {lass="modal-body p-6">
            background: #10b981;ass="space-y-4">
        }
label for="login-email-alt" class="block text-sm font-medium text-gray-300 mb-2">邮箱地址</label>
        .history-item.login::before {
            background: #3b82f6;border-gray-600 rounded-md text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-tech-blue focus:border-transparent">
        }

        .history-item.notification::before {label for="login-password-alt" class="block text-sm font-medium text-gray-300 mb-2">密码</label>
            background: #f59e0b;
        }-gray-600 rounded-md text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-tech-blue focus:border-transparent">

        .filter-tag {lass="flex items-center justify-between">
            transition: all 0.2s ease;
            cursor: pointer;"rounded border-gray-600 text-tech-blue focus:ring-tech-blue">
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
"#" class="text-sm text-tech-blue hover:underline">忘记密码？</a>
        .filter-tag:hover {
            background: rgba(168, 85, 247, 0.2);n type="submit" class="w-full bg-tech-blue text-white py-2 px-4 rounded-md hover:bg-blue-600 transition-colors font-medium">
            border-color: rgba(168, 85, 247, 0.5);
            transform: scale(1.05);on>
        }
ass="mt-6 text-center">
        .filter-tag.active {没有账户？ 
            background: linear-gradient(45deg, #a855f7, #3b82f6);s="text-tech-blue hover:underline">立即注册</a>
            border-color: transparent;
            color: white;
        }

        /* 搜索框样式 */
        .search-input {
            background: rgba(17, 24, 39, 0.6);    <!-- 量子球容器 -->
            border: 1px solid rgba(255, 255, 255, 0.2);um-orb-container" class="fixed bottom-6 right-6 z-50">
            transition: all 0.3s ease;to-r from-purple-500 to-blue-500 shadow-lg cursor-pointer hover:scale-110 transition-all duration-300 flex items-center justify-center">
        }

        .search-input:focus {
            background: rgba(17, 24, 39, 0.8);
            border-color: rgba(168, 85, 247, 0.5);    <!-- 聊天界面 -->
            box-shadow: 0 0 0 3px rgba(168, 85, 247, 0.1);-interface" class="fixed bottom-24 right-6 w-80 h-96 bg-gray-800 rounded-lg shadow-xl z-40 transform scale-0 transition-all duration-300 origin-bottom-right">
        }
er-b border-gray-700">
        /* 空状态样式 */
        .empty-state { hover:text-white">
            opacity: 0.7;
            transition: opacity 0.3s ease;
        }
hat-messages" class="flex-1 p-4 overflow-y-auto max-h-64">
        .empty-state:hover {
            opacity: 1;
        }

        /* 导出和清空按钮样式 */lass="p-4 border-t border-gray-700">
        .action-button {
            transition: all 0.3s ease;hat-input" placeholder="输入您的问题..." 
            position: relative;ed-md focus:outline-none focus:ring-2 focus:ring-purple-500">
            overflow: hidden;
        }

        .action-button::after {
            content: '';
            position: absolute; <!-- chat-widget 结束 -->
            top: 50%;
            left: 50%;
            width: 0;    <!-- 极简背景容器 -->
            height: 0;list-background" class="fixed inset-0 z-0 opacity-50 pointer-events-none">
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: all 0.4s ease;
        }    <!-- 前端修复调试面板 -->
nel" class="fixed bottom-20 right-4 w-80 bg-gray-900/95 backdrop-blur-sm border border-gray-600 rounded-lg p-4 text-sm z-50 hidden">
        .action-button:hover::after {
            width: 300px;4>
            height: 300px;nel').classList.add('hidden')" class="text-gray-400 hover:text-white">
        }

        .action-button:hover {
            transform: translateY(-2px);d="debug-info" class="text-xs text-gray-300">
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
        }
lass="mt-3 flex gap-2">
        /* 响应式设计增强 */FrontendFixes()" class="px-2 py-1 bg-blue-600 text-white rounded text-xs hover:bg-blue-700">
        @media (max-width: 768px) {
            .emoji-panel {>
                width: 90vw !important;nclick="document.getElementById('debug-panel').classList.add('hidden')" class="px-2 py-1 bg-gray-600 text-white rounded text-xs hover:bg-gray-700">
                max-width: 280px;
                left: 50% !important;on>
                right: auto !important;
                transform: translateX(-50%);
            }
    <!-- 调试面板开关按钮 -->
            .history-panel { bottom-4 right-4 z-50">
                width: 95vw !important;yId('debug-panel').classList.toggle('hidden')" 
                height: 80vh !important;w-lg transition-all">
                left: 2.5vw !important;
                right: auto !important;
            }

            .chat-widget {    <script src="assets/js/system-health-checker.js"></script>\n</body>
                width: 95vw !important;                height: 80vh !important;
                left: 2.5vw !important;
                right: auto !important;
                bottom: 2vh !important;
            }
        }

        /* 滚动条美化 */
        .custom-scrollbar::-webkit-scrollbar {
            width: 6px;
        }

        .custom-scrollbar::-webkit-scrollbar-track {
            background: rgba(17, 24, 39, 0.3);
            border-radius: 3px;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb {
            background: linear-gradient(to bottom, #a855f7, #3b82f6);
            border-radius: 3px;
        }

        .custom-scrollbar::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(to bottom, #9333ea, #2563eb);
        }

        /* 加载动画 */
        @keyframes loading-dots {
            0%, 20% { opacity: 0.2; }
            50% { opacity: 1; }
            80%, 100% { opacity: 0.2; }
        }

        .loading-dots span {
            animation: loading-dots 1.4s infinite;
        }

        .loading-dots span:nth-child(2) {
            animation-delay: 0.2s;
        }

        .loading-dots span:nth-child(3) {
            animation-delay: 0.4s;
        }
        
        /* AI助手按钮发光动画 */
        @keyframes ai-glow {
            0%, 100% { box-shadow: 0 0 20px rgba(108, 19, 255, 0.5), 0 0 40px rgba(0, 212, 255, 0.2); }
            50% { box-shadow: 0 0 40px rgba(108, 19, 255, 0.7), 0 0 60px rgba(0, 212, 255, 0.3); }
        }
        
        @keyframes neon-flicker {
            0%, 100% { opacity: 1; text-shadow: 0 0 8px currentColor; }
            50% { opacity: 0.8; text-shadow: 0 0 12px currentColor, 0 0 24px currentColor; }
        }
        
        @keyframes slide-in-left {
            from { transform: translateX(-100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        @keyframes slide-in-right {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        
        @keyframes scale-up {
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }
        
        @keyframes fade-in-up {
            from { transform: translateY(30px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        
        /* 动画类 */
        .float-animation { animation: float 6s ease-in-out infinite; }
        .wave-animation { animation: wave 4s ease-in-out infinite; }
        .glow-animation { animation: glow-pulse 3s ease-in-out infinite; }
        .flicker-animation { animation: neon-flicker 2s ease-in-out infinite; }
        .slide-in-left { animation: slide-in-left 0.8s ease-out; }
        .slide-in-right { animation: slide-in-right 0.8s ease-out; }
        .scale-up { animation: scale-up 0.6s ease-out; }
        .fade-in-up { animation: fade-in-up 0.8s ease-out; }
        
        /* 触摸反馈 */
        .touch-feedback {
            position: relative;
            overflow: hidden;
        }
        
        .touch-feedback::after {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            transform: translate(-50%, -50%);
            transition: width 0.6s, height 0.6s;
        }
        
        .touch-feedback:active::after {
            width: 300px;
            height: 300px;
        }
        
        /* 加载状态动画 */
        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(108, 19, 255, 0.3);
            border-radius: 50%;
            border-top-color: #6C13FF;
            animation: spin 1s ease-in-out infinite;
        }
        
        @keyframes spin {
            to { transform: rotate(360deg); }
        }
        
        /* 进度条动画 */
        .progress-bar {
            position: relative;
            overflow: hidden;
        }
        
        .progress-bar::after {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: progress-shine 2s infinite;
        }
        
        @keyframes progress-shine {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        /* 3D 变换和悬停效果 */